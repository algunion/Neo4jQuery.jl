var documenterSearchIndex = {"docs":
[{"category":"section","location":"guide/connections/#connections","page":"Connections","text":"Neo4jQuery connects to Neo4j over HTTP using the Query API v2.","title":"Connections"},{"category":"section","location":"guide/connections/#Connecting-with-connect","page":"Connections","text":"conn = connect(host, database;\n    port=7474,\n    auth=BasicAuth(\"neo4j\", \"password\"),\n    scheme=\"http\",\n    readtimeout=120,\n    connect_timeout=10)\n\nOn construction the client hits the discovery endpoint (GET /) to verify the server is reachable.\n\nArguments:\n\nParameter Type Default Description\nhost String required Hostname or IP\ndatabase String required Database name (e.g. \"neo4j\")\nport Int 7474 HTTP port\nauth AbstractAuth required Authentication strategy\nscheme String \"http\" \"http\" or \"https\"\nreadtimeout Int 120 Seconds to wait for a response (0 = no limit)\nconnect_timeout Int 10 Seconds to wait for the connection (0 = no explicit bound)","title":"Connecting with connect"},{"category":"section","location":"guide/connections/#Client-timeouts","page":"Connections","text":"Every request is bounded by two client-side timeouts, carried on the connection and settable on connect/connect_from_env:\n\nreadtimeout (default 120s) — how long to wait for the server's response. If it fires, the call raises a typed Neo4jHTTPError (status 0, message \"request timed out …\") instead of hanging the caller indefinitely. 0 disables the read timeout (wait forever).\nconnect_timeout (default 10s) — how long to wait for the TCP/TLS connection to be established. 0 removes this explicit bound; the underlying HTTP layer then falls back to bounding connect by readtimeout when readtimeout > 0, so a fully unbounded connect requires setting both connect_timeout = 0 and readtimeout = 0.\n\nOverride the read timeout for a single call with the timeout keyword on query, stream, read_query, and read_stream — useful when one query is expected to run longer (or must fail faster) than the connection default:\n\n# This call gets 5s; the rest of the connection keeps its 120s default.\nresult = query(conn, \"MATCH (n) RETURN count(n) AS c\"; timeout=5)\n\nA timed-out request is not retried, even for a read: the underlying HTTP layer treats a timeout as unrecoverable, so it surfaces immediately as the typed error rather than doubling the wall-clock wait.\n\nnote: Client vs. server timeouts\nThese are client-side timeouts — they abort the client's wait, but do not tell Neo4j to stop executing. To bound server-side execution, pass max_execution_time (seconds) to query/stream/read_query/read_stream/begin_transaction: the database itself aborts a pathological query after that budget and returns a Cypher error (requires Neo4j 2026.04+; older servers reject the unknown field — that server error is the intended signal). Set the client timeout larger than max_execution_time so the server's own abort wins and surfaces as a clean error, instead of the client giving up first and leaving the query running. The companion tx_metadata keyword (arbitrary metadata stamped on the transaction, visible in SHOW TRANSACTIONS / the query log) rides the same five carriers; cypher_version (pin the Cypher language version, 5 or 25) rides query/stream/read_query/read_stream but not begin_transaction. On read_query/read_stream all of these pass through alongside the read-only guard.","title":"Client timeouts"},{"category":"section","location":"guide/connections/#Connecting-from-the-environment:-connect_from_env","page":"Connections","text":"Loads connection details from environment variables, optionally reading them from a .env file first:\n\nconn = connect_from_env()\nprintln(conn)\n\nExpected variables (with default prefix NEO4J_):\n\nVariable Description\nNEO4J_URI Full URI, e.g. neo4j+s://xxx.databases.neo4j.io\nNEO4J_USERNAME Username\nNEO4J_PASSWORD Password\nNEO4J_DATABASE Database name\n\nThe URI scheme is parsed to determine HTTP vs HTTPS:\n\nneo4j+s://, neo4j+ssc://, bolt+s://, bolt+ssc:// → HTTPS\nneo4j://, bolt:// → HTTP","title":"Connecting from the environment: connect_from_env"},{"category":"section","location":"guide/connections/#Authentication","page":"Connections","text":"Two strategies are available:\n\n# HTTP Basic Auth (RFC 7617)\nauth = BasicAuth(\"neo4j\", \"password\")\nprintln(auth)\n\n# Bearer token auth\nauth = BearerAuth(\"eyJhbGciOi...\")\nprintln(auth)\n\nBoth produce the Authorization header pair used on every request. Implement auth_header(::YourAuth) for custom strategies.","title":"Authentication"},{"category":"section","location":"guide/connections/#The-dotenv-helper","page":"Connections","text":"You can load .env files independently:\n\nvars = dotenv(\".env\"; overwrite=false)\n\nSupports comments (#), quoted values. Existing ENV keys are preserved unless overwrite=true.","title":"The dotenv helper"},{"category":"section","location":"guide/connections/#.env-file-format","page":"Connections","text":"# Comment lines are ignored\nNEO4J_URI=neo4j+s://xxx.databases.neo4j.io\nNEO4J_USERNAME=neo4j\nNEO4J_PASSWORD=secret\nNEO4J_DATABASE=neo4j\n\n# Quoted values (quotes are stripped)\nNEO4J_PASSWORD=\"my secret password\"\nNEO4J_PASSWORD='my secret password'","title":".env file format"},{"category":"section","location":"guide/connections/#URI-scheme-mapping","page":"Connections","text":"The scheme in NEO4J_URI is mapped to HTTP or HTTPS:\n\nURI Scheme Protocol Default Port\nneo4j:// HTTP 7474\nbolt:// HTTP 7474\nneo4j+s:// HTTPS 443\nneo4j+ssc:// HTTPS 443\nbolt+s:// HTTPS 443\nbolt+ssc:// HTTPS 443\n\nAn explicit port in the URI overrides the default, e.g. neo4j+s://host:7688 → HTTPS on port 7688.","title":"URI scheme mapping"},{"category":"section","location":"guide/connections/#Custom-authentication","page":"Connections","text":"To implement a custom auth strategy, define a struct that subtypes AbstractAuth and implement auth_header, returning the full header pair (\"Authorization\" => value):\n\nstruct ApiKeyAuth <: Neo4jQuery.AbstractAuth\n    key::String\nend\n\nNeo4jQuery.auth_header(a::ApiKeyAuth) = \"Authorization\" => \"ApiKey $(a.key)\"\n\nconn = connect(\"localhost\", \"neo4j\"; auth=ApiKeyAuth(\"my-api-key\"))","title":"Custom authentication"},{"category":"section","location":"guide/connections/#Verifying-a-connection","page":"Connections","text":"connect hits the discovery endpoint on construction. If the server is unreachable, it will throw an error immediately:\n\ntry\n    conn = connect(\"localhost\", \"neo4j\"; auth=BasicAuth(\"neo4j\", \"pw\"))\n    println(\"Connected to: \", conn.base_url)\ncatch e\n    println(\"Connection failed: \", e)\nend","title":"Verifying a connection"},{"category":"section","location":"guide/connections/#Displaying-a-connection","page":"Connections","text":"conn = connect(\"localhost\", \"neo4j\"; auth=BasicAuth(\"neo4j\", \"pw\"))\nprintln(conn)  # Neo4jConnection(http://localhost:7474/db/neo4j)","title":"Displaying a connection"},{"category":"section","location":"api/#api-reference","page":"API Reference","text":"Full reference for all public types and functions.","title":"API Reference"},{"category":"section","location":"api/#Connection","page":"API Reference","text":"","title":"Connection"},{"category":"section","location":"api/#Authentication","page":"API Reference","text":"auth_header is public but unexported — call or extend it as Neo4jQuery.auth_header.","title":"Authentication"},{"category":"section","location":"api/#Environment","page":"API Reference","text":"","title":"Environment"},{"category":"section","location":"api/#Query","page":"API Reference","text":"","title":"Query"},{"category":"section","location":"api/#Transactions","page":"API Reference","text":"","title":"Transactions"},{"category":"section","location":"api/#Streaming","page":"API Reference","text":"","title":"Streaming"},{"category":"section","location":"api/#Read-Only-Guard","page":"API Reference","text":"","title":"Read-Only Guard"},{"category":"section","location":"api/#Read-only-classifier-(internal)","page":"API Reference","text":"ReadOnlyConnection documents its refusal semantics in terms of the internal lexical classifier — included here so those references resolve:","title":"Read-only classifier (internal)"},{"category":"section","location":"api/#Introspection","page":"API Reference","text":"The GraphSchema field types are public but unexported — reference them as Neo4jQuery.PropertyInfo etc.","title":"Introspection"},{"category":"section","location":"api/#GraphRAG-(vector-search)","page":"API Reference","text":"","title":"GraphRAG (vector search)"},{"category":"section","location":"api/#Result-Types","page":"API Reference","text":"","title":"Result Types"},{"category":"section","location":"api/#Graph-Types","page":"API Reference","text":"","title":"Graph Types"},{"category":"section","location":"api/#Wire-Format-(Typed-JSON)","page":"API Reference","text":"to_typed_json is public but unexported — call it as Neo4jQuery.to_typed_json.","title":"Wire Format (Typed JSON)"},{"category":"section","location":"api/#Errors","page":"API Reference","text":"","title":"Errors"},{"category":"section","location":"api/#DSL-—-Schema","page":"API Reference","text":"","title":"DSL — Schema"},{"category":"section","location":"api/#DSL-—-Macros","page":"API Reference","text":"","title":"DSL — Macros"},{"category":"type","location":"api/#Neo4jQuery.Neo4jConnection","page":"API Reference","text":"Neo4jConnection\n\nRepresents a connection to a Neo4j database via the Query API v2.\n\nCreate one with connect rather than calling the constructor directly.\n\nCarries the client-side timeouts applied to every request (F-10):\n\nreadtimeout::Int — seconds to wait for the response before the request is aborted with a typed Neo4jHTTPError; 0 disables the read timeout (wait indefinitely). Overridable per call via the timeout kwarg of query/stream/read_query/read_stream.\nconnect_timeout::Int — seconds to wait for the TCP/TLS connection to be established. 0 removes this explicit bound; HTTP.jl then falls back to bounding connect by readtimeout when readtimeout > 0, so a fully unbounded connect requires BOTH connect_timeout = 0 and readtimeout = 0.\n\nBoth timeouts must be >= 0; the constructor (and therefore connect/ connect_from_env) throws ArgumentError for negative values — negative sentinels are internal to the request layer and must never enter here.\n\nThe 3-argument constructor keeps the documented defaults (120s / 10s): Neo4jConnection(base_url, database, auth).\n\n\n\n\n\n","title":"Neo4jQuery.Neo4jConnection"},{"category":"function","location":"api/#Neo4jQuery.connect","page":"API Reference","text":"connect(host, database; port=7474, auth, scheme=\"http\",\n        readtimeout=120, connect_timeout=10) -> Neo4jConnection\n\nEstablish a connection to a Neo4j instance.  Validates connectivity by hitting the discovery endpoint (GET /).\n\nreadtimeout/connect_timeout (seconds) bound every subsequent request — and the discovery request itself; see Neo4jConnection for their exact semantics (0 = no read timeout / no explicit connect bound; negative values throw ArgumentError). A fired read timeout — including during discovery — surfaces as a Neo4jHTTPError rather than hanging the caller.\n\nExample\n\nconn = connect(\"localhost\", \"neo4j\"; auth=BasicAuth(\"neo4j\", \"password\"))\n\n\n\n\n\n","title":"Neo4jQuery.connect"},{"category":"function","location":"api/#Neo4jQuery.connect_from_env","page":"API Reference","text":"connect_from_env(; path=\".env\", prefix=\"NEO4J_\") -> Neo4jConnection\n\nConvenience constructor that loads credentials from environment variables (optionally reading a .env file first) and returns a ready-to-use connection.\n\nExpected variables (with default NEO4J_ prefix):\n\nNEO4J_URI      — full URI, e.g. neo4j+s://xxx.databases.neo4j.io\nNEO4J_USERNAME — e.g. neo4j\nNEO4J_PASSWORD — the password\nNEO4J_DATABASE — e.g. neo4j  (defaults to \"neo4j\" if unset)\n\nThe URI scheme is mapped automatically:\n\nneo4j+s://, neo4j+ssc:// → HTTPS (port 443)\nneo4j://, bolt:// → HTTP (port 7474)\n\nExample\n\nconn = connect_from_env()                    # reads .env, connects\nconn = connect_from_env(path=\"prod.env\")     # different file\n\nreadtimeout/connect_timeout (seconds) set the client-side timeouts on the returned connection; see Neo4jConnection for their semantics (F-10). Negative values throw ArgumentError — validated up front, before any .env/ENV access.\n\n\n\n\n\n","title":"Neo4jQuery.connect_from_env"},{"category":"type","location":"api/#Neo4jQuery.AbstractAuth","page":"API Reference","text":"AbstractAuth\n\nAbstract type for authentication strategies used to authorize Neo4j requests.\n\n\n\n\n\n","title":"Neo4jQuery.AbstractAuth"},{"category":"type","location":"api/#Neo4jQuery.BasicAuth","page":"API Reference","text":"BasicAuth(username::String, password::String)\n\nHTTP Basic authentication (RFC 7617).  Generates an Authorization: Basic … header from the supplied credentials.\n\nExample\n\nauth = BasicAuth(\"neo4j\", \"verysecret\")\n\n\n\n\n\n","title":"Neo4jQuery.BasicAuth"},{"category":"type","location":"api/#Neo4jQuery.BearerAuth","page":"API Reference","text":"BearerAuth(token::String)\n\nHTTP Bearer-token authentication.  Generates an Authorization: Bearer … header in the Query API's wire format Bearer <base64(token)> — the token is base64-wrapped on the way out, per the Query API authentication spec.\n\nPass the raw SSO token; do not pre-encode it (a pre-encoded token would be double-wrapped and rejected by the server).\n\nExample\n\nauth = BearerAuth(\"xbhkjnlvianztghqwawxqfe\")   # raw token, as issued by the SSO provider\n\n\n\n\n\n","title":"Neo4jQuery.BearerAuth"},{"category":"function","location":"api/#Neo4jQuery.auth_header","page":"API Reference","text":"auth_header(auth::AbstractAuth) -> Pair{String,String}\n\nReturn the Authorization header pair for a given authentication strategy.\n\n\n\n\n\n","title":"Neo4jQuery.auth_header"},{"category":"function","location":"api/#Neo4jQuery.dotenv","page":"API Reference","text":"dotenv(path=\".env\"; overwrite=false) -> Dict{String,String}\n\nParse a .env file and load its key-value pairs into ENV. Returns the parsed dictionary.\n\nLines starting with # are treated as comments and ignored. Empty lines are skipped.  Values may optionally be quoted with \" or '.\n\nIf overwrite is false (default), existing ENV entries are not overwritten—the file values serve as defaults.\n\nExample\n\ndotenv()                        # loads .env from current directory\ndotenv(\"config/.env.test\")      # loads a specific file\n\n\n\n\n\n","title":"Neo4jQuery.dotenv"},{"category":"function","location":"api/#Neo4jQuery.query","page":"API Reference","text":"query(conn, statement; parameters, access_mode, include_counters, bookmarks, impersonated_user, max_execution_time, tx_metadata, cypher_version, timeout) -> QueryResult\n\nExecute a Cypher statement against the database using an implicit transaction.\n\nArguments\n\nconn::Neo4jConnection — the connection to use.\nstatement::String — the Cypher query text.  Use {{param}} placeholders for parameters (converted to $param for Neo4j), or the traditional \\$param escape if you prefer.\n\nKeyword Arguments\n\nparameters::Dict{String,Any}=Dict{String,Any}() — query parameters.\naccess_mode::Symbol=:write — :read or :write for cluster routing. :read requests also auto-retry a transient transport failure (e.g. a stale pooled connection) once, since a read is provably side-effect-free (access_mode=:read is enforced server-side, not just by client intent). :write (the default) never retries, to avoid double-applying a mutation.\ninclude_counters::Bool=false — whether to request query counters.\nbookmarks::Vector{String}=String[] — bookmarks for causal consistency.\nimpersonated_user::Union{String,Nothing}=nothing — run as another user.\nmax_execution_time::Union{Int,Nothing}=nothing — server-side execution budget in seconds (requires Neo4j 2026.04+). When set (must be > 0), the database itself aborts the query after this wall-clock time and returns a Cypher error. This is the server half of F-10; the client-side timeout is the client half. Set the client timeout larger than max_execution_time so the server's own abort wins and surfaces as a clean error, rather than the client giving up first and leaving the query running. Older servers reject the unknown field — that server error is the intended signal. nothing omits it.\ntx_metadata::Union{AbstractDict,Nothing}=nothing — metadata stamped on the transaction (visible in SHOW TRANSACTIONS and the query log), e.g. Dict(\"app\" => \"qa\") (requires Neo4j 2026.04+). Keys are coerced to String; values are serialized as typed-JSON. nothing omits it.\ncypher_version::Union{Int,Nothing}=nothing — pin the Cypher language version for this statement only by prepending a CYPHER <version> prefix. Valid values are 5 and 25; any other throws ArgumentError. nothing (the default) sends the statement untouched, letting the database's default Cypher version apply — that per-DB default is a separate server setting, not set from the client.\ntimeout::Union{Int,Nothing}=nothing — per-call read-timeout override in seconds (F-10). nothing uses the connection's readtimeout; an integer overrides it for this call only (0 = wait indefinitely). A fired timeout raises Neo4jHTTPError(0, \"request timed out …\") instead of hanging.\n\nExample\n\n# Recommended: Mustache-style placeholders (no escaping needed)\nresult = query(conn,\n    \"MATCH (p:Person) WHERE p.age > {{min_age}} RETURN p.name AS name\",\n    parameters=Dict{String,Any}(\"min_age\" => 25))\n\n# Also works: escaped $ (traditional Cypher style)\nresult = query(conn,\n    \"MATCH (p:Person) WHERE p.age > \\$min_age RETURN p.name AS name\",\n    parameters=Dict{String,Any}(\"min_age\" => 25))\n\n# Best: use the cypher\"\" string macro for automatic parameter capture\nmin_age = 25\nq = cypher\"MATCH (p:Person) WHERE p.age > $min_age RETURN p.name AS name\"\nresult = query(conn, q)\n\n\n\n\n\nquery(conn, q::CypherQuery; kwargs...) -> QueryResult\n\nExecute a CypherQuery (typically from the @cypher_str macro). Parameters from the CypherQuery are merged with any extra parameters kwarg (the kwarg takes precedence on conflicts).\n\n\n\n\n\nquery(tx::Transaction, statement; parameters, include_counters) -> QueryResult\n\nExecute a Cypher statement inside an open transaction.\n\n\n\n\n\n","title":"Neo4jQuery.query"},{"category":"type","location":"api/#Neo4jQuery.CypherQuery","page":"API Reference","text":"CypherQuery\n\nA Cypher query statement together with its parameter bindings.  Typically constructed via the @cypher_str string macro.\n\nFields\n\nstatement::String — the Cypher text (with $param placeholders)\nparameters::Dict{String,Any} — parameter name → Julia value\n\n\n\n\n\n","title":"Neo4jQuery.CypherQuery"},{"category":"macro","location":"api/#Neo4jQuery.@cypher_str","page":"API Reference","text":"@cypher_str -> CypherQuery\n\nCreate a CypherQuery from a Cypher string literal, automatically capturing local variables referenced with $ as query parameters.\n\nJulia does not interpolate $ inside non-standard string literals, so the $ is passed through verbatim and the macro can detect it as a Cypher parameter reference.\n\nExample\n\nname = \"Alice\"\nage  = 42\nq = cypher\"MATCH (n:Person {name: $name, age: $age}) RETURN n\"\n# CypherQuery(\"MATCH (n:Person {name: $name, age: $age}) RETURN n\", 2 parameters)\n# q.parameters == Dict(\"name\" => \"Alice\", \"age\" => 42)\n\nThe resulting query uses parameterised Cypher, which is both safer (no injection) and faster (query plan caching).\n\n\n\n\n\n","title":"Neo4jQuery.@cypher_str"},{"category":"type","location":"api/#Neo4jQuery.Transaction","page":"API Reference","text":"Transaction\n\nAn explicit Neo4j transaction.  Obtain one via begin_transaction or the transaction do-block helper.\n\n\n\n\n\n","title":"Neo4jQuery.Transaction"},{"category":"function","location":"api/#Neo4jQuery.begin_transaction","page":"API Reference","text":"begin_transaction(conn; statement=nothing, parameters=Dict{String,Any}(), bookmarks=String[], max_execution_time=nothing, tx_metadata=nothing) -> Transaction\n\nOpen a new explicit transaction, optionally executing an initial statement.\n\nThe statement keyword accepts a plain String, a CypherQuery (from cypher\"...\"), or nothing.\n\nbookmarks::Vector{String} supplies causal-consistency bookmarks (typically the return value of a prior commit!): the new transaction is guaranteed to observe every write acknowledged by those bookmarks. An empty vector (the default) omits the key entirely, so unbookmarked transactions are unaffected.\n\nmax_execution_time::Union{Int,Nothing} (seconds, > 0) and tx_metadata::Union{AbstractDict,Nothing} set the whole transaction's server-side execution budget and metadata — see query for the semantics. Both require Neo4j 2026.04+ (older servers reject the unknown fields); nothing (the default) omits them, and they carry on all three statement branches.\n\nExamples\n\ntx = begin_transaction(conn)\nresult = query(tx, cypher\"CREATE (n:Person {name: $name}) RETURN n\")\ncommit!(tx)\n\n# With an initial statement\nname = \"Alice\"\ntx = begin_transaction(conn; statement=cypher\"CREATE (n:Person {name: $name}) RETURN n\")\ncommit!(tx)\n\n# Causal chaining: tx2 sees everything tx1 committed\nbookmarks = commit!(tx1)\ntx2 = begin_transaction(conn; bookmarks=bookmarks)\n\n\n\n\n\n","title":"Neo4jQuery.begin_transaction"},{"category":"function","location":"api/#Neo4jQuery.commit!","page":"API Reference","text":"commit!(tx; statement=nothing, parameters=Dict{String,Any}()) -> Vector{String}\n\nCommit an open transaction, optionally executing a final statement. Returns the bookmarks from the committed transaction.\n\nThe statement keyword accepts a plain String, a CypherQuery (from cypher\"...\"), or nothing.\n\nExample\n\ntx = begin_transaction(conn)\nbookmarks = commit!(tx; statement=cypher\"CREATE (n:Final) RETURN n\")\n\n\n\n\n\n","title":"Neo4jQuery.commit!"},{"category":"function","location":"api/#Neo4jQuery.rollback!","page":"API Reference","text":"rollback!(tx::Transaction) -> Nothing\n\nRoll back an open transaction, discarding all changes.\n\n\n\n\n\n","title":"Neo4jQuery.rollback!"},{"category":"function","location":"api/#Neo4jQuery.transaction","page":"API Reference","text":"transaction(f, conn; kwargs...) -> result\n\nExecute f(tx) inside an explicit transaction.  The transaction is committed if f returns normally, or rolled back if an exception is thrown.\n\nExample\n\ntransaction(conn) do tx\n    query(tx, \"CREATE (a:Person {name: 'Alice'})\")\n    query(tx, \"CREATE (b:Person {name: 'Bob'})\")\nend  # auto-commit\n\n\n\n\n\n","title":"Neo4jQuery.transaction"},{"category":"function","location":"api/#Neo4jQuery.stream","page":"API Reference","text":"stream(conn, statement; parameters, access_mode, bookmarks, impersonated_user, max_execution_time, tx_metadata, cypher_version) -> StreamingResult\n\nExecute a Cypher query with streaming enabled.  Returns a StreamingResult that yields NamedTuple rows via iteration.\n\nmax_execution_time::Union{Int,Nothing} (seconds, > 0) and tx_metadata::Union{AbstractDict,Nothing} are the same server-side execution controls as on query (require Neo4j 2026.04+; nothing omits them). cypher_version::Union{Int,Nothing} (5 or 25) pins the Cypher language version for this statement, exactly as on query; nothing leaves it to the DB.\n\nRows arrive incrementally: the HTTP request runs in a background task that drains the response body into a buffer, and each iterate reads the next line as it lands — so a consumer processes row 1 without waiting for the last byte.\n\nAs with query, access_mode=:read auto-retries a transient transport failure once (safe: server-enforced read-only) — but only before the first row is consumed — while the :write default never retries. timeout::Union{Int,Nothing}=nothing is a per-call read-timeout override in seconds (F-10): nothing uses the connection's readtimeout, an integer overrides it (0 = wait indefinitely). Note the timeout bounds the whole transfer, not idle time: a stalled server fires it as Neo4jHTTPError before the Header is read, instead of hanging.\n\nTo stop early, call close(sr) — it releases the connection. Abandoning a StreamingResult without consuming it or closing it keeps the connection checked out until the server finishes sending the whole response (the background task drains it).\n\nExample\n\nsr = stream(conn, \"MATCH (n:Person) RETURN n.name AS name\")\nfor row in sr\n    println(row.name)\n    row.name == \"Alice\" && (close(sr); break)   # stop early, release the connection\nend\n\n\n\n\n\nstream(tx::Transaction, statement; parameters) -> StreamingResult\n\nExecute a streaming query inside an existing explicit transaction.\n\n\n\n\n\n","title":"Neo4jQuery.stream"},{"category":"type","location":"api/#Neo4jQuery.StreamingResult","page":"API Reference","text":"StreamingResult\n\nAn in-progress streaming query result.  Implements Julia's iteration protocol so records can be consumed with a for loop:\n\nfor row in stream(conn, \"MATCH (n) RETURN n.name AS name\")\n    println(row.name)\nend\n\nAfter iteration completes (or is interrupted), call summary to retrieve bookmarks, counters, and notifications.\n\n\n\n\n\n","title":"Neo4jQuery.StreamingResult"},{"category":"method","location":"api/#Base.summary-Tuple{StreamingResult}","page":"API Reference","text":"summary(sr::StreamingResult) -> NamedTuple\n\nAccess bookmarks, counters, notifications, and query plans after the stream has been consumed.  Returns a NamedTuple with keys:\n\nbookmarks::Vector{String}\ncounters::Union{QueryCounters, Nothing}\nnotifications::Vector{Notification}\ntransaction::Union{JSON.Object, Nothing}\nquery_plan::Union{JSON.Object, Nothing}\nprofiled_query_plan::Union{JSON.Object, Nothing}\n\n\n\n\n\n","title":"Base.summary"},{"category":"method","location":"api/#Base.close-Tuple{StreamingResult}","page":"API Reference","text":"close(sr::StreamingResult)\n\nAbandon an unfinished stream: mark it done and close the underlying buffer. If the writer task is still draining the response it fails its next write into the closed buffer and unwinds — that error is intentionally swallowed (you asked to stop). Idempotent: a second close, or closing a fully-consumed stream, is a no-op.\n\nPrefer this to dropping a StreamingResult on the floor: an abandoned stream that is never closed keeps its HTTP connection checked out until the server finishes sending the whole response (the writer task keeps draining it in the background).\n\n\n\n\n\n","title":"Base.close"},{"category":"type","location":"api/#Neo4jQuery.ReadOnlyConnection","page":"API Reference","text":"ReadOnlyConnection(conn::Neo4jConnection)\n\nA wrapper permitting only reads. Every statement is classified by _classify_cypher before any request is built; writes throw ReadOnlyViolationError. access_mode is always forced to :read, so the Neo4j server independently enforces read-only — the client-side classifier is a fail-fast guard, not the sole guarantee.\n\nThe classifier is lexical and errs on the side of refusing: a write keyword used as a bare alias (e.g. RETURN n AS create) is conservatively rejected even though it performs no write. See _classify_cypher for the full set of known lexical inaccuracies.\n\n\n\n\n\n","title":"Neo4jQuery.ReadOnlyConnection"},{"category":"function","location":"api/#Neo4jQuery.read_query","page":"API Reference","text":"read_query(roc, statement; parameters, include_counters, bookmarks, impersonated_user, max_execution_time, tx_metadata, cypher_version, timeout) -> QueryResult\n\nRun a read-only query. Rejects any write statement before contacting the server; access_mode is always :read. Because reads are provably side-effect-free (server-enforced, not just client intent), a transient transport failure (e.g. a stale pooled connection) is automatically retried once — see query. A read timeout, however, is never retried (HTTP.jl treats it as unrecoverable); it surfaces as Neo4jHTTPError. timeout::Union{Int,Nothing}=nothing overrides the connection's readtimeout for this call (F-10). max_execution_time::Union{Int,Nothing} (seconds, > 0) and tx_metadata::Union{AbstractDict,Nothing} are the server-side execution controls from query (require Neo4j 2026.04+; nothing omits them). cypher_version::Union{Int,Nothing} (5 or 25) pins the statement's Cypher language version (F-29); the read-only classifier still runs on the RAW statement, so the inert CYPHER <version> prefix cannot smuggle a write past the guard.\n\n\n\n\n\n","title":"Neo4jQuery.read_query"},{"category":"function","location":"api/#Neo4jQuery.read_stream","page":"API Reference","text":"read_stream(roc, statement; parameters, kwargs...) -> StreamingResult\n\nStreaming variant of read_query; same read-only guarantee, including the auto-retry of a transient transport failure (see stream). The per-call timeout override (F-10) and the server-side max_execution_time, tx_metadata, and cypher_version (F-29, 5 or 25) controls all ride through kwargs... to stream.\n\naccess_mode is NOT among the forwardable kwargs: read_stream always runs accessMode=Read, and supplying access_mode throws ArgumentError. Because kwargs... is spliced rightmost when forwarding to stream, a caller's access_mode=:write would otherwise override the explicit :read and defeat the server-side read-only backstop — exactly where the lexical guard's documented false negatives (a write inside CALL some.write.proc()) rely on it.\n\n\n\n\n\n","title":"Neo4jQuery.read_stream"},{"category":"type","location":"api/#Neo4jQuery.ReadOnlyViolationError","page":"API Reference","text":"ReadOnlyViolationError(statement, matched)\n\nThrown by the read-only guard when a statement classified as a write is submitted through a ReadOnlyConnection. matched is the offending write clause. No HTTP request is issued.\n\n\n\n\n\n","title":"Neo4jQuery.ReadOnlyViolationError"},{"category":"function","location":"api/#Neo4jQuery._classify_cypher","page":"API Reference","text":"_classify_cypher(statement) -> Symbol\n\n:write if statement contains any write clause (after stripping comments and literals), else :read. Conservative / fail-closed.\n\nGuarded clauses: CREATE, MERGE, DELETE, SET, REMOVE, DROP, FOREACH, LOAD CSV, IN TRANSACTIONS, plus the admin/DDL commands ALTER, GRANT, DENY, REVOKE, RENAME, TERMINATE, START/STOP DATABASE, ENABLE SERVER, DEALLOCATE, REALLOCATE. The admin/DDL keywords are a fail-fast convenience only — a ReadOnlyConnection already forces access_mode=:read, so the server rejects them regardless. SET remains the noisiest false-positive source; the admin keywords add negligible extra FP risk (they are rare as bare aliases, and the multi-word forms above only match the full phrase).\n\nBecause it scans for keywords lexically (it is not a Cypher parser), it has two known, opposite inaccuracies:\n\nFalse negative — a write performed inside a called procedure (CALL some.write.proc()) is not detected by clause scanning. On a ReadOnlyConnection this is still rejected by the server-enforced access_mode=:read, so read-only safety is preserved.\nFalse positive — a write keyword used as a bare identifier or alias (e.g. RETURN n AS create, RETURN x AS set) is misclassified as a write and refused, even though the statement performs no write. This is a conservative over-refusal (a usability cost), not a safety issue.\n\n\n\n\n\n","title":"Neo4jQuery._classify_cypher"},{"category":"function","location":"api/#Neo4jQuery.validate_cypher","page":"API Reference","text":"validate_cypher(conn, statement; parameters=Dict{String,Any}())\n    -> @NamedTuple{valid::Bool, error::Union{Neo4jQueryError,Nothing}, plan::Union{JSON.Object{String,Any},Nothing}}\n\nServer-truth validation for (LLM-generated) Cypher without executing it: runs EXPLAIN <statement> under access_mode=:read and returns a NamedTuple:\n\nvalid=true, error=nothing, plan=<queryPlan> when the server planned the statement (plan is the parsed queryPlan object, or nothing if the server returned none).\nvalid=false, error=<Neo4jQueryError>, plan=nothing on any syntax/semantic error — error.message carries the server's line/column position. Positions refer to your statement: the injected EXPLAIN prefix is stripped from the message and line-1 columns/offsets shifted back before the error is returned, so a caret or repair loop aligned to the caller's text is correct as-is.\n\nOnly a genuine Neo4jQueryError means \"the Cypher is wrong\". Any other failure (transport/proxy, timeout, auth) is rethrown, never folded into valid=false.\n\nModifier handling (safety-critical)\n\nA leading PROFILE executes the statement, so it is stripped; a leading EXPLAIN is de-duplicated. One or more leading modifiers are removed together and replaced with exactly one EXPLAIN, so PROFILE can never reach the wire.\n\nReadOnlyConnection\n\nAlso callable on a ReadOnlyConnection, where it intentionally bypasses the lexical read-only guard. EXPLAIN never executes the statement — the server plans it under accessMode=Read and returns without running it (probe L6, empirically verified) — so validating a write is provably side-effect-free and must not be refused. The server, not the client classifier, is the guarantee.\n\nExample\n\nv = validate_cypher(conn, \"MATCH (n RETURN n\")   # missing ')'\nv.valid            # false\nv.error.message    # \"Invalid input ... (line 1, column ...)\"\n\nv = validate_cypher(conn, \"MATCH (n) RETURN n\")\nv.valid            # true\nv.plan             # JSON.Object — the EXPLAIN plan\n\n\n\n\n\nvalidate_cypher(roc::ReadOnlyConnection, statement; kwargs...)\n\nReadOnlyConnection overload. Forwards to the inner connection: EXPLAIN never executes, so the lexical write-guard is intentionally not applied here (see the main validate_cypher docstring for why this is safe).\n\n\n\n\n\n","title":"Neo4jQuery.validate_cypher"},{"category":"function","location":"api/#Neo4jQuery.graph_schema","page":"API Reference","text":"graph_schema(conn_or_roc) -> GraphSchema\n\nIntrospect the live database schema and return a GraphSchema. Issues four read queries — db.schema.nodeTypeProperties(), db.schema.relTypeProperties(), a sampled connectivity MATCH (LIMIT 1000), and SHOW INDEXES — each under access_mode=:read. A Neo4jConnection is wrapped in a ReadOnlyConnection so every query goes through read_query: the reads are provably side-effect-free, so this is safe on a read-only connection to a production database.\n\nPair with schema_prompt to ground a text-to-Cypher system prompt.\n\n\n\n\n\n","title":"Neo4jQuery.graph_schema"},{"category":"function","location":"api/#Neo4jQuery.schema_prompt","page":"API Reference","text":"schema_prompt(s::GraphSchema; max_labels::Int=50) -> String\nschema_prompt(conn_or_roc; max_labels::Int=50) -> String\n\nRender a compact, LLM-ready description of s (or of the schema read live from a connection). The rendering is deterministic — safe to embed in a cached system prompt.\n\nFormat\n\nOne line per node label: (:Label {prop: Type, optional?: Type}) — mandatory properties plain, optional ones suffixed ?; multiple observed types joined with |. A label with no properties renders as (:Label).\nOne line per observed connection: (:A)-[:T]->(:B).\nOne line per vector index: VECTOR index name on Label(prop) plus a , N-dim <similarity> suffix when options.indexConfig provides them. Other index kinds are omitted as prompt noise.\n\nLabels are capped at max_labels; the excess is reported with an explicit … and N more labels marker (never silently dropped). An empty schema renders a short sentinel string rather than raising.\n\nExample\n\nroc = ReadOnlyConnection(conn)\nsystem_prompt = \"You translate questions to Cypher.\\n\\n\" * schema_prompt(roc)\n\n\n\n\n\n","title":"Neo4jQuery.schema_prompt"},{"category":"type","location":"api/#Neo4jQuery.GraphSchema","page":"API Reference","text":"GraphSchema\n\nA server-truth snapshot of a graph's schema: node labels::Vector{LabelInfo}, relationship reltypes::Vector{RelTypeInfo}, and indexes::Vector{IndexInfo}. Produced by graph_schema; render it for an LLM with schema_prompt.\n\n\n\n\n\n","title":"Neo4jQuery.GraphSchema"},{"category":"type","location":"api/#Neo4jQuery.PropertyInfo","page":"API Reference","text":"PropertyInfo(name::String, types::Vector{String}, mandatory::Bool)\n\nOne property of a node label or relationship type, as reported by db.schema.nodeTypeProperties() / db.schema.relTypeProperties(). types is the set of Cypher type names observed for the property (a property may hold more than one type across instances); mandatory is true only when every instance carries it. Unexported; reachable as Neo4jQuery.PropertyInfo.\n\n\n\n\n\n","title":"Neo4jQuery.PropertyInfo"},{"category":"type","location":"api/#Neo4jQuery.LabelInfo","page":"API Reference","text":"LabelInfo(label::String, properties::Vector{PropertyInfo})\n\nA node label and its observed properties (see PropertyInfo). Unexported; reachable as Neo4jQuery.LabelInfo.\n\n\n\n\n\n","title":"Neo4jQuery.LabelInfo"},{"category":"type","location":"api/#Neo4jQuery.RelTypeInfo","page":"API Reference","text":"RelTypeInfo(reltype::String, properties::Vector{PropertyInfo},\n            connections::Vector{Tuple{String,String}})\n\nA relationship type: its properties and the (from_label, to_label) endpoint pairs observed in a sampled scan of the graph. Unexported; reachable as Neo4jQuery.RelTypeInfo.\n\n\n\n\n\n","title":"Neo4jQuery.RelTypeInfo"},{"category":"type","location":"api/#Neo4jQuery.IndexInfo","page":"API Reference","text":"IndexInfo(name::String, kind::String, entity::String,\n          properties::Vector{String}, options::Union{JSON.Object{String,Any},Nothing})\n\nAn index. kind is the index type (\"VECTOR\", \"RANGE\", \"FULLTEXT\", …), entity the label or relationship type it covers (the first of labelsOrTypes), properties the indexed property names, and options the server's raw options map (or nothing). For a VECTOR index options[\"indexConfig\"] carries \"vector.dimensions\" and \"vector.similarity_function\". Unexported; reachable as Neo4jQuery.IndexInfo.\n\n\n\n\n\n","title":"Neo4jQuery.IndexInfo"},{"category":"function","location":"api/#Neo4jQuery.vector_search","page":"API Reference","text":"vector_search(conn_or_roc, index::AbstractString, embedding::AbstractVector{<:Real};\n              k::Int=5, return_node::Bool=false) -> QueryResult\n\nk-nearest-neighbour search over a Neo4j vector index — the retrieval half of a GraphRAG loop. Runs the parameterized\n\nCALL db.index.vector.queryNodes($idx, $k, $vec) YIELD node, score\nRETURN elementId(node) AS id, labels(node) AS labels, properties(node) AS properties, score\n\nand returns a QueryResult of up to k rows, ordered by descending similarity score::Float64. With return_node=true the projection is node, score (the raw Node) instead of the id/labels/properties columns.\n\nThe index, k, and embedding are sent as parameters ($idx, $k, $vec), never string-interpolated — so a hostile or write-looking index name cannot inject Cypher, and cannot bypass the read-only guard (it is not part of the statement text). embedding is coerced to Vector{Float64}, so an integer vector is promoted rather than rejected by the procedure.\n\nReadOnlyConnection\n\nOn a ReadOnlyConnection the call funnels through read_query: the statement is a pure read (CALL … YIELD … RETURN), so it passes the lexical guard and runs under server-enforced accessMode=Read. Safe against a production read replica.\n\nValidation (fail loud)\n\nArgumentError — before any request is issued — if k < 1, index is empty, or embedding is empty, contains a non-finite value (NaN/Inf), or is all-zero. The last two mirror the server's own contract (\"Vector must only contain finite values, and have positive and finite l2-norm\"): a zero vector has no direction, so cosine KNN against it is undefined.\n\nDeprecation note\n\nNeo4j 2026.04 deprecates db.index.vector.queryNodes in favour of the Cypher-25 SEARCH clause. This helper will migrate to SEARCH transparently; the signature above is the stable contract and callers do not change.\n\nExample\n\nroc = ReadOnlyConnection(conn)\nhits = vector_search(roc, \"chunk_vec\", query_embedding; k=5)\nfor h in hits\n    println(h.score, \"  \", h.properties[\"text\"])\nend\n\n\n\n\n\nvector_search(roc::ReadOnlyConnection, index, embedding; kwargs...)\n\nReadOnlyConnection overload — routes through read_query, keeping the read-only guard. See the main vector_search docstring.\n\n\n\n\n\n","title":"Neo4jQuery.vector_search"},{"category":"function","location":"api/#Neo4jQuery.create_vector_index","page":"API Reference","text":"create_vector_index(conn, name, label, property;\n                    dimensions::Int, similarity::Symbol=:cosine) -> QueryResult\n\nCreate a Neo4j vector index named name on (:label).property, idempotently:\n\nCREATE VECTOR INDEX `name` IF NOT EXISTS\nFOR (n:`label`) ON (n.`property`)\nOPTIONS {indexConfig: {`vector.dimensions`: <dimensions>, `vector.similarity_function`: '<similarity>'}}\n\ndimensions is the embedding length; similarity is :cosine (default) or :euclidean. Returns the write QueryResult. This is the runtime helper; the @cypher-DSL macro form (Symbol literals) is a separate surface.\n\nValidation (fail loud)\n\nArgumentError if dimensions < 1, similarity ∉ (:cosine, :euclidean), or if name/label/property is empty or contains a backtick, quote, whitespace, or control character. Cypher DDL cannot be parameterized, so these identifiers are interpolated; the sanitizer makes the interpolation un-injectable (see also the DSL, whose inputs are Symbol literals and hence a narrower surface).\n\nExample\n\ncreate_vector_index(conn, \"chunk_vec\", \"Chunk\", \"embedding\";\n                    dimensions=384, similarity=:cosine)\n\n\n\n\n\n","title":"Neo4jQuery.create_vector_index"},{"category":"type","location":"api/#Neo4jQuery.QueryResult","page":"API Reference","text":"QueryResult\n\nThe result of a Cypher query.  Supports iteration and indexing—each row is a NamedTuple whose keys match the query's field names.\n\nIteration\n\nfor row in result\n    println(row.name, \" is \", row.age, \" years old\")\nend\n\nIndexing\n\nresult[1]          # first row as NamedTuple\nresult[end]        # last row\nlength(result)     # number of rows\n\n\n\n\n\n","title":"Neo4jQuery.QueryResult"},{"category":"type","location":"api/#Neo4jQuery.QueryCounters","page":"API Reference","text":"QueryCounters\n\nStatistics about database changes performed by a query.\n\n\n\n\n\n","title":"Neo4jQuery.QueryCounters"},{"category":"type","location":"api/#Neo4jQuery.Notification","page":"API Reference","text":"Notification\n\nA server notification (performance warning, deprecation hint, etc.) attached to a query response.\n\n\n\n\n\n","title":"Neo4jQuery.Notification"},{"category":"type","location":"api/#Neo4jQuery.Node","page":"API Reference","text":"Node\n\nA Neo4j graph node with an element ID, labels, and a property map.\n\nProperty access is supported via both indexing and dot syntax:\n\nnode[\"name\"]   # indexing\nnode.name      # dot syntax\n\nTwo Nodes are equal (and hash equal) when they share the same element_id, matching the official Neo4j drivers — a Node denotes a graph element, not a snapshot of its properties. NOTE: element ids are only guaranteed stable within a transaction, so equal ids read from different snapshots may carry different property values yet still compare ==. This is deliberate and lets Nodes dedup in a Set/Dict (result handling, F-17).\n\n\n\n\n\n","title":"Neo4jQuery.Node"},{"category":"type","location":"api/#Neo4jQuery.Relationship","page":"API Reference","text":"Relationship\n\nA Neo4j graph relationship with an element ID, start/end node element IDs, a type string, and a property map.\n\nrel[\"since\"]   # indexing\nrel.since      # dot syntax\n\nEquality and hashing follow the same element_id identity rule as Node: two Relationships with the same element_id are equal regardless of property snapshot.\n\n\n\n\n\n","title":"Neo4jQuery.Relationship"},{"category":"type","location":"api/#Neo4jQuery.Path","page":"API Reference","text":"Path\n\nA Neo4j graph path—an alternating sequence of Node and Relationship objects.\n\nTwo Paths are equal (and hash equal) when their elements are equal in the same order, element-wise by the Node/Relationship element-id identity.\n\n\n\n\n\n","title":"Neo4jQuery.Path"},{"category":"type","location":"api/#Neo4jQuery.CypherPoint","page":"API Reference","text":"CypherPoint\n\nA Cypher spatial point value.  Stored as an SRID integer and a coordinate vector. Serialised on the wire as a WKT string, e.g. \"SRID=7203;POINT (1.2 3.4)\".\n\nTwo points are equal when their srid and coordinates match; ==, isequal, and hash each compose the corresponding field relation, so a point mirrors its wrapped Float64s exactly — ±0.0 points are == but not isequal (and hash apart), NaN points are isequal but not ==, and a Set of points behaves like a Set of the underlying floats.\n\n\n\n\n\n","title":"Neo4jQuery.CypherPoint"},{"category":"type","location":"api/#Neo4jQuery.CypherDuration","page":"API Reference","text":"CypherDuration\n\nA Cypher duration value.  Stored as the original ISO-8601 string (e.g. \"P14DT16H12M\").\n\nEquality (and hashing) compares the stored ISO-8601 string.\n\n\n\n\n\n","title":"Neo4jQuery.CypherDuration"},{"category":"type","location":"api/#Neo4jQuery.CypherVector","page":"API Reference","text":"CypherVector\n\nA Neo4j vector value (Enterprise Edition). coordinates is a Vector{String} and is kept as strings on purpose: the server's coordinatesType spans FLOAT64/FLOAT32 and INT64/INT32/INT16/INT8, and storing the exact wire tokens is lossless across all of them — parsing to one fixed numeric type on read would perturb float digits or overflow the narrow ints. Convert explicitly when you need numbers for math:\n\nlength(v)             # dimension (coordinate count)\nVector{Float32}(v)    # → Vector{Float32}\nVector{Float64}(v)    # → Vector{Float64}\nVector{Int8}(v)       # → Vector{Int8}   (any T<:Real)\n\nConversion parses each coordinate with parse(T, …); a non-numeric coordinate — or an integer target over a fractional value (Vector{Int} of a FLOAT32 vector) — throws ArgumentError (an out-of-range integer throws OverflowError). This is deliberate fail-loud behavior: no silent NaN, zero, or truncation.\n\nEquality (and hashing) compares coordinates_type and coordinates.\n\n\n\n\n\n","title":"Neo4jQuery.CypherVector"},{"category":"type","location":"api/#Neo4jQuery.CypherTime","page":"API Reference","text":"CypherTime\n\nA Cypher TIME value: a time-of-day carrying a fixed UTC offset (Neo4j's zoned TIME, as opposed to the offset-free LocalTime which materializes as a bare Dates.Time). Fields:\n\ntime::Dates.Time — nanosecond-resolution time-of-day.\ntimezone::TimeZones.FixedTimeZone — the fixed UTC offset.\n\nRound-trips losslessly through Typed JSON Time: to_typed_json reproduces the exact _value string the server sent (e.g. \"12:50:35.556+01:00\"), including sub-second nanosecond fractions. A zero offset serializes to the canonical Z suffix (e.g. \"09:00:00Z\") — the server's UTC emission for zoned TIME, which gives read/write symmetry with materialization. Two CypherTimes compare == (and hash) on their time and UTC offset, so the Z, UTC, and +00:00 spellings of the zero offset are equal.\n\n\n\n\n\n","title":"Neo4jQuery.CypherTime"},{"category":"function","location":"api/#Neo4jQuery.to_typed_json","page":"API Reference","text":"to_typed_json(val) -> Dict{String,Any}\n\nConvert a Julia value into its Neo4j Typed JSON envelope representation for use as a query parameter. Every AbstractDict is encoded as a Cypher Map — there is no pre-encoded-envelope passthrough, so an envelope-shaped dict (with $type/_value keys) is wrapped as a Map like any other. A value with no to_typed_json method raises ErrorException rather than being silently passed through.\n\n\n\n\n\nto_typed_json(v::TimeZones.ZonedDateTime)\n\nSerialize a ZonedDateTime to its Neo4j Typed JSON envelope. A named IANA zone (VariableTimeZone) lowers to a ZonedDateTime envelope whose _value carries the ±HH:MM[Area/City] suffix the server expects (e.g. 2024-01-15T10:30:00+01:00[Europe/Paris]), so the zone name survives the round-trip (F-06). A fixed offset (FixedTimeZone) lowers to an OffsetDateTime envelope — byte-stable with 0.3.x, where every ZonedDateTime serialized as a bare offset. string(::ZonedDateTime) emits the offset form and renders a millisecond fraction only when non-zero; Julia's ms resolution means no sub-millisecond digits are ever produced.\n\n\n\n\n\nto_typed_json(v::CypherTime)\n\nSerialize a zoned TIME back to its Typed JSON Time envelope, emitting the same _value the server sent — \"<time><±HH:MM>\", or \"<time>Z\" for a zero offset (the pinned UTC form, e.g. \"09:00:00Z\") — for a byte-identical F-12 round-trip. string(::Dates.Time) preserves nanoseconds, so sub-second fractions survive.\n\n\n\n\n\n","title":"Neo4jQuery.to_typed_json"},{"category":"type","location":"api/#Neo4jQuery.Neo4jError","page":"API Reference","text":"Neo4jError <: Exception\n\nAbstract base type for all Neo4j-related errors.\n\n\n\n\n\n","title":"Neo4jQuery.Neo4jError"},{"category":"type","location":"api/#Neo4jQuery.AuthenticationError","page":"API Reference","text":"AuthenticationError <: Neo4jError\n\nRaised when the server returns HTTP 401 (missing, incorrect, or invalid credentials).\n\n\n\n\n\n","title":"Neo4jQuery.AuthenticationError"},{"category":"type","location":"api/#Neo4jQuery.Neo4jQueryError","page":"API Reference","text":"Neo4jQueryError <: Neo4jError\n\nRaised when the server response contains an errors array (query syntax errors, constraint violations, etc.).\n\n\n\n\n\n","title":"Neo4jQuery.Neo4jQueryError"},{"category":"type","location":"api/#Neo4jQuery.TransactionExpiredError","page":"API Reference","text":"TransactionExpiredError <: Neo4jError\n\nRaised when a request that references an existing explicit transaction (query(tx, …), commit!, stream(tx, …)) fails because the server no longer has that transaction — it expired, timed out, or was rolled back server-side.\n\nClassified by error code (Neo.ClientError.Transaction.TransactionNotFound, …TransactionTimedOut, …TransactionTimedOutClientConfiguration) plus the documented expired-tx shape Neo.ClientError.Request.Invalid with a \"was not found\" message. Errors outside a transaction context — e.g. a plain query hitting a lock-acquisition timeout — are never classified as this; they raise Neo4jQueryError with the server's code instead.\n\n\n\n\n\n","title":"Neo4jQuery.TransactionExpiredError"},{"category":"type","location":"api/#Neo4jQuery.Neo4jHTTPError","page":"API Reference","text":"Neo4jHTTPError <: Neo4jError\n\nRaised when the server (or an intermediary such as a proxy/load balancer) returns an HTTP failure that carries no Neo4j errors array — e.g. a 5xx with an HTML body, or an unexpected status with an unparseable payload.\n\n\n\n\n\n","title":"Neo4jQuery.Neo4jHTTPError"},{"category":"function","location":"api/#Neo4jQuery.is_transient","page":"API Reference","text":"is_transient(e::Neo4jError) -> Bool\n\ntrue when the error is safe to retry per Neo4j's status-code taxonomy (Neo.TransientError.*) or an HTTP 429/503. Cypher errors ride HTTP 202, so status-based classification alone is impossible — use this predicate in agent retry loops (retry the work, idempotently; the transport-level read retry inside Neo4jQuery is separate and automatic).\n\nEverything else is false, including deterministic failures that a blind retry cannot fix: syntax/constraint errors, AuthenticationError (same credentials loop forever), and TransactionExpiredError — an expired tx is not blind-retryable, the caller must re-begin_transaction and replay the work against a fresh handle rather than re-send against the dead one.\n\n\n\n\n\n","title":"Neo4jQuery.is_transient"},{"category":"type","location":"api/#Neo4jQuery.PropertyDef","page":"API Reference","text":"PropertyDef\n\nA single property definition within a node or relationship schema.\n\nFields\n\nname::Symbol — property name\ntype::Symbol — Julia type name (e.g. :String, :Int)\nrequired::Bool — whether the property must be supplied on creation\ndefault — default value for optional properties, or nothing for required\n\n\n\n\n\n","title":"Neo4jQuery.PropertyDef"},{"category":"type","location":"api/#Neo4jQuery.NodeSchema","page":"API Reference","text":"NodeSchema\n\nSchema descriptor for a Neo4j node label, created by the @node macro.\n\nFields\n\nlabel::Symbol — the Neo4j label (e.g. :Person)\nproperties::Vector{PropertyDef} — typed property definitions\n\n\n\n\n\n","title":"Neo4jQuery.NodeSchema"},{"category":"type","location":"api/#Neo4jQuery.RelSchema","page":"API Reference","text":"RelSchema\n\nSchema descriptor for a Neo4j relationship type, created by the @rel macro.\n\nFields\n\nreltype::Symbol — the relationship type (e.g. :KNOWS)\nproperties::Vector{PropertyDef} — typed property definitions\n\n\n\n\n\n","title":"Neo4jQuery.RelSchema"},{"category":"macro","location":"api/#Neo4jQuery.@node","page":"API Reference","text":"@node Name begin\n    field::Type\n    optional_field::Type = default_value\nend\n\nDeclare a node schema with label Name and typed properties.\n\nCreates a constant Name::NodeSchema and registers it for runtime validation in @create and @merge operations.\n\nExample\n\n@node Person begin\n    name::String\n    age::Int\n    email::String = \"\"\nend\n\n# Person is now a NodeSchema constant\nPerson.label       # :Person\nPerson.properties  # [PropertyDef(:name, :String, true, nothing), ...]\n\n\n\n\n\n@node Name\n\nDeclare a node schema with no properties (label-only).\n\n\n\n\n\n","title":"Neo4jQuery.@node"},{"category":"macro","location":"api/#Neo4jQuery.@rel","page":"API Reference","text":"@rel TYPE begin\n    field::Type\n    optional_field::Type = default_value\nend\n\nDeclare a relationship schema with type TYPE and typed properties.\n\nCreates a constant TYPE::RelSchema and registers it for runtime validation.\n\nExample\n\n@rel KNOWS begin\n    since::Int\n    weight::Float64 = 1.0\nend\n\n\n\n\n\n@rel TYPE\n\nDeclare a relationship schema with no properties (type-only).\n\n\n\n\n\n","title":"Neo4jQuery.@rel"},{"category":"function","location":"api/#Neo4jQuery.get_node_schema","page":"API Reference","text":"Look up a registered node schema by label, or return nothing.\n\n\n\n\n\n","title":"Neo4jQuery.get_node_schema"},{"category":"function","location":"api/#Neo4jQuery.get_rel_schema","page":"API Reference","text":"Look up a registered relationship schema by type, or return nothing.\n\n\n\n\n\n","title":"Neo4jQuery.get_rel_schema"},{"category":"function","location":"api/#Neo4jQuery.validate_node_properties","page":"API Reference","text":"validate_node_properties(schema::NodeSchema, props::Dict{String,<:Any})\n\nValidate that props satisfies the schema's required properties. Warns on unknown properties not declared in the schema. Throws on missing required properties.\n\n\n\n\n\n","title":"Neo4jQuery.validate_node_properties"},{"category":"function","location":"api/#Neo4jQuery.validate_rel_properties","page":"API Reference","text":"validate_rel_properties(schema::RelSchema, props::Dict{String,<:Any})\n\nValidate that props satisfies the schema's required properties.\n\n\n\n\n\n","title":"Neo4jQuery.validate_rel_properties"},{"category":"macro","location":"api/#Neo4jQuery.@cypher","page":"API Reference","text":"@cypher conn begin ... end\n@cypher conn [comprehension]\n\nUnified graph query DSL that compiles Julia expressions to parameterized Cypher.\n\nCombines Julia-native ergonomics with full Cypher coverage.\n\nPattern Syntax\n\nThe >> chain is the primary pattern language. Arrow syntax also works.\n\nJulia Cypher\np::Person (p:Person)\n::Person (:Person)\np::Person >> r::KNOWS >> q::Person (p:Person)-[r:KNOWS]->(q:Person)\np::Person >> KNOWS >> q::Person (p:Person)-[:KNOWS]->(q:Person)\np::Person << r::KNOWS << q::Person (p:Person)<-[r:KNOWS]-(q:Person)\na::A >> R1 >> b::B >> R2 >> c::C (a:A)-[:R1]->(b:B)-[:R2]->(c:C)\n(p:Person)-[r:KNOWS]->(q:Person) (p:Person)-[r:KNOWS]->(q:Person)\na::A >> KNOWS{2,5} >> b::B (a:A)-[:KNOWS]->{2,5}(b:B)\na::A >> r::KNOWS{1,nothing} >> b::B (a:A)-[r:KNOWS]->+(b:B)\na::A >> KNOWS{0,nothing} >> b::B (a:A)-[:KNOWS]->*(b:B)\na::A >> KNOWS{3} >> b::B (a:A)-[:KNOWS]->{3}(b:B)\n\nPath Variables\n\nAssign a pattern to a path variable with =:\n\nJulia Cypher\np = a::A >> R >> b::B MATCH p = (a:A)-[:R]->(b:B)\n\nShortest Path Selectors\n\nJulia Cypher\nshortest(k, pattern) MATCH SHORTEST k pattern\nall_shortest(pattern) MATCH ALL SHORTEST pattern\nshortest_groups(k, pattern) MATCH SHORTEST k GROUPS pattern\nany_paths(pattern) / any_paths(k, pattern) MATCH ANY [k] pattern\np = shortest(1, pattern) MATCH p = SHORTEST 1 pattern\n\nClause Functions\n\nClause Cypher\nwhere(cond1, cond2) WHERE cond1 AND cond2\nret(expr => :alias, ...) RETURN expr AS alias, ...\nret(distinct, expr) RETURN DISTINCT expr\nreturning(expr) RETURN expr (alias for ret)\norder(expr, :desc) ORDER BY expr DESC\ntake(n) / skip(n) LIMIT n / SKIP n\ncreate(pattern) / merge(pattern) CREATE / MERGE\noptional(pattern) OPTIONAL MATCH pattern\nmatch(p1, p2) MATCH p1, p2 (explicit multi)\nwith(expr => :alias, ...) WITH expr AS alias, ...\nunwind($list => :var) UNWIND $list AS var\ndelete(vars) / detach_delete(vars) DELETE / DETACH DELETE\non_create(p.prop = val) ON CREATE SET p.prop = val\non_match(p.prop = val) ON MATCH SET p.prop = val\np.prop = $val (assignment) SET p.prop = $val (auto-SET)\nremove(items) REMOVE items\nunion() / union_all() UNION / UNION ALL\ncall(begin ... end) CALL { ... } subquery\nload_csv(url => :row) LOAD CSV FROM url AS row\nload_csv_headers(url => :row) LOAD CSV WITH HEADERS ...\nforeach(expr => :var, begin...end) FOREACH (var IN expr | ...)\ncreate_index(:Label, :prop) CREATE INDEX ...\ndrop_index(:name) DROP INDEX name IF EXISTS\ncreate_constraint(:L, :p, :type) CREATE CONSTRAINT ...\ndrop_constraint(:name) DROP CONSTRAINT name IF EXISTS\n\nExamples\n\n# Simple traversal\nresult = @cypher conn begin\n    p::Person >> r::KNOWS >> q::Person\n    where(p.age > $min_age, q.name == $target)\n    ret(p.name => :name, r.since, q.name => :friend)\n    order(p.age, :desc)\n    take(10)\nend\n\n# Mutations with auto-SET\n@cypher conn begin\n    p::Person\n    where(p.name == $name)\n    p.age = $new_age\n    p.active = true\n    ret(p)\nend\n\n# Create relationships with >> in create()\n@cypher conn begin\n    match(a::Person, b::Person)\n    where(a.name == $n1, b.name == $n2)\n    create(a >> r::KNOWS >> b)\n    r.since = $year\n    ret(r)\nend\n\n# UNION\n@cypher conn begin\n    p::Person\n    where(p.age > 30)\n    ret(p.name => :name)\n    union()\n    p::Person\n    where(startswith(p.name, \"A\"))\n    ret(p.name => :name)\nend\n\n# CALL subquery\n@cypher conn begin\n    p::Person\n    call(begin\n        with(p)\n        p >> r::KNOWS >> friend::Person\n        ret(count(friend) => :friend_count)\n    end)\n    ret(p.name => :name, friend_count)\nend\n\n# FOREACH\n@cypher conn begin\n    p::Person\n    where(p.active == true)\n    foreach(collect(p) => :n, begin\n        n.verified = true\n    end)\nend\n\n# Comprehension form\nresult = @cypher conn [p.name for p in Person if p.age > 25]\n\n# Quantified relationship (variable-length)\nresult = @cypher conn begin\n    a::Person >> KNOWS{1,5} >> b::Person\n    where(a.name == $start)\n    ret(a, b)\nend\n\n# Path variable assignment\nresult = @cypher conn begin\n    p = a::Person >> KNOWS >> b::Person\n    ret(p, length(p))\nend\n\n# Shortest path\nresult = @cypher conn begin\n    shortest(1, a::Station >> LINK{1,nothing} >> b::Station)\n    where(a.name == $src, b.name == $dst)\n    ret(a, b)\nend\n\n# All shortest with path variable\nresult = @cypher conn begin\n    p = all_shortest(a::Station >> LINK{1,nothing} >> b::Station)\n    where(a.name == $src, b.name == $dst)\n    ret(p, length(p) => :hops)\nend\n\n\n\n\n\n","title":"Neo4jQuery.@cypher"},{"category":"macro","location":"api/#Neo4jQuery.@create","page":"API Reference","text":"@create conn Label(prop1=val1, prop2=val2, ...)\n\nCreate a single node with the given label and properties. Returns the created Node.\n\nIf a schema is registered for Label (via @node), properties are validated at runtime — missing required properties raise an error.\n\nExample\n\n@node Person begin\n    name::String\n    age::Int\nend\n\nalice = @create conn Person(name=\"Alice\", age=30)\n# alice is a Node with labels=[\"Person\"], properties={name: \"Alice\", age: 30}\n\n# Variables work too:\nname = \"Bob\"\nage = 25\nbob = @create conn Person(name=name, age=age)\n\n\n\n\n\n","title":"Neo4jQuery.@create"},{"category":"macro","location":"api/#Neo4jQuery.@merge","page":"API Reference","text":"@merge conn Label(match_prop=val) on_create(prop=val, ...) on_match(prop=val, ...)\n\nMERGE a node by matching properties, with optional ON CREATE SET and ON MATCH SET.\n\nReturns the merged Node.\n\nExample\n\n# Merge by name, set age only on creation, update last_seen on every match\nnode = @merge conn Person(name=\"Alice\") on_create(age=30) on_match(last_seen=\"2025-02-15\")\n\n# Simple merge (no on_create/on_match):\nnode = @merge conn Person(name=\"Alice\", age=30)\n\n\n\n\n\n","title":"Neo4jQuery.@merge"},{"category":"macro","location":"api/#Neo4jQuery.@relate","page":"API Reference","text":"@relate conn start_node => REL_TYPE(props...) => end_node\n\nCreate a relationship between two existing Node objects. Returns the created Relationship.\n\nUses elementId() to match the start and end nodes.\n\nExample\n\nalice = @create conn Person(name=\"Alice\", age=30)\nbob   = @create conn Person(name=\"Bob\", age=25)\n\nrel = @relate conn alice => KNOWS(since=2024) => bob\n# rel is a Relationship with type=\"KNOWS\", properties={since: 2024}\n\n\n\n\n\n","title":"Neo4jQuery.@relate"},{"category":"section","location":"guide/biomedical_case_study/#biomedical-case-study","page":"Biomedical Case Study","text":"This case study demonstrates building a realistic biomedical knowledge graph using the Neo4jQuery DSL. It covers schema declarations, node/relationship creation, and complex analytical queries — with side-by-side comparisons of raw Cypher vs the @cypher DSL.\n\nNeo4jQuery provides the @cypher macro — a unified query interface that uses:\n\nFunction-call clause syntax: where(), ret(), order(), take(), with(), create(), merge(), optional(), unwind(), …\n>> chain patterns (recommended): p::Person >> r::KNOWS >> q::Person\nArrow patterns (also supported): (p:Person)-[r:KNOWS]->(q:Person)\nAuto-SET from property assignments: p.age = $val\nComprehension form: @cypher conn [p.name for p in Person if p.age > 25]\n\nThe macro compiles to Cypher at macro expansion time — zero runtime overhead for query construction. Only parameter values are captured at runtime.\n\nnote: Unified pattern syntax in `@cypher`\nThe @cypher macro uses >> chains as its single, canonical pattern language — for queries, mutations, merges, everything. The same >> syntax works in create(), merge(), optional(), match(), and bare implicit MATCH. Arrow syntax (-[]->), while backward-compatible, is not needed.\n\nThe full runnable scripts are:\n\ntest/biomedical_graph_test.jl — @create / @relate standalone macro examples\ntest/biomedical_graph_dsl_test.jl — @cypher DSL examples\n\n","title":"Biomedical Knowledge Graph — Case Study"},{"category":"section","location":"guide/biomedical_case_study/#Domain-model","page":"Biomedical Case Study","text":"The graph models a clinical/biomedical knowledge base with 12 node types and 17 relationship types:\n\nNode Labels Relationship Types\nDisease ASSOCIATED_WITH\nGene TARGETS\nProtein INHIBITS\nDrug ENCODES\nClinicalTrial PARTICIPATES_IN\nPatient DIAGNOSED_WITH\nHospital ENROLLED_IN\nPhysician TREATS\nPathway PRESCRIBED_BY\nSymptom LOCATED_AT\nBiomarker PRESENTS_WITH\nPublication INDICATES, PUBLISHED_IN\n EXPRESSES, HAS_SIDE_EFFECT\n INTERACTS_WITH\n\n","title":"Domain model"},{"category":"section","location":"guide/biomedical_case_study/#1.-Schema-declarations","page":"Biomedical Case Study","text":"","title":"1. Schema declarations"},{"category":"section","location":"guide/biomedical_case_study/#Raw-Cypher","page":"Biomedical Case Study","text":"Cypher has no built-in schema declarations. You rely on conventions and documentation:\n\n// No schema — just hope everyone remembers the properties\nCREATE (d:Disease {name: \"Breast Cancer\", icd10_code: \"C50\", category: \"Oncology\", chronic: true})\n// Oops, someone writes: CREATE (d:Disease {naam: \"Lung Cancer\"})  — no error","title":"Raw Cypher"},{"category":"section","location":"guide/biomedical_case_study/#@cypher-DSL-—-schema-declarations","page":"Biomedical Case Study","text":"@node Disease begin\n    name::String\n    icd10_code::String\n    category::String\n    chronic::Bool\nend\n\n@node Gene begin\n    symbol::String\n    full_name::String\n    chromosome::String\n    locus::String = \"\"       # optional, with default\nend\n\n@node Protein begin\n    uniprot_id::String\n    name::String\n    molecular_weight::Float64\n    function_desc::String = \"\"\nend\n\n@node Drug begin\n    name::String\n    trade_name::String\n    mechanism::String\n    approved_year::Int\n    phase::String = \"approved\"\nend\n\n@node Patient begin\n    patient_id::String\n    age::Int\n    sex::String\n    ethnicity::String = \"\"\nend\n\nAdditional node schemas:\n\n@node ClinicalTrial begin\n    trial_id::String\n    title::String\n    phase::String\n    status::String\n    start_year::Int\n    enrollment::Int\nend\n\n@node Hospital begin\n    name::String\n    city::String\n    country::String\n    beds::Int\nend\n\n@node Physician begin\n    name::String\n    specialty::String\n    license_no::String\nend\n\n@node Pathway begin\n    name::String\n    kegg_id::String\n    category::String\nend\n\n@node Symptom begin\n    name::String\n    severity_scale::String\n    body_system::String\nend\n\n@node Biomarker begin\n    name::String\n    biomarker_type::String\n    unit::String\nend\n\n@node Publication begin\n    doi::String\n    title::String\n    journal::String\n    year::Int\nend\nnothing # hide\n\nRelationship schemas:\n\n@rel ASSOCIATED_WITH begin\n    score::Float64\n    source::String\nend\n\n@rel TARGETS begin\n    action::String\n    binding_affinity::Float64 = 0.0\nend\n\n@rel TREATS begin\n    efficacy::Float64\n    evidence_level::String\nend\n\n@rel DIAGNOSED_WITH begin\n    diagnosis_date::String\n    stage::String = \"\"\nend\n\n@rel ENCODES begin\n    transcript_id::String\nend\n\n@rel PARTICIPATES_IN begin\n    role::String\nend\n\n@rel ENROLLED_IN begin\n    enrollment_date::String\n    arm::String\nend\n\n@rel LOCATED_AT begin\n    department::String\nend\n\n@rel INDICATES begin\n    threshold::Float64\n    direction::String\nend\n\n@rel PUBLISHED_IN begin\n    contribution::String\nend\n\n@rel HAS_SIDE_EFFECT begin\n    frequency::String\n    severity::String\nend\n\n@rel EXPRESSES begin\n    tissue::String\n    expression_level::Float64\nend\n\n@rel INHIBITS begin\n    ic50::Float64\n    mechanism::String = \"\"\nend\n\n@rel PRESCRIBED_BY begin\n    prescription_date::String\nend\n\n@rel PRESENTS_WITH begin\n    onset::String\n    frequency::String = \"\"\nend\n\n@rel INTERACTS_WITH begin\n    interaction_type::String\n    confidence::Float64\nend\nnothing # hide\n\nSchemas enable runtime validation — misspelled properties, missing required fields, and type mismatches are caught immediately. The @cypher macro uses the same schema registry.\n\n","title":"@cypher DSL — schema declarations"},{"category":"section","location":"guide/biomedical_case_study/#2.-Creating-nodes","page":"Biomedical Case Study","text":"","title":"2. Creating nodes"},{"category":"section","location":"guide/biomedical_case_study/#Raw-Cypher-2","page":"Biomedical Case Study","text":"CREATE (d:Disease {name: 'Breast Cancer', icd10_code: 'C50', category: 'Oncology', chronic: true})\nRETURN d\n\nCREATE (g:Gene {symbol: 'BRCA1', full_name: 'BRCA1 DNA Repair Associated',\n                chromosome: '17q21.31', locus: '17q21'})\nRETURN g\n\nCREATE (drug:Drug {name: 'Trastuzumab', trade_name: 'Herceptin',\n                   mechanism: 'HER2 monoclonal antibody', approved_year: 1998})\nRETURN drug","title":"Raw Cypher"},{"category":"section","location":"guide/biomedical_case_study/#Julia-DSL-—-@create-macro","page":"Biomedical Case Study","text":"breast_cancer = @create conn Disease(\n    name=\"Breast Cancer\",\n    icd10_code=\"C50\",\n    category=\"Oncology\",\n    chronic=true\n)\n\nbrca1 = @create conn Gene(\n    symbol=\"BRCA1\", full_name=\"BRCA1 DNA Repair Associated\",\n    chromosome=\"17q21.31\", locus=\"17q21\"\n)\n\ntrastuzumab = @create conn Drug(\n    name=\"Trastuzumab\", trade_name=\"Herceptin\",\n    mechanism=\"HER2 monoclonal antibody\", approved_year=1998\n)\n\nEach call returns a Node object you can use directly to create relationships.","title":"Julia DSL — @create macro"},{"category":"section","location":"guide/biomedical_case_study/#Julia-DSL-—-@cypher-macro","page":"Biomedical Case Study","text":"breast_cancer = let\n    r = @cypher conn begin\n        create(d::Disease)\n        d.name = \"Breast Cancer\"\n        d.icd10_code = \"C50\"\n        d.category = \"Oncology\"\n        d.chronic = true\n        ret(d)\n    end\n    r[1].d\nend\n\nbrca1 = let\n    r = @cypher conn begin\n        create(g::Gene)\n        g.symbol = \"BRCA1\"\n        g.full_name = \"BRCA1 DNA Repair Associated\"\n        g.chromosome = \"17q21.31\"\n        g.locus = \"17q21\"\n        ret(g)\n    end\n    r[1].g\nend\n\ntrastuzumab = let\n    r = @cypher conn begin\n        create(d::Drug)\n        d.name = \"Trastuzumab\"\n        d.trade_name = \"Herceptin\"\n        d.mechanism = \"HER2 monoclonal antibody\"\n        d.approved_year = 1998\n        ret(d)\n    end\n    r[1].d\nend\n\nThe @cypher approach uses create() + property assignments (d.name = \"...\") which compile to CREATE (d:Disease) SET d.name = '...'. Properties are set via auto-SET detection.\n\nFor creating nodes, create(d::Disease) is a single-node pattern. For creating relationships, create() takes a >> chain (see next section).\n\n","title":"Julia DSL — @cypher macro"},{"category":"section","location":"guide/biomedical_case_study/#3.-Creating-relationships","page":"Biomedical Case Study","text":"","title":"3. Creating relationships"},{"category":"section","location":"guide/biomedical_case_study/#Raw-Cypher-3","page":"Biomedical Case Study","text":"// Need to match nodes by some property first\nMATCH (g:Gene {symbol: 'BRCA1'}), (d:Disease {name: 'Breast Cancer'})\nCREATE (g)-[r:ASSOCIATED_WITH {score: 0.95, source: 'ClinVar'}]->(d)\nRETURN r\n\nMATCH (drug:Drug {name: 'Trastuzumab'}), (d:Disease {name: 'Breast Cancer'})\nCREATE (drug)-[r:TREATS {efficacy: 0.85, evidence_level: '1A'}]->(d)\nRETURN r","title":"Raw Cypher"},{"category":"section","location":"guide/biomedical_case_study/#Julia-DSL-—-@relate-macro","page":"Biomedical Case Study","text":"@relate conn brca1 => ASSOCIATED_WITH(score=0.95, source=\"ClinVar\") => breast_cancer\n\n@relate conn trastuzumab => TREATS(efficacy=0.85, evidence_level=\"1A\") => breast_cancer\n\nNo need to re-match nodes by property. The DSL uses elementId() from the returned Node objects — zero ambiguity, zero duplication.","title":"Julia DSL — @relate macro"},{"category":"section","location":"guide/biomedical_case_study/#Julia-DSL-—-@cypher-macro-2","page":"Biomedical Case Study","text":"# Match nodes by property, then create the relationship with >> chain\n@cypher conn begin\n    match(g::Gene, d::Disease)\n    where(g.symbol == \"BRCA1\", d.name == \"Breast Cancer\")\n    create(g >> r::ASSOCIATED_WITH >> d)\n    r.score = 0.95\n    r.source = \"ClinVar\"\n    ret(r)\nend\n\n@cypher conn begin\n    match(drug::Drug, d::Disease)\n    where(drug.name == \"Trastuzumab\", d.name == \"Breast Cancer\")\n    create(drug >> r::TREATS >> d)\n    r.efficacy = 0.85\n    r.evidence_level = \"1A\"\n    ret(r)\nend\n\nThe same >> pattern syntax used in queries also works in create(), merge(), and optional(). Relationship properties are set via auto-SET.\n\n","title":"Julia DSL — @cypher macro"},{"category":"section","location":"guide/biomedical_case_study/#4.-Complex-queries-—-side-by-side","page":"Biomedical Case Study","text":"","title":"4. Complex queries — side by side"},{"category":"section","location":"guide/biomedical_case_study/#4.1-Multi-hop:-Gene-Protein-Pathway-for-a-disease","page":"Biomedical Case Study","text":"Raw Cypher:\n\nMATCH (g:Gene)-[:ASSOCIATED_WITH]->(d:Disease {name: 'Breast Cancer'})\nMATCH (g)-[:ENCODES]->(p:Protein)-[:PARTICIPATES_IN]->(pw:Pathway)\nRETURN g.symbol AS gene, p.name AS protein, pw.name AS pathway\nORDER BY g.symbol\n\n@cypher DSL:\n\ndisease_name = \"Breast Cancer\"\nresult = @cypher conn begin\n    g::Gene >> ::ASSOCIATED_WITH >> d::Disease\n    g >> ::ENCODES >> p::Protein >> ::PARTICIPATES_IN >> pw::Pathway\n    where(d.name == $disease_name)\n    ret(g.symbol => :gene, p.name => :protein, pw.name => :pathway)\n    order(g.symbol)\nend\n\nThe >> chains are the canonical pattern syntax in @cypher — they work uniformly for queries, mutations, and everything in between. Bare patterns become implicit MATCH clauses.\n\n","title":"4.1 Multi-hop: Gene → Protein → Pathway for a disease"},{"category":"section","location":"guide/biomedical_case_study/#4.2-Drug-repurposing-—-shared-pathway-targets","page":"Biomedical Case Study","text":"Raw Cypher:\n\nMATCH (drug:Drug)-[:TARGETS]->(prot:Protein)-[:PARTICIPATES_IN]->(pw:Pathway)\nMATCH (prot2:Protein)-[:PARTICIPATES_IN]->(pw)\nMATCH (g:Gene)-[:ENCODES]->(prot2)\nMATCH (g)-[:ASSOCIATED_WITH]->(d:Disease {name: 'Breast Cancer'})\nRETURN drug.name AS drug, pw.name AS pathway, g.symbol AS gene\nORDER BY drug.name\n\n@cypher DSL:\n\ntarget_disease = \"Breast Cancer\"\nresult = @cypher conn begin\n    drug::Drug >> ::TARGETS >> prot::Protein >> ::PARTICIPATES_IN >> pw::Pathway\n    prot2::Protein >> ::PARTICIPATES_IN >> pw\n    g::Gene >> ::ENCODES >> prot2\n    g >> ::ASSOCIATED_WITH >> d::Disease\n    where(d.name == $target_disease)\n    ret(drug.name => :drug, pw.name => :pathway, g.symbol => :gene)\n    order(drug.name)\nend\n\n","title":"4.2 Drug repurposing — shared pathway targets"},{"category":"section","location":"guide/biomedical_case_study/#4.3-Patient-cohort-analysis-with-optional-trial-enrollment","page":"Biomedical Case Study","text":"Raw Cypher:\n\nMATCH (pt:Patient)-[dx:DIAGNOSED_WITH]->(d:Disease)\nOPTIONAL MATCH (pt)-[e:ENROLLED_IN]->(ct:ClinicalTrial)\nRETURN pt.patient_id AS patient, d.name AS disease,\n       dx.stage AS stage, ct.trial_id AS trial\nORDER BY d.name, pt.patient_id\n\n@cypher DSL:\n\nresult = @cypher conn begin\n    pt::Patient >> dx::DIAGNOSED_WITH >> d::Disease\n    optional(pt >> e::ENROLLED_IN >> ct::ClinicalTrial)\n    ret(pt.patient_id => :patient, d.name => :disease,\n        dx.stage => :stage, ct.trial_id => :trial)\n    order(d.name, pt.patient_id)\nend\n\noptional() maps to OPTIONAL MATCH and accepts the same >> chain patterns.\n\n","title":"4.3 Patient cohort analysis with optional trial enrollment"},{"category":"section","location":"guide/biomedical_case_study/#4.4-Aggregation:-drug-count-and-average-efficacy-per-disease","page":"Biomedical Case Study","text":"Raw Cypher:\n\nMATCH (drug:Drug)-[t:TREATS]->(d:Disease)\nWITH d.name AS disease, count(drug) AS drug_count, avg(t.efficacy) AS mean_efficacy\nWHERE drug_count > 1\nRETURN disease, drug_count, mean_efficacy\nORDER BY drug_count DESC\n\n@cypher DSL:\n\nresult = @cypher conn begin\n    drug::Drug >> t::TREATS >> d::Disease\n    with(d.name => :disease, count(drug) => :drug_count, avg(t.efficacy) => :mean_efficacy)\n    where(drug_count > 1)\n    ret(disease, drug_count, mean_efficacy)\n    order(drug_count, :desc)\nend\n\n","title":"4.4 Aggregation: drug count and average efficacy per disease"},{"category":"section","location":"guide/biomedical_case_study/#4.5-Side-effect-overlap-between-drugs","page":"Biomedical Case Study","text":"Raw Cypher:\n\nMATCH (d1:Drug)-[:HAS_SIDE_EFFECT]->(s:Symptom)\nMATCH (d2:Drug)-[:HAS_SIDE_EFFECT]->(s)\nWHERE d1.name < d2.name\nRETURN d1.name AS drug1, d2.name AS drug2, collect(s.name) AS shared_effects\nORDER BY d1.name\n\n@cypher DSL:\n\nresult = @cypher conn begin\n    d1::Drug >> ::HAS_SIDE_EFFECT >> s::Symptom\n    d2::Drug >> ::HAS_SIDE_EFFECT >> s\n    where(d1.name < d2.name)\n    ret(d1.name => :drug1, d2.name => :drug2, collect(s.name) => :shared_effects)\n    order(d1.name)\nend\n\n","title":"4.5 Side effect overlap between drugs"},{"category":"section","location":"guide/biomedical_case_study/#4.6-Full-patient-journey-—-diagnosis-to-treatment","page":"Biomedical Case Study","text":"Raw Cypher:\n\nMATCH (pt:Patient {patient_id: $pid})-[dx:DIAGNOSED_WITH]->(d:Disease)\nMATCH (drug:Drug)-[t:TREATS]->(d)\nOPTIONAL MATCH (pt)-[e:ENROLLED_IN]->(ct:ClinicalTrial)\nOPTIONAL MATCH (drug)-[:HAS_SIDE_EFFECT]->(se:Symptom)\nRETURN d.name AS disease, drug.name AS drug_option,\n       t.efficacy AS efficacy, ct.title AS trial,\n       collect(se.name) AS side_effects\nORDER BY t.efficacy DESC\n\n@cypher DSL:\n\npid = \"PT-2024-001\"\nresult = @cypher conn begin\n    pt::Patient >> dx::DIAGNOSED_WITH >> d::Disease\n    drug::Drug >> t::TREATS >> d\n    where(pt.patient_id == $pid)\n    optional(pt >> e::ENROLLED_IN >> ct::ClinicalTrial)\n    optional(drug >> ::HAS_SIDE_EFFECT >> se::Symptom)\n    ret(d.name => :disease, drug.name => :drug_option,\n        t.efficacy => :efficacy, ct.title => :trial,\n        collect(se.name) => :side_effects)\n    order(t.efficacy, :desc)\nend\n\n","title":"4.6 Full patient journey — diagnosis to treatment"},{"category":"section","location":"guide/biomedical_case_study/#4.7-Genes-with-multiple-disease-associations","page":"Biomedical Case Study","text":"Raw Cypher:\n\nMATCH (g:Gene)-[a:ASSOCIATED_WITH]->(d:Disease)\nWITH g.symbol AS gene, count(d) AS disease_count, collect(d.name) AS diseases\nWHERE disease_count >= $min_diseases\nRETURN gene, disease_count, diseases\nORDER BY disease_count DESC\n\n@cypher DSL:\n\nmin_diseases = 2\nresult = @cypher conn begin\n    g::Gene >> a::ASSOCIATED_WITH >> d::Disease\n    with(g.symbol => :gene, count(d) => :disease_count, collect(d.name) => :diseases)\n    where(disease_count >= $min_diseases)\n    ret(gene, disease_count, diseases)\n    order(disease_count, :desc)\nend\n\n","title":"4.7 Genes with multiple disease associations"},{"category":"section","location":"guide/biomedical_case_study/#4.8-Hospital-physician-coverage","page":"Biomedical Case Study","text":"Raw Cypher:\n\nMATCH (ph:Physician)-[:LOCATED_AT]->(h:Hospital)\nRETURN h.name AS hospital, collect(ph.specialty) AS specialties,\n       count(ph) AS physician_count\nORDER BY physician_count DESC\n\n@cypher DSL:\n\nresult = @cypher conn begin\n    ph::Physician >> ::LOCATED_AT >> h::Hospital\n    ret(h.name => :hospital, collect(ph.specialty) => :specialties,\n        count(ph) => :physician_count)\n    order(physician_count, :desc)\nend\n\n","title":"4.8 Hospital physician coverage"},{"category":"section","location":"guide/biomedical_case_study/#4.9-Biomarker-guided-treatment-selection","page":"Biomedical Case Study","text":"Raw Cypher:\n\nMATCH (bm:Biomarker)-[:INDICATES]->(d:Disease)\nMATCH (drug:Drug)-[:TREATS]->(d)\nRETURN bm.name AS biomarker, d.name AS disease,\n       drug.name AS drug, drug.mechanism AS mechanism\nORDER BY bm.name, drug.name\n\n@cypher DSL:\n\nresult = @cypher conn begin\n    bm::Biomarker >> ::INDICATES >> d::Disease\n    drug::Drug >> ::TREATS >> d\n    ret(bm.name => :biomarker, d.name => :disease,\n        drug.name => :drug, drug.mechanism => :mechanism)\n    order(bm.name, drug.name)\nend\n\n","title":"4.9 Biomarker-guided treatment selection"},{"category":"section","location":"guide/biomedical_case_study/#4.10-Full-knowledge-chain:-Biomarker-Disease-Gene-Protein-Pathway","page":"Biomedical Case Study","text":"Raw Cypher:\n\nMATCH (bm:Biomarker)-[:INDICATES]->(d:Disease)\nMATCH (g:Gene)-[:ASSOCIATED_WITH]->(d)\nMATCH (g)-[:ENCODES]->(p:Protein)-[:PARTICIPATES_IN]->(pw:Pathway)\nRETURN bm.name AS biomarker, d.name AS disease,\n       g.symbol AS gene, p.name AS protein, pw.name AS pathway\nORDER BY d.name, g.symbol\n\n@cypher DSL:\n\nresult = @cypher conn begin\n    bm::Biomarker >> ::INDICATES >> d::Disease\n    g::Gene >> ::ASSOCIATED_WITH >> d\n    g >> ::ENCODES >> p::Protein >> ::PARTICIPATES_IN >> pw::Pathway\n    ret(bm.name => :biomarker, d.name => :disease,\n        g.symbol => :gene, p.name => :protein, pw.name => :pathway)\n    order(d.name, g.symbol)\nend\n\nA five-hop traversal across the entire knowledge base — expressed as readable Julia. The >> chains make the directionality explicit and composable.\n\n","title":"4.10 Full knowledge chain: Biomarker → Disease → Gene → Protein → Pathway"},{"category":"section","location":"guide/biomedical_case_study/#4.11-Mixed-direction-chains:-Drug-Disease-Gene","page":"Biomedical Case Study","text":"In real knowledge graphs, not every hop goes in the same direction. The @cypher DSL handles this naturally — mix >> (forward) and << (backward) in a single chain to express convergent patterns where different entity types meet at a shared node.\n\nRaw Cypher:\n\nMATCH (dr:Drug)-[:TREATS]->(d:Disease)<-[:ASSOCIATED_WITH]-(g:Gene)\nRETURN dr.name AS drug, d.name AS disease, g.symbol AS gene\nORDER BY d.name, dr.name, g.symbol\n\n@cypher DSL:\n\nresult = @cypher conn begin\n    dr::Drug >> ::TREATS >> d::Disease << ::ASSOCIATED_WITH << g::Gene\n    ret(dr.name => :drug, d.name => :disease, g.symbol => :gene)\n    order(d.name, dr.name, g.symbol)\nend\n\nThe >> and << operators can be freely combined in a single path — each relationship is bracketed by the same operator (>> rel >> for forward, << rel << for backward), and you can have any number of direction changes:\n\n# Four-segment chain: Drug → Disease ← Gene → Protein → Pathway\nresult = @cypher conn begin\n    dr::Drug >> ::TREATS >> d::Disease << ::ASSOCIATED_WITH << g::Gene >> ::ENCODES >> p::Protein >> ::PARTICIPATES_IN >> pw::Pathway\n    ret(dr.name => :drug, d.name => :disease,\n        g.symbol => :gene, p.name => :protein, pw.name => :pathway)\n    order(d.name, g.symbol)\nend\n\ntip: Reading mixed chains\nRead >> as \"points to\" and << as \"is pointed to by\". The chain Drug >> TREATS >> Disease << ASSOCIATED_WITH << Gene reads as: \"Drug treats Disease, which Gene is associated with.\"\n\n","title":"4.11 Mixed-direction chains: Drug → Disease ← Gene"},{"category":"section","location":"guide/biomedical_case_study/#4.12-Batch-insert-with-UNWIND","page":"Biomedical Case Study","text":"Raw Cypher:\n\nUNWIND $adverse_events AS ae\nMATCH (drug:Drug)\nWHERE drug.name = ae.drug_name\nCREATE (drug)-[:REPORTED_AE]->(event:AdverseEvent)\nSET event.name = ae.event, event.grade = ae.grade\nRETURN drug.name AS drug, event.name AS event, event.grade AS grade\n\n@cypher DSL:\n\nadverse_events = [\n    Dict(\"drug_name\" => \"Trastuzumab\", \"event\" => \"Cardiotoxicity\", \"grade\" => 2),\n    Dict(\"drug_name\" => \"Erlotinib\", \"event\" => \"Rash\", \"grade\" => 1),\n    Dict(\"drug_name\" => \"Pembrolizumab\", \"event\" => \"Pneumonitis\", \"grade\" => 3),\n    Dict(\"drug_name\" => \"Olaparib\", \"event\" => \"Anemia\", \"grade\" => 2),\n]\n\nresult = @cypher conn begin\n    unwind($adverse_events => :ae)\n    drug::Drug\n    where(drug.name == ae.drug_name)\n    create(drug >> ::REPORTED_AE >> event::AdverseEvent)\n    event.name = ae.event\n    event.grade = ae.grade\n    ret(drug.name => :drug, event.name => :event, event.grade => :grade)\nend\n\n","title":"4.12 Batch insert with UNWIND"},{"category":"section","location":"guide/biomedical_case_study/#4.13-Complex-WHERE-with-string-functions","page":"Biomedical Case Study","text":"Raw Cypher:\n\nMATCH (g:Gene)-[:ASSOCIATED_WITH]->(d:Disease)\nWHERE g.chromosome STARTS WITH '17' AND d.category = 'Oncology'\nRETURN g.symbol AS gene, g.chromosome AS chr, d.name AS disease\nORDER BY g.symbol\n\n@cypher DSL:\n\nresult = @cypher conn begin\n    g::Gene >> ::ASSOCIATED_WITH >> d::Disease\n    where(startswith(g.chromosome, \"17\"), d.category == \"Oncology\")\n    ret(g.symbol => :gene, g.chromosome => :chr, d.name => :disease)\n    order(g.symbol)\nend\n\nMulti-condition where() auto-ANDs conditions — no need for &&.\n\n","title":"4.13 Complex WHERE with string functions"},{"category":"section","location":"guide/biomedical_case_study/#4.14-WITH-pipeline-—-treatment-landscape","page":"Biomedical Case Study","text":"Raw Cypher:\n\nMATCH (drug:Drug)-[t:TREATS]->(d:Disease)\nWITH d, count(drug) AS n_drugs, avg(t.efficacy) AS avg_eff\nMATCH (g:Gene)-[:ASSOCIATED_WITH]->(d)\nRETURN d.name AS disease, n_drugs, avg_eff, count(g) AS n_genes\nORDER BY n_drugs DESC\n\n@cypher DSL:\n\nresult = @cypher conn begin\n    drug::Drug >> t::TREATS >> d::Disease\n    with(d, count(drug) => :n_drugs, avg(t.efficacy) => :avg_eff)\n    g::Gene >> ::ASSOCIATED_WITH >> d\n    ret(d.name => :disease, n_drugs, avg_eff, count(g) => :n_genes)\n    order(n_drugs, :desc)\nend\n\n","title":"4.14 WITH pipeline — treatment landscape"},{"category":"section","location":"guide/biomedical_case_study/#4.15-Publication-Disease-knowledge-network","page":"Biomedical Case Study","text":"Raw Cypher:\n\nMATCH (pub:Publication)-[:PUBLISHED_IN]->(d:Disease)\nMATCH (drug:Drug)-[:TREATS]->(d)\nWITH pub, d, collect(drug.name) AS drugs\nRETURN pub.title AS publication, pub.journal AS journal,\n       d.name AS disease, drugs\nORDER BY pub.year DESC\n\n@cypher DSL:\n\nresult = @cypher conn begin\n    pub::Publication >> ::PUBLISHED_IN >> d::Disease\n    drug::Drug >> ::TREATS >> d\n    with(pub, d, collect(drug.name) => :drugs)\n    ret(pub.title => :publication, pub.journal => :journal,\n        d.name => :disease, drugs)\n    order(pub.year, :desc)\nend\n\n","title":"4.15 Publication → Disease knowledge network"},{"category":"section","location":"guide/biomedical_case_study/#Key-takeaways","page":"Biomedical Case Study","text":"Aspect Raw Cypher @cypher DSL\nInjection safety Manual parameterisation Automatic — $var captures safely\nSchema validation None built-in @node/@rel with runtime checks\nNode references Re-match by property match() + where() + create(), or @relate\nPatterns (p:Person)-[r:KNOWS]->(q:Person) p::Person >> r::KNOWS >> q::Person (everywhere)\nClauses Cypher keywords where(), ret(), order(), take()\nOperator mapping <>, AND, STARTS WITH !=, &&, startswith (Julia-native)\nReturn aliases expr AS alias expr => :alias\nMutations CREATE, SET create() + auto-SET assignments\nOPTIONAL MATCH OPTIONAL MATCH optional()\nCompile-time String at runtime Cypher assembled at macro expansion\n\nThe @cypher macro compiles to identical Cypher at macro expansion time — there is zero runtime overhead for query construction. Only parameter values are captured at runtime. The >> syntax works uniformly across queries (MATCH), mutations (CREATE, MERGE), and OPTIONAL MATCH — one pattern language for everything.","title":"Key takeaways"},{"category":"section","location":"guide/dsl/#dsl","page":"DSL","text":"Neo4jQuery includes a compile-time DSL that translates Julia expressions into parameterised Cypher. The unified @cypher macro gives you type-safe, injection-proof graph operations with Julia-native syntax.","title":"DSL"},{"category":"section","location":"guide/dsl/#Schema-declarations","page":"DSL","text":"Register node and relationship schemas for validation:\n\n@node Person begin\n    name::String\n    age::Int\n    email::String = \"\"    # optional, with default\nend\n\n@rel KNOWS begin\n    since::Int\n    weight::Float64 = 1.0\nend\n\nSchemas are stored in a global registry and used by mutation macros to validate properties at runtime.\n\nget_node_schema(:Person)\n\nget_rel_schema(:KNOWS)\n\nLabel-only schemas (no properties):\n\n@node Marker\n\n@rel LINKS","title":"Schema declarations"},{"category":"section","location":"guide/dsl/#Property-validation","page":"DSL","text":"Validation throws if required properties are missing and warns on unknown properties:\n\nschema = get_node_schema(:Person)\nvalidate_node_properties(schema, Dict{String,Any}(\"name\" => \"Alice\", \"age\" => 30))\nprintln(\"Node validation passed\")\n\nrel_schema = get_rel_schema(:KNOWS)\nvalidate_rel_properties(rel_schema, Dict{String,Any}(\"since\" => 2024))\nprintln(\"Relationship validation passed\")","title":"Property validation"},{"category":"section","location":"guide/dsl/#@cypher-—-the-unified-query-builder","page":"DSL","text":"@cypher compiles a Julia block into a single parameterised Cypher query. It combines full Cypher coverage with ergonomic Julia-native syntax:\n\nresult = @cypher conn begin\n    p::Person >> r::KNOWS >> q::Person\n    where(p.age > $min_age, q.name == $target)\n    ret(p.name => :name, r.since, q.name => :friend)\n    order(p.age, :desc)\n    take(10)\nend\n\nThis expands at compile time into:\n\nMATCH (p:Person)-[r:KNOWS]->(q:Person)\nWHERE p.age > $min_age AND q.name = $target\nRETURN p.name AS name, r.since, q.name AS friend\nORDER BY p.age DESC\nLIMIT 10","title":"@cypher — the unified query builder"},{"category":"section","location":"guide/dsl/#Pattern-syntax","page":"DSL","text":"@cypher supports two pattern styles. Use whichever feels natural — they both compile to the same Cypher.","title":"Pattern syntax"},{"category":"section","location":"guide/dsl/#chain-syntax-(recommended)","page":"DSL","text":"The >> operator is the universal pattern connector. It works the same way in MATCH, CREATE, MERGE, and OPTIONAL MATCH.\n\n# Labeled node (Julia type annotation)\np::Person                    # → (p:Person)\n\n# Anonymous node\n::Person                     # → (:Person)\n\n# Right-directed chain\np::Person >> r::KNOWS >> q::Person\n# → (p:Person)-[r:KNOWS]->(q:Person)\n\n# Anonymous relationship in chain\np::Person >> KNOWS >> q::Person\n# → (p:Person)-[:KNOWS]->(q:Person)\n\n# Left-directed chain (<< operator)\np::Person << r::KNOWS << q::Person\n# → (p:Person)<-[r:KNOWS]-(q:Person)\n\n# Multi-hop chain\na::Person >> r::KNOWS >> b::Person >> s::WORKS_AT >> c::Company\n# → (a:Person)-[r:KNOWS]->(b:Person)-[s:WORKS_AT]->(c:Company)\n\n# Mixed-direction chain (>> and << in the same path)\n# Use >> for forward hops and << for backward hops.\n# Each relationship must be bracketed by the same operator:\n#   >> rel >>  for forward,  << rel <<  for backward.\ndr::Drug >> ::TREATS >> d::Disease << ::ASSOCIATED_WITH << g::Gene\n# → (dr:Drug)-[:TREATS]->(d:Disease)<-[:ASSOCIATED_WITH]-(g:Gene)\n\n# Longer mixed chains work with any number of direction changes\na::A >> ::R1 >> b::B << ::R2 << c::C >> ::R3 >> d::D\n# → (a:A)-[:R1]->(b:B)<-[:R2]-(c:C)-[:R3]->(d:D)","title":">> chain syntax (recommended)"},{"category":"section","location":"guide/dsl/#Quantified-relationships-(variable-length,-Neo4j-5)","page":"DSL","text":"Use Julia's curly-brace syntax on relationship types for quantified (variable-length) patterns. This generates the modern GQL-conformant Cypher syntax.\n\n# Range quantifier: 2 to 5 hops\na::Person >> KNOWS{2,5} >> b::Person\n# → (a:Person)-[:KNOWS]->{2,5}(b:Person)\n\n# Exact hop count\na::Person >> KNOWS{3} >> b::Person\n# → (a:Person)-[:KNOWS]->{3}(b:Person)\n\n# One-or-more (+)\na::Person >> KNOWS{1,nothing} >> b::Person\n# → (a:Person)-[:KNOWS]->+(b:Person)\n\n# Zero-or-more (*)\na::Person >> KNOWS{0,nothing} >> b::Person\n# → (a:Person)-[:KNOWS]->*(b:Person)\n\n# Named relationship with quantifier\na::Person >> r::KNOWS{1,5} >> b::Person\n# → (a:Person)-[r:KNOWS]->{1,5}(b:Person)\n\n# Left-directed with quantifier\nb::Person << KNOWS{2,4} << a::Person\n# → (b:Person)<-[:KNOWS]-{2,4}(a:Person)\n\nThe nothing sentinel maps to an unbounded upper limit.","title":"Quantified relationships (variable-length, Neo4j 5+)"},{"category":"section","location":"guide/dsl/#Path-variables","page":"DSL","text":"Assign a path to a variable using =:\n\n# Path variable assignment\np = a::Person >> KNOWS >> b::Person\n# → MATCH p = (a:Person)-[:KNOWS]->(b:Person)\n\n# Path variable with quantifier\np = a::Person >> KNOWS{1,nothing} >> b::Person\n# → MATCH p = (a:Person)-[:KNOWS]->+(b:Person)\n\nUse length(p), nodes(p), relationships(p) on the path variable.","title":"Path variables"},{"category":"section","location":"guide/dsl/#Shortest-path-selectors-(Neo4j-5)","page":"DSL","text":"# SHORTEST 1 — single shortest path\nshortest(1, a::Person >> KNOWS{1,nothing} >> b::Person)\n# → MATCH SHORTEST 1 (a:Person)-[:KNOWS]->+(b:Person)\n\n# ALL SHORTEST — all paths with minimum length\nall_shortest(a::Person >> KNOWS{1,nothing} >> b::Person)\n# → MATCH ALL SHORTEST (a:Person)-[:KNOWS]->+(b:Person)\n\n# SHORTEST k GROUPS — k groups by length\nshortest_groups(2, a::Person >> KNOWS{1,nothing} >> b::Person)\n# → MATCH SHORTEST 2 GROUPS (a:Person)-[:KNOWS]->+(b:Person)\n\n# ANY — any single path (non-deterministic)\nany_paths(a::Person >> KNOWS{1,nothing} >> b::Person)\n# → MATCH ANY (a:Person)-[:KNOWS]->+(b:Person)\n\n# ANY k — any k paths\nany_paths(3, a::Person >> KNOWS{1,nothing} >> b::Person)\n# → MATCH ANY 3 (a:Person)-[:KNOWS]->+(b:Person)\n\n# With path variable\np = shortest(1, a::Person >> KNOWS{1,nothing} >> b::Person)\n# → MATCH p = SHORTEST 1 (a:Person)-[:KNOWS]->+(b:Person)\n\np = all_shortest(a::Person >> KNOWS{1,nothing} >> b::Person)\n# → MATCH p = ALL SHORTEST (a:Person)-[:KNOWS]->+(b:Person)","title":"Shortest path selectors (Neo4j 5+)"},{"category":"section","location":"guide/dsl/#Arrow-syntax","page":"DSL","text":"The classic Cypher-like arrow syntax also works:\n\n# Right arrow (typed)\n(p:Person)-[r:KNOWS]->(q:Person)\n\n# Simple directed arrow\n(a) --> (b)\n\n# Left arrow (typed)\n(a:Person)<-[r:KNOWS]-(b:Person)\n\n# Undirected\n(a:Person)-[r:KNOWS]-(b:Person)\n\n# Variable-length\n(a:Person)-[r:KNOWS, 1, 3]->(b:Person)\n# → (a:Person)-[r:KNOWS*1..3]->(b:Person)\n\n# Chained path\n(a)-[r:R]->(b)-[s:S]->(c)\n\n# Multiple patterns (comma-separated)\nmatch((p:Person), (c:Company))","title":"Arrow syntax"},{"category":"section","location":"guide/dsl/#Clause-functions","page":"DSL","text":"All clauses use plain function-call syntax — no @ prefixes:\n\nFunction Cypher\nwhere(cond1, cond2, ...) WHERE cond1 AND cond2 AND ...\nret(expr => :alias, ...) RETURN expr AS alias, ...\nreturning(expr => :alias) RETURN expr AS alias (synonym for ret)\nret(distinct, expr) RETURN DISTINCT expr\nret(*) RETURN *\norder(expr, :desc) ORDER BY expr DESC\ntake(n) / skip(n) LIMIT n / SKIP n\nmatch(p1, p2) MATCH p1, p2 (explicit multi-pattern)\noptional(pattern) OPTIONAL MATCH pattern\noptional(p1, p2) OPTIONAL MATCH p1, p2 (multi-pattern)\ncreate(pattern) CREATE pattern\ncreate(p1, p2) CREATE p1, p2 (multi-pattern)\nmerge(pattern) MERGE pattern\nwith(expr => :alias, ...) WITH expr AS alias, ...\nwith(distinct, expr => :alias, ...) WITH DISTINCT expr AS alias, ...\nunwind($list => :var) UNWIND $list AS var\ndelete(vars...) DELETE vars\ndetach_delete(vars...) DETACH DELETE vars\non_create(p.prop = val) ON CREATE SET p.prop = val\non_match(p.prop = val) ON MATCH SET p.prop = val\nremove(p.prop) REMOVE p.prop\np.prop = $val (assignment) SET p.prop = $val (auto-detected)\nunion() UNION\nunion_all() UNION ALL\ncall(begin ... end) CALL { ... } subquery\nload_csv(url => :row) LOAD CSV FROM url AS row\nload_csv_headers(url => :row) LOAD CSV WITH HEADERS FROM url AS row\nforeach(var, :in, expr, begin ... end) FOREACH (var IN expr | ...)\ncreate_index(:Label, :prop) CREATE INDEX FOR (n:Label) ON (n.prop)\ncreate_index(:Label, :prop, :if_not_exists) trailing :if_not_exists → IF NOT EXISTS (also on create_constraint)\ndrop_index(:name) DROP INDEX name IF EXISTS\ncreate_vector_index(:name, :Label, :prop, dims::Int, :sim) CREATE VECTOR INDEX ... IF NOT EXISTS ... OPTIONS {…} (:sim = :cosine/:euclidean)\ncreate_fulltext_index(:name, :Label, :prop...) CREATE FULLTEXT INDEX name IF NOT EXISTS FOR (n:Label) ON EACH [n.prop, …]\ncreate_constraint(:Label, :prop, :unique) CREATE CONSTRAINT ... IS UNIQUE\ncreate_constraint(:L, :p, :not_null, :n) NOT NULL constraint (:notnull also works)\ndrop_constraint(:name) DROP CONSTRAINT name IF EXISTS","title":"Clause functions"},{"category":"section","location":"guide/dsl/#Implicit-MATCH","page":"DSL","text":"Bare graph patterns in a @cypher block are automatically treated as MATCH clauses:\n\n@cypher conn begin\n    p::Person >> r::KNOWS >> q::Person    # implicit MATCH\n    where(p.age > 25)\n    ret(p.name, q.name)\nend","title":"Implicit MATCH"},{"category":"section","location":"guide/dsl/#WHERE-operators","page":"DSL","text":"Julia operators are translated to Cypher:\n\nJulia Cypher\n== =\n!= / ≠ <>\n&& AND\n|| OR\n! NOT\n>=, <=, >, < >=, <=, >, <\nstartswith STARTS WITH\nendswith ENDS WITH\ncontains CONTAINS\nin / ∈ IN\nisnothing IS NULL\nmatches =~ (regex match)\nexists((p)-[:R]->(q)) EXISTS { MATCH ... }\nif ... elseif ... else ... end CASE WHEN ... THEN ... ELSE ... END\n\nArithmetic operators (+, -, *, /, %, ^) and unary negation (-expr) are also supported:\n\nwhere(p.score * 2 + 10 > $threshold)\nwhere(p.id % 2 == 0)\nwhere(-p.balance < $limit)\n\nJulia's nothing maps to Cypher null, and vector literals [a, b, c] map to Cypher list literals:\n\nwhere(p.email != nothing)          # p.email <> null\nret(coalesce(p.name, \"unknown\"))   # any Cypher function works\n\nGeneric function pass-through: Any function call not specifically listed above is passed through verbatim as a Cypher function call. This means you can use Cypher built-in functions directly — coalesce(a, b), toUpper(x), toInteger(x), trim(s), size(list), elementId(n), labels(n), type(r), keys(n), properties(n), toString(x), point(...), distance(...), abs(x), round(x), substring(s, start, len), etc.\n\nMulti-condition WHERE auto-ANDs:\n\nwhere(p.age > 25, p.active == true, startswith(p.name, \"A\"))\n# → WHERE p.age > 25 AND p.active = true AND p.name STARTS WITH 'A'","title":"WHERE operators"},{"category":"section","location":"guide/dsl/#Parameter-capture","page":"DSL","text":"$var references capture Julia variables as safe Cypher parameters:\n\nmin_age = 25\nresult = @cypher conn begin\n    p::Person\n    where(p.age > $min_age)\n    ret(p.name => :name)\nend\n\nParameters work in where(), property assignments, unwind(), skip(), take(), and any expression position.","title":"Parameter capture"},{"category":"section","location":"guide/dsl/#Keyword-arguments","page":"DSL","text":"Pass query options after the block:\n\nresult = @cypher conn begin\n    p::Person\n    ret(p.name)\nend include_counters=true\n\nnote: Automatic access_mode\n@cypher automatically sets access_mode=:read for pure read queries and access_mode=:write when any mutation clause is present (CREATE/MERGE/SET/DELETE/…). You rarely need to specify it manually. An explicit access_mode=:write (or :read) after end overrides the inferred value.","title":"Keyword arguments"},{"category":"section","location":"guide/dsl/#Complete-end-to-end-example","page":"DSL","text":"","title":"Complete end-to-end example"},{"category":"section","location":"guide/dsl/#Step-1:-Define-your-graph-model","page":"DSL","text":"@node Person begin\n    name::String\n    age::Int\n    email::String = \"\"\nend\n\n@node Company begin\n    name::String\n    founded::Int\n    industry::String = \"Technology\"\nend\n\n@rel KNOWS begin\n    since::Int\n    weight::Float64 = 1.0\nend\n\n@rel WORKS_AT begin\n    role::String\n    since::Int\nend","title":"Step 1: Define your graph model"},{"category":"section","location":"guide/dsl/#Step-2:-Create-nodes-using-schemas","page":"DSL","text":"alice = @create conn Person(name=\"Alice\", age=30, email=\"alice@example.com\")\n\nbob = @create conn Person(name=\"Bob\", age=25)\n\ncarol = @create conn Person(name=\"Carol\", age=35)\n\nacme = @create conn Company(name=\"Acme Corp\", founded=2010)","title":"Step 2: Create nodes using schemas"},{"category":"section","location":"guide/dsl/#Step-3:-Create-relationships","page":"DSL","text":"rel1 = @relate conn alice => KNOWS(since=2020) => bob\n\nrel2 = @relate conn alice => KNOWS(since=2022, weight=0.8) => carol\n\nrel3 = @relate conn bob => KNOWS(since=2023) => carol\n\n@relate conn alice => WORKS_AT(role=\"Engineer\", since=2021) => acme\n\n@relate conn bob => WORKS_AT(role=\"Designer\", since=2022) => acme","title":"Step 3: Create relationships"},{"category":"section","location":"guide/dsl/#Step-4:-Query-with-@cypher","page":"DSL","text":"min_age = 20\nresult = @cypher conn begin\n    p::Person >> r::KNOWS >> friend::Person\n    where(p.name == \"Alice\", friend.age > $min_age)\n    ret(friend.name => :name, r.since => :since)\n    order(r.since, :desc)\nend\n\nfor row in result\n    println(row.name, \" — known since \", row.since)\nend","title":"Step 4: Query with @cypher"},{"category":"section","location":"guide/dsl/#Step-5:-Aggregation-with-WITH","page":"DSL","text":"min_connections = 1\nresult = @cypher conn begin\n    p::Person >> r::KNOWS >> q::Person\n    with(p, count(r) => :degree)\n    where(degree > $min_connections)\n    order(degree, :desc)\n    ret(p.name => :person, degree)\nend","title":"Step 5: Aggregation with WITH"},{"category":"section","location":"guide/dsl/#Step-6:-Friend-of-friend-recommendations","page":"DSL","text":"my_name = \"Bob\"\nresult = @cypher conn begin\n    (me:Person)-[:KNOWS]->(friend:Person)-[:KNOWS]->(fof:Person)\n    where(me.name == $my_name, fof.name != me.name)\n    ret(distinct, fof.name => :suggestion)\n    take(10)\nend","title":"Step 6: Friend-of-friend recommendations"},{"category":"section","location":"guide/dsl/#Step-7:-Updating-data","page":"DSL","text":"name = \"Alice\"\nnew_age = 31\nnew_email = \"alice@latest.com\"\n\nresult = @cypher conn begin\n    p::Person\n    where(p.name == $name)\n    p.age = $new_age           # auto-SET\n    p.email = $new_email       # merged into same SET clause\n    ret(p)\nend\n\nThis generates: MATCH (p:Person) WHERE p.name = $name SET p.age = $new_age, p.email = $new_email RETURN p","title":"Step 7: Updating data"},{"category":"section","location":"guide/dsl/#Step-8:-MERGE-with-conditional-SET","page":"DSL","text":"node = @merge conn Person(name=\"Alice\") on_create(age=30) on_match(last_seen=\"2025-02-15\")\n\nOr using @cypher for more complex patterns:\n\nnow = \"2025-02-15\"\nresult = @cypher conn begin\n    merge((p:Person))\n    on_create(p.created_at = $now)\n    on_match(p.last_seen = $now)\n    ret(p)\nend","title":"Step 8: MERGE with conditional SET"},{"category":"section","location":"guide/dsl/#Step-9:-Batch-operations-with-UNWIND","page":"DSL","text":"people = [\n    Dict(\"name\" => \"Dave\", \"age\" => 28),\n    Dict(\"name\" => \"Eve\", \"age\" => 22),\n    Dict(\"name\" => \"Frank\", \"age\" => 40),\n]\n\nresult = @cypher conn begin\n    unwind($people => :person)\n    create((p:Person))\n    p.name = person.name\n    p.age = person.age\n    ret(p)\nend","title":"Step 9: Batch operations with UNWIND"},{"category":"section","location":"guide/dsl/#Step-10:-OPTIONAL-MATCH","page":"DSL","text":"result = @cypher conn begin\n    p::Person\n    optional(p >> w::WORKS_AT >> c::Company)\n    ret(p.name => :person, c.name => :company, w.role => :role)\n    order(p.name)\nend","title":"Step 10: OPTIONAL MATCH"},{"category":"section","location":"guide/dsl/#Step-11:-Pagination","page":"DSL","text":"page = 2\npage_size = 10\noffset = (page - 1) * page_size\n\nresult = @cypher conn begin\n    p::Person\n    ret(p.name => :name, p.age => :age)\n    order(p.name)\n    skip($offset)\n    take($page_size)\nend","title":"Step 11: Pagination"},{"category":"section","location":"guide/dsl/#Step-12:-Deleting-data","page":"DSL","text":"target = \"Frank\"\n@cypher conn begin\n    (p:Person)\n    where(p.name == $target)\n    detach_delete(p)\nend\n\n# Remove a property\nresult = @cypher conn begin\n    p::Person\n    remove(p.email)\n    ret(p)\nend","title":"Step 12: Deleting data"},{"category":"section","location":"guide/dsl/#Step-13:-Complex-WHERE-conditions","page":"DSL","text":"result = @cypher conn begin\n    p::Person\n    where(startswith(p.name, \"A\"), !(isnothing(p.email)), p.age >= 18)\n    ret(p.name => :name, p.email => :email)\nend\n\n# IN operator with a parameter\nallowed_names = [\"Alice\", \"Bob\", \"Carol\"]\nresult = @cypher conn begin\n    p::Person\n    where(in(p.name, $allowed_names))\n    ret(p)\nend","title":"Step 13: Complex WHERE conditions"},{"category":"section","location":"guide/dsl/#Step-14:-Aggregation-functions","page":"DSL","text":"result = @cypher conn begin\n    p::Person\n    ret(count(p) => :total, avg(p.age) => :avg_age, collect(p.name) => :names)\nend","title":"Step 14: Aggregation functions"},{"category":"section","location":"guide/dsl/#Step-15:-Pattern-direction-variants","page":"DSL","text":"# Left-directed chain\nresult = @cypher conn begin\n    a::Person << r::KNOWS << b::Person\n    ret(a.name => :target, b.name => :source, r.since => :since)\nend\n\n# Arrow syntax (left arrow)\nresult = @cypher conn begin\n    (a:Person)<-[r:KNOWS]-(b:Person)\n    ret(a.name => :target, b.name => :source)\nend\n\n# Variable-length — find paths of 1 to 3 hops\nresult = @cypher conn begin\n    (a:Person)-[r:KNOWS, 1, 3]->(b:Person)\n    ret(a.name => :start, b.name => :reachable)\nend","title":"Step 15: Pattern direction variants"},{"category":"section","location":"guide/dsl/#Step-16:-Regex-matching","page":"DSL","text":"result = @cypher conn begin\n    p::Person\n    where(matches(p.name, \"^A.*e\\$\"))\n    ret(p.name => :name)\nend","title":"Step 16: Regex matching"},{"category":"section","location":"guide/dsl/#Step-17:-CASE/WHEN-expressions","page":"DSL","text":"Use Julia's if/elseif/else/end syntax to generate Cypher CASE expressions:\n\nresult = @cypher conn begin\n    p::Person\n    ret(p.name => :name, if p.age > 65; \"senior\"; elseif p.age > 30; \"adult\"; else; \"young\"; end => :category)\nend\n\nGenerates:\n\nRETURN p.name AS name, CASE WHEN p.age > 65 THEN 'senior' WHEN p.age > 30 THEN 'adult' ELSE 'young' END AS category","title":"Step 17: CASE/WHEN expressions"},{"category":"section","location":"guide/dsl/#Step-18:-EXISTS-subqueries","page":"DSL","text":"result = @cypher conn begin\n    p::Person\n    where(exists((p)-[:KNOWS]->(:Person)))\n    ret(p.name => :name)\nend\n\n# Negated EXISTS\nresult = @cypher conn begin\n    p::Person\n    where(!(exists((p)-[:KNOWS]->(:Person))))\n    ret(p.name => :loner)\nend","title":"Step 18: EXISTS subqueries"},{"category":"section","location":"guide/dsl/#Step-19:-UNION-and-UNION-ALL","page":"DSL","text":"Combine multiple query parts:\n\nresult = @cypher conn begin\n    p::Person\n    where(p.age > 30)\n    ret(p.name => :name)\n    union()\n    p::Person\n    where(startswith(p.name, \"A\"))\n    ret(p.name => :name)\nend\n\nresult = @cypher conn begin\n    p::Person\n    ret(p.name => :name)\n    union_all()\n    c::Company\n    ret(c.name => :name)\nend","title":"Step 19: UNION and UNION ALL"},{"category":"section","location":"guide/dsl/#Step-20:-CALL-subqueries","page":"DSL","text":"Nest a full sub-query with call():\n\nresult = @cypher conn begin\n    p::Person\n    call(begin\n        with(p)\n        p >> r::KNOWS >> friend::Person\n        ret(count(friend) => :friend_count)\n    end)\n    ret(p.name => :name, friend_count)\n    order(friend_count, :desc)\nend","title":"Step 20: CALL subqueries"},{"category":"section","location":"guide/dsl/#Step-21:-LOAD-CSV","page":"DSL","text":"Import data from CSV files. The WITH HEADERS variant is recommended as it maps columns by name:\n\n# With headers (recommended)\n@cypher conn begin\n    load_csv_headers(\"file:///data/people.csv\" => :row)\n    create(p::Person)\n    p.name = row.name\n    p.age = row.age\nend\n\n# Without headers (row is a raw list)\n@cypher conn begin\n    load_csv(\"file:///data/people.csv\" => :row)\n    create(p::Person)\n    p.name = row\n    ret(p)\nend\n\nnote: Note\nLOAD CSV requires the CSV file to be accessible from the Neo4j server. For file:// URLs, the server must be configured with server.directories.import.","title":"Step 21: LOAD CSV"},{"category":"section","location":"guide/dsl/#Step-22:-FOREACH","page":"DSL","text":"Apply updates over a collection:\n\nnames = [\"Alice\", \"Bob\", \"Carol\"]\n@cypher conn begin\n    p::Person\n    where(in(p.name, $names))\n    with(collect(p) => :people)\n    foreach(people => :n, begin\n        n.verified = true\n    end)\nend\nprintln(\"FOREACH applied\")\n\nFOREACH body supports property assignments (auto-SET), create(), merge(), delete(), detach_delete(), remove(), and nested foreach().","title":"Step 22: FOREACH"},{"category":"section","location":"guide/dsl/#Step-23:-Index-and-constraint-management","page":"DSL","text":"@cypher conn begin\n    create_index(:Person, :name)\nend\nprintln(\"Index created\")\n\n# Named index\n@cypher conn begin\n    create_index(:Person, :email, :person_email_idx)\nend\nprintln(\"Named index created\")\n\n# Drop an index\n@cypher conn begin\n    drop_index(:person_email_idx)\nend\nprintln(\"Index dropped\")\n\n# Uniqueness constraint\n@cypher conn begin\n    create_constraint(:Person, :email, :unique)\nend\nprintln(\"Constraint created\")\n\n# NOT NULL constraint (named)\n@cypher conn begin\n    create_constraint(:Person, :name, :not_null, :person_name_required)\nend\nprintln(\"NOT NULL constraint created\")\n\n# Drop a constraint\n@cypher conn begin\n    drop_constraint(:person_name_required)\nend\nprintln(\"Constraint dropped\")","title":"Step 23: Index and constraint management"},{"category":"section","location":"guide/dsl/#Step-24:-Quantified-relationships","page":"DSL","text":"# Find all Person nodes reachable in exactly 2 hops via KNOWS from Alice\nresult = @cypher conn begin\n    a::Person >> KNOWS{2} >> b::Person\n    where(a.name == \"Alice\")\n    ret(distinct, b.name => :name)\n    order(b.name)\nend\n\n# One-or-more hops: find everyone reachable from Alice\nresult = @cypher conn begin\n    a::Person >> KNOWS{1,nothing} >> b::Person\n    where(a.name == \"Alice\")\n    ret(distinct, b.name => :name)\n    order(b.name)\nend","title":"Step 24: Quantified relationships"},{"category":"section","location":"guide/dsl/#Step-25:-Path-variables","page":"DSL","text":"# Assign the path to a variable, then inspect its length\nresult = @cypher conn begin\n    p = a::Person >> KNOWS >> b::Person\n    where(a.name == \"Alice\", b.name == \"Bob\")\n    ret(length(p) => :hops, a.name => :src, b.name => :dst)\nend\n\n# Path variable with quantified relationship — get all nodes along the path\nresult = @cypher conn begin\n    p = a::Person >> KNOWS{3} >> b::Person\n    where(a.name == \"Alice\", b.name == \"Eve\")\n    ret(nodes(p) => :path_nodes, length(p) => :hops)\nend","title":"Step 25: Path variables"},{"category":"section","location":"guide/dsl/#Step-26:-Shortest-path-selectors","page":"DSL","text":"# Find the single shortest path from Alice to Eve\nresult = @cypher conn begin\n    p = shortest(1, a::Person >> KNOWS{1,nothing} >> b::Person)\n    where(a.name == \"Alice\", b.name == \"Eve\")\n    ret(a.name => :src, b.name => :dst, length(p) => :hops)\nend\n\n# All shortest paths (all paths with minimum length)\nresult = @cypher conn begin\n    p = all_shortest(a::Person >> KNOWS{1,nothing} >> b::Person)\n    where(a.name == \"Alice\", b.name == \"Eve\")\n    ret(length(p) => :hops)\nend\n\n# Shortest with parameterized endpoints\nsrc_name = \"Alice\"\ndst_name = \"Eve\"\nresult = @cypher conn begin\n    p = shortest(1, a::Person >> KNOWS{1,nothing} >> b::Person)\n    where(a.name == $src_name, b.name == $dst_name)\n    ret(length(p) => :hops)\nend","title":"Step 26: Shortest path selectors"},{"category":"section","location":"guide/dsl/#Comprehension-form","page":"DSL","text":"For simple match-filter-return queries, use Julia's comprehension syntax:\n\nresult = @cypher conn [p.name for p in Person if p.age > 25]\n\nresult = @cypher conn [p for p in Person]\n\nresult = @cypher conn [(p.name, p.age) for p in Person]\n\nresult = @cypher conn [p.name => :n for p in Person]","title":"Comprehension form"},{"category":"section","location":"guide/dsl/#Known-limitations","page":"DSL","text":"These Cypher features are not supported by the DSL:\n\nInline property patterns in MATCH ({name: $v}) — Julia's parser cannot parse {…} as an expression; use where() instead\nLegacy shortestPath/allShortestPaths functions — use the modern shortest(), all_shortest() DSL functions instead\nQuantified path patterns (((a)-[r]->(b)){2,4}) — only quantified relationships (KNOWS{2,5}) are supported\nMatch modes (REPEATABLE ELEMENTS, DIFFERENT RELATIONSHIPS) are not yet supported\nProcedure calls via CALL db.xxx() (distinct from CALL subqueries)\nMap projections and list comprehensions in the Cypher sense","title":"Known limitations"},{"category":"section","location":"guide/dsl/#Standalone-mutations","page":"DSL","text":"For common single-entity operations, use the dedicated macros:","title":"Standalone mutations"},{"category":"section","location":"guide/dsl/#@create-—-create-a-node","page":"DSL","text":"node = @create conn Person(name=\"Diana\", age=28)\n\nValidates against the registered Person schema (if present).","title":"@create — create a node"},{"category":"section","location":"guide/dsl/#@merge-—-upsert-a-node","page":"DSL","text":"node = @merge conn Person(name=\"Diana\") on_create(age=28, email=\"d@b.com\") on_match(age=29)\n\nSimple merge without on_create/on_match:\n\nnode = @merge conn Person(name=\"Diana\", age=29)","title":"@merge — upsert a node"},{"category":"section","location":"guide/dsl/#@relate-—-create-a-relationship","page":"DSL","text":"diana = @create conn Person(name=\"Diana2\", age=28)\nedgar = @create conn Person(name=\"Edgar\", age=32)\n\nrel = @relate conn diana => KNOWS(since=2024) => edgar\n\nprintln(rel.type)\nprintln(rel[\"since\"])\n\nMatches nodes by elementId() and validates against the KNOWS schema (if registered).","title":"@relate — create a relationship"},{"category":"section","location":"getting_started/#getting-started","page":"Getting Started","text":"","title":"Getting Started"},{"category":"section","location":"getting_started/#Installation","page":"Getting Started","text":"Neo4jQuery.jl is currently installed from GitHub:\n\nusing Pkg\nPkg.add(url=\"https://github.com/algunion/Neo4jQuery.jl\")","title":"Installation"},{"category":"section","location":"getting_started/#Prerequisites","page":"Getting Started","text":"You need a running Neo4j instance (5.x or later) with the Query API v2 enabled. This is the default for:\n\nNeo4j Aura (cloud)\nNeo4j Community / Enterprise 5.x+ (self-hosted)","title":"Prerequisites"},{"category":"section","location":"getting_started/#Connecting","page":"Getting Started","text":"The simplest way to connect:\n\nusing Neo4jQuery\n\nconn = connect(\"localhost\", \"neo4j\";\n    port=7474,\n    auth=BasicAuth(\"neo4j\", \"password\"),\n    scheme=\"http\")\n\nOr load credentials from environment variables / a .env file:\n\nconn = connect_from_env()\nprintln(conn)\n\nSee Connections for full details.","title":"Connecting"},{"category":"section","location":"getting_started/#Your-First-Query","page":"Getting Started","text":"# Create a node using the cypher\"\" string macro (recommended)\nname = \"Alice\"\nage = 30\nresult = query(conn, cypher\"CREATE (p:Person {name: $name, age: $age}) RETURN p\";\n    include_counters=true)\n\nprintln(result[1].p)\nprintln(result.counters)\n\n# Read it back\nname = \"Alice\"\nq = cypher\"MATCH (p:Person {name: $name}) RETURN p.name AS name, p.age AS age\"\nresult = query(conn, q; access_mode=:read)\nprintln(result[1].name)\n\nYou can also use {{param}} placeholders in raw query strings — no escaping required:\n\nresult = query(conn,\n    \"MATCH (p:Person {name: {{name}}}) RETURN p.name AS name, p.age AS age\",\n    parameters=Dict{String,Any}(\"name\" => \"Alice\"); access_mode=:read)\nprintln(result[1].name, \" is \", result[1].age)","title":"Your First Query"},{"category":"section","location":"getting_started/#Quick-DSL-Example","page":"Getting Started","text":"The DSL lets you write graph queries in Julia syntax:\n\n@node Person begin\n    name::String\n    age::Int\nend\n\n@rel KNOWS begin\n    since::Int\nend\nnothing # hide\n\n# Create nodes\nalice = @create conn Person(name=\"Alice2\", age=30)\nbob   = @create conn Person(name=\"Bob2\", age=25)\n\n# Create a relationship between them\nrel = @relate conn alice => KNOWS(since=2024) => bob\n\n# Query the graph\nmin_age = 20\nresult = @cypher conn begin\n    p::Person >> r::KNOWS >> friend::Person\n    where(p.name == \"Alice2\", friend.age > $min_age)\n    ret(friend.name => :name, r.since => :since)\n    order(r.since, :desc)\nend\n\nfor row in result\n    println(row.name, \" — known since \", row.since)\nend\n\n# Or as a one-liner comprehension\nresult = @cypher conn [p.name for p in Person if p.age > 20]\n\n# Mutations with auto-SET\nresult = @cypher conn begin\n    p::Person\n    where(p.name == \"Alice2\")\n    p.age = 31\n    ret(p)\nend\n\nSee DSL for the full guide with advanced examples.","title":"Quick DSL Example"},{"category":"section","location":"getting_started/#What's-Next?","page":"Getting Started","text":"Queries — parameterised queries, counters, bookmarks\nTransactions — explicit begin/commit/rollback and do-block API\nStreaming — memory-efficient row-by-row iteration\nDSL — @cypher, @create, @merge, @relate macros\nAPI Reference — full function and type documentation","title":"What's Next?"},{"category":"section","location":"llm/#Neo4jQuery.jl-—-LLM-Reference","page":"Neo4jQuery.jl — LLM Reference","text":"Complete, high-signal reference for Neo4jQuery.jl — a Julia client for Neo4j using the Query API v2 over HTTP with Typed JSON.\n\n","title":"Neo4jQuery.jl — LLM Reference"},{"category":"section","location":"llm/#Installation","page":"Neo4jQuery.jl — LLM Reference","text":"using Pkg\nPkg.add(url=\"https://github.com/algunion/Neo4jQuery.jl\")\n\nRequires Neo4j 5.x+ with Query API v2 enabled (default for Aura and Community/Enterprise 5.x+).\n\n","title":"Installation"},{"category":"section","location":"llm/#Exports","page":"Neo4jQuery.jl — LLM Reference","text":"# Connection\nNeo4jConnection, connect, connect_from_env, dotenv\n\n# Auth\nAbstractAuth, BasicAuth, BearerAuth\n\n# Query\nquery, @cypher_str, CypherQuery\n\n# Read-only guard\nReadOnlyConnection, read_query, read_stream, ReadOnlyViolationError\n\n# Introspection — server-truth validation\nvalidate_cypher\n\n# Schema grounding (text-to-Cypher)\ngraph_schema, schema_prompt, GraphSchema\n\n# GraphRAG — vector search + index management\nvector_search, create_vector_index\n\n# Transactions\nTransaction, begin_transaction, commit!, rollback!, transaction\n\n# Streaming\nstream, StreamingResult, summary\n\n# Result types\nQueryResult, QueryCounters, Notification\n\n# Graph types\nNode, Relationship, Path, CypherPoint, CypherDuration, CypherVector, CypherTime\n\n# Errors\nNeo4jError, AuthenticationError, Neo4jQueryError, TransactionExpiredError, Neo4jHTTPError\nis_transient\n\n# DSL — Schema\nPropertyDef, NodeSchema, RelSchema, @node, @rel\nget_node_schema, get_rel_schema\nvalidate_node_properties, validate_rel_properties\n\n# DSL — Macros\n@cypher, @create, @merge, @relate\n\n","title":"Exports"},{"category":"section","location":"llm/#Connection","page":"Neo4jQuery.jl — LLM Reference","text":"","title":"Connection"},{"category":"section","location":"llm/#connect(host,-database;-kwargs...)","page":"Neo4jQuery.jl — LLM Reference","text":"conn = connect(\"localhost\", \"neo4j\"; port=7474, auth=BasicAuth(\"neo4j\", \"password\"), scheme=\"http\")\n\nParam Type Default Description\nhost String required Hostname or IP\ndatabase String required Database name\nport Int 7474 HTTP port\nauth AbstractAuth required Auth strategy\nscheme String \"http\" \"http\" or \"https\"\n\nHits GET / discovery endpoint on construction; throws on failure.","title":"connect(host, database; kwargs...)"},{"category":"section","location":"llm/#connect_from_env(;-path,-prefix)","page":"Neo4jQuery.jl — LLM Reference","text":"conn = connect_from_env(; path=\".env\", prefix=\"NEO4J_\")\n\nReads from env vars (optionally from .env file first):\n\nVariable Description\nNEO4J_URI neo4j+s://host, bolt://host, etc\nNEO4J_USERNAME Username\nNEO4J_PASSWORD Password\nNEO4J_DATABASE Database name (default: \"neo4j\")\n\nURI scheme mapping:\n\nneo4j+s://, neo4j+ssc://, bolt+s://, bolt+ssc:// → HTTPS, port 443\nneo4j://, bolt:// → HTTP, port 7474\n\nAn explicit port in the URI overrides the default: neo4j+s://host:7688 → HTTPS, port 7688.","title":"connect_from_env(; path, prefix)"},{"category":"section","location":"llm/#dotenv(path;-overwrite)","page":"Neo4jQuery.jl — LLM Reference","text":"vars = dotenv(\".env\"; overwrite=false)\n\nLoads .env into ENV. Supports # comments, quoted values. Existing keys preserved unless overwrite=true.\n\n","title":"dotenv(path; overwrite)"},{"category":"section","location":"llm/#Authentication","page":"Neo4jQuery.jl — LLM Reference","text":"auth = BasicAuth(\"neo4j\", \"password\")   # HTTP Basic (RFC 7617)\nauth = BearerAuth(\"eyJhbG...\")          # Bearer token\n\nCustom auth: subtype AbstractAuth, implement auth_header(::YourAuth) -> Pair{String,String}.\n\nstruct ApiKeyAuth <: Neo4jQuery.AbstractAuth\n    key::String\nend\nNeo4jQuery.auth_header(a::ApiKeyAuth) = \"Authorization\" => \"ApiKey $(a.key)\"\n\n","title":"Authentication"},{"category":"section","location":"llm/#Query","page":"Neo4jQuery.jl — LLM Reference","text":"","title":"Query"},{"category":"section","location":"llm/#Basic-query","page":"Neo4jQuery.jl — LLM Reference","text":"result = query(conn, \"MATCH (p:Person) RETURN p.name AS name, p.age AS age\")","title":"Basic query"},{"category":"section","location":"llm/#With-parameters-(always-use-for-user-input)","page":"Neo4jQuery.jl — LLM Reference","text":"Use {{param}} Mustache-style placeholders to avoid \\$ escaping:\n\nresult = query(conn,\n    \"MATCH (p:Person {name: {{name}}}) RETURN p\",\n    parameters=Dict{String,Any}(\"name\" => \"Alice\"))\n\nThe legacy \\$param form still works but requires escaping in Julia strings:\n\nresult = query(conn,\n    \"MATCH (p:Person {name: \\$name}) RETURN p\",\n    parameters=Dict{String,Any}(\"name\" => \"Alice\"))","title":"With parameters (always use for user input)"},{"category":"section","location":"llm/#@cypher_str-macro-(preferred)","page":"Neo4jQuery.jl — LLM Reference","text":"Captures $var references from local scope as safe parameters:\n\nname = \"Alice\"\nage = 30\nq = cypher\"MATCH (p:Person {name: $name, age: $age}) RETURN p\"\n# q.statement == \"MATCH (p:Person {name: $name, age: $age}) RETURN p\"\n# q.parameters == Dict(\"name\" => \"Alice\", \"age\" => 30)\nresult = query(conn, q)","title":"@cypher_str macro (preferred)"},{"category":"section","location":"llm/#Parameter-mismatch-warnings","page":"Neo4jQuery.jl — LLM Reference","text":"When using raw string queries with a parameters dict, the client warns if parameter keys don't appear as $key placeholders in the query string. This catches accidental Julia string interpolation (where $var was expanded before reaching Neo4j).","title":"Parameter mismatch warnings"},{"category":"section","location":"llm/#Query-options","page":"Neo4jQuery.jl — LLM Reference","text":"Keyword Type Default Description\nparameters Dict{String,Any} Dict() Query parameters\naccess_mode Symbol :write :read or :write\ninclude_counters Bool false Include mutation statistics\nbookmarks Vector{String} String[] Causal consistency bookmarks\nimpersonated_user Union{String,Nothing} nothing Impersonate another user\n\n","title":"Query options"},{"category":"section","location":"llm/#QueryResult","page":"Neo4jQuery.jl — LLM Reference","text":"Returned by query(). Each row is a NamedTuple with keys matching query field names.\n\nresult = query(conn, \"MATCH (p:Person) RETURN p.name AS name, p.age AS age\")\n\nresult[1]           # first row NamedTuple: (name = \"Alice\", age = 30)\nresult[1].name      # \"Alice\"\nresult[1:3]         # range indexing → Vector{NamedTuple}\nfirst(result)       # first row\nlast(result)        # last row\nresult.fields       # [\"name\", \"age\"]\nlength(result)      # number of rows\nsize(result)        # (nrows,)\nisempty(result)     # Bool\n\nfor row in result\n    println(row.name, \" — \", row.age)\nend\n\nnames = [row.name for row in result]","title":"QueryResult"},{"category":"section","location":"llm/#Counters","page":"Neo4jQuery.jl — LLM Reference","text":"result = query(conn, \"CREATE (n:Test) RETURN n\"; include_counters=true)\nc = result.counters\n# Fields: nodes_created, nodes_deleted, relationships_created, relationships_deleted,\n#         properties_set, labels_added, labels_removed, indexes_added, indexes_removed,\n#         constraints_added, constraints_removed, contains_updates, contains_system_updates, system_updates","title":"Counters"},{"category":"section","location":"llm/#Query-plans","page":"Neo4jQuery.jl — LLM Reference","text":"result.query_plan           # Union{JSON.Object, Nothing} — EXPLAIN plan\nresult.profiled_query_plan  # Union{JSON.Object, Nothing} — PROFILE plan","title":"Query plans"},{"category":"section","location":"llm/#Bookmarks","page":"Neo4jQuery.jl — LLM Reference","text":"r1 = query(conn, \"CREATE (n:Test)\")\nr2 = query(conn, \"MATCH (n:Test) RETURN n\"; bookmarks=r1.bookmarks)","title":"Bookmarks"},{"category":"section","location":"llm/#Notifications","page":"Neo4jQuery.jl — LLM Reference","text":"result.notifications  # Vector{Notification} — performance warnings, deprecation hints\n# Each has: code, title, description, severity, category, position\n# position is Union{JSON.Object, Nothing} — byte offset in query string\n\n","title":"Notifications"},{"category":"section","location":"llm/#Graph-Types","page":"Neo4jQuery.jl — LLM Reference","text":"","title":"Graph Types"},{"category":"section","location":"llm/#Node","page":"Neo4jQuery.jl — LLM Reference","text":"node.element_id         # String\nnode.labels             # Vector{String}\nnode.properties         # JSON.Object{String,Any}\nnode[\"name\"]            # property access (indexing)\nnode.name               # property access (dot syntax)\nhaskey(node, \"name\")    # Bool — check if property exists\nget(node, \"name\", \"\")   # property with default fallback\npropertynames(node)     # all field + property names as symbols","title":"Node"},{"category":"section","location":"llm/#Relationship","page":"Neo4jQuery.jl — LLM Reference","text":"rel.element_id                # String\nrel.start_node_element_id     # String\nrel.end_node_element_id       # String\nrel.type                      # String (e.g. \"KNOWS\")\nrel[\"since\"]                  # property access\nrel.since                     # dot syntax\nhaskey(rel, \"since\")          # Bool\nget(rel, \"since\", 0)          # with default\npropertynames(rel)            # all field + property names","title":"Relationship"},{"category":"section","location":"llm/#Path","page":"Neo4jQuery.jl — LLM Reference","text":"path.elements   # Vector{Union{Node, Relationship}} — alternating node/rel sequence","title":"Path"},{"category":"section","location":"llm/#CypherPoint","page":"Neo4jQuery.jl — LLM Reference","text":"pt.srid          # Int (e.g. 4326 for WGS84)\npt.coordinates   # Vector{Float64}","title":"CypherPoint"},{"category":"section","location":"llm/#CypherDuration","page":"Neo4jQuery.jl — LLM Reference","text":"d.value   # String — ISO-8601 (e.g. \"P1Y2M3DT4H\")","title":"CypherDuration"},{"category":"section","location":"llm/#CypherVector","page":"Neo4jQuery.jl — LLM Reference","text":"v.coordinates_type   # String\nv.coordinates        # Vector{String}","title":"CypherVector"},{"category":"section","location":"llm/#CypherTime","page":"Neo4jQuery.jl — LLM Reference","text":"A Cypher zoned TIME (time-of-day + fixed UTC offset). Round-trips losslessly through Typed JSON Time, including sub-second nanoseconds.\n\nt.time       # Dates.Time — nanosecond-resolution time-of-day\nt.timezone   # TimeZones.FixedTimeZone — the fixed UTC offset\n# Two CypherTimes compare == (and hash) on time + UTC offset,\n# so \"Z\", \"UTC\", and \"+00:00\" spellings of the zero offset are equal.\n\n","title":"CypherTime"},{"category":"section","location":"llm/#Transactions","page":"Neo4jQuery.jl — LLM Reference","text":"","title":"Transactions"},{"category":"section","location":"llm/#Explicit","page":"Neo4jQuery.jl — LLM Reference","text":"tx = begin_transaction(conn)\nquery(tx, \"CREATE (n:Person {name: \\$name})\", parameters=Dict{String,Any}(\"name\" => \"Alice\"))\nbookmarks = commit!(tx)\n\ntx = begin_transaction(conn)\nquery(tx, \"CREATE (n:Temp)\")\nrollback!(tx)   # discards all changes\n\nOptional initial/final statements:\n\ntx = begin_transaction(conn; statement=\"CREATE (n:Init) RETURN n\", parameters=Dict{String,Any}())\nbookmarks = commit!(tx; statement=\"CREATE (n:Final) RETURN n\", parameters=Dict{String,Any}())","title":"Explicit"},{"category":"section","location":"llm/#Do-block-(recommended-—-auto-commit/rollback)","page":"Neo4jQuery.jl — LLM Reference","text":"transaction(conn) do tx\n    query(tx, \"CREATE (a:Person {name: 'Alice'})\")\n    query(tx, \"CREATE (b:Person {name: 'Bob'})\")\nend  # auto-commits; auto-rolls-back on exception","title":"Do-block (recommended — auto-commit/rollback)"},{"category":"section","location":"llm/#Transaction-state","page":"Neo4jQuery.jl — LLM Reference","text":"tx.committed        # Bool\ntx.rolled_back      # Bool\ntx.expires          # String — server-reported expiry timestamp\ntx.cluster_affinity # Union{String, Nothing} — Aura cluster routing\n# Using committed/rolled-back tx throws an error\n\n","title":"Transaction state"},{"category":"section","location":"llm/#Streaming","page":"Neo4jQuery.jl — LLM Reference","text":"Row-by-row iteration over JSONL responses; memory-efficient for large result sets.\n\nsr = stream(conn, \"MATCH (p:Person) RETURN p.name AS name, p.age AS age\")\nfor row in sr\n    println(row.name)\nend\n\nSame keyword arguments as query (including include_counters). Also works inside transactions:\n\ntx = begin_transaction(conn)\nsr = stream(tx, \"MATCH (p) RETURN p\")\nrows = collect(sr)\ncommit!(tx)","title":"Streaming"},{"category":"section","location":"llm/#Summary-(after-consuming-stream)","page":"Neo4jQuery.jl — LLM Reference","text":"import Neo4jQuery: summary   # required — Base.summary takes precedence\ns = summary(sr)\n# s.bookmarks, s.counters, s.notifications, s.transaction, s.query_plan, s.profiled_query_plan\n\nStreamingResult implements Base.iterate, Base.IteratorSize (→ SizeUnknown), and Base.eltype (→ NamedTuple).\n\n","title":"Summary (after consuming stream)"},{"category":"section","location":"llm/#Errors","page":"Neo4jQuery.jl — LLM Reference","text":"Type Trigger\nAuthenticationError HTTP 401 (bad credentials)\nNeo4jQueryError Cypher syntax errors, constraint violations, etc.\nTransactionExpiredError Transaction expired or rolled back server-side\n\nAll subtype Neo4jError <: Exception. Fields: code::String, message::String (except TransactionExpiredError which has only message).\n\n","title":"Errors"},{"category":"section","location":"llm/#Pre-flight-Validation-(text-to-Cypher-loops)","page":"Neo4jQuery.jl — LLM Reference","text":"validate_cypher checks LLM-generated Cypher against the server without executing it: it runs EXPLAIN <stmt> under access_mode=:read, so the server plans the statement but never runs it. A leading PROFILE (which would execute) is stripped; a leading EXPLAIN is not doubled. Works on a ReadOnlyConnection too — even for write statements — because EXPLAIN never executes. Error positions refer to your statement (the internal EXPLAIN prefix is stripped and columns/offsets mapped back).\n\nScope (measured on a read-only repair loop): pre-flight validation does not reduce turns or tokens versus just executing — plan-time errors surface identically either way. Use it when the statement could write (validate, then refuse without running) or when execution is expensive. It cannot catch silent mistakes: a wrong property name returns nulls, not an error — ground the model with graph_schema/schema_prompt for vocabulary.\n\nroc = ReadOnlyConnection(conn)\nstmt, params = llm_generate(prompt)                 # your text-to-Cypher step\nv = validate_cypher(roc, stmt; parameters=params)   # zero-execution server check\nif v.valid\n    result = read_query(roc, stmt; parameters=params)\nelse\n    # v.error.message carries the server's line/column — feed it back to the LLM\n    stmt = llm_repair(prompt, stmt, v.error.message)\nend\n\nReturns (valid::Bool, error::Union{Neo4jQueryError,Nothing}, plan); plan is the parsed queryPlan object. Only genuine Cypher errors become valid=false — transport/auth failures are rethrown.\n\n","title":"Pre-flight Validation (text-to-Cypher loops)"},{"category":"section","location":"llm/#Schema-Grounding-(ground-your-system-prompt)","page":"Neo4jQuery.jl — LLM Reference","text":"schema_prompt renders the live database schema as a compact, deterministic block for a text-to-Cypher system prompt — one source of truth, so the prompt can never drift from the graph. Don't hand-roll schema text; read it from the server.\n\nroc = ReadOnlyConnection(conn)\nsystem_prompt = \"\"\"\nYou translate natural-language questions into Neo4j Cypher.\nUse ONLY the labels, relationships, and indexes below.\n\n$(schema_prompt(roc))\n\"\"\"\n\ngraph_schema(conn_or_roc) -> GraphSchema issues four read queries (db.schema.nodeTypeProperties(), db.schema.relTypeProperties(), a sampled connectivity MATCH ... LIMIT 1000, and SHOW INDEXES), all under access_mode=:read. It is safe on a ReadOnlyConnection to a production database — the reads are provably side-effect-free. The returned GraphSchema has typed fields labels::Vector{LabelInfo}, reltypes::Vector{RelTypeInfo}, indexes::Vector{IndexInfo} for programmatic use.\n\nschema_prompt(s::GraphSchema; max_labels=50) — or the schema_prompt(conn_or_roc; kwargs...) convenience that reads live first — renders:\n\n(:Label {prop: Type, optional?: Type}) — one line per label; mandatory properties plain, optional ones suffixed ?, multiple observed types joined with |.\n(:A)-[:T]->(:B) — one line per observed connection.\nVECTOR index name on Label(prop) 384-dim cosine — one line per vector index; the N-dim <similarity> suffix comes from options.indexConfig when present. Non-semantic index kinds are omitted.\n\nLabels beyond max_labels are reported with an explicit … and N more labels marker — never silently dropped.\n\n","title":"Schema Grounding (ground your system prompt)"},{"category":"section","location":"llm/#GraphRAG-—-Vector-Search","page":"Neo4jQuery.jl — LLM Reference","text":"vector_search is the retrieval half of a GraphRAG loop: k-nearest-neighbour search over a Neo4j vector index.\n\nroc = ReadOnlyConnection(conn)\nhits = vector_search(roc, \"chunk_vec\", query_embedding; k=5)   # query_embedding::Vector{<:Real}\nfor h in hits\n    println(h.score, \"  \", h.properties[\"text\"])\nend\n\nvector_search(conn_or_roc, index::AbstractString, embedding::AbstractVector{<:Real};\n              k::Int=5, return_node::Bool=false) -> QueryResult\n\nRuns the parameterized\n\nCALL db.index.vector.queryNodes($idx, $k, $vec) YIELD node, score\nRETURN elementId(node) AS id, labels(node) AS labels, properties(node) AS properties, score\n\nRows are ordered by descending similarity score::Float64; the id column is elementId(node) (a stable String).\nreturn_node=true switches the projection to node, score (the raw Node).\nindex, k, and embedding are sent as parameters ($idx, $k, $vec) — never string-interpolated. A hostile or write-looking index name can neither inject Cypher nor bypass the read-only guard (it is not part of the statement text). embedding is coerced to Vector{Float64}.\nOn a ReadOnlyConnection it funnels through read_query (server-enforced accessMode=Read) — safe against a production read replica.\nValidation (fail loud): ArgumentError — before any request — if k < 1, index is empty, or embedding is empty, contains a non-finite value (NaN/Inf), or is all-zero. The last two mirror the server's contract (\"Vector must only contain finite values, and have positive and finite l2-norm\"): a zero-norm vector has no direction, so cosine KNN against it is undefined.\n\nDeprecation: Neo4j 2026.04 deprecates db.index.vector.queryNodes in favour of the Cypher-25 SEARCH clause. The helper will migrate to SEARCH transparently; the signature above is the stable contract.","title":"GraphRAG — Vector Search"},{"category":"section","location":"llm/#create_vector_index(conn,-name,-label,-property;-dimensions)","page":"Neo4jQuery.jl — LLM Reference","text":"create_vector_index(conn, name, label, property;\n                    dimensions::Int, similarity::Symbol=:cosine) -> QueryResult\n\nCreates a vector index idempotently (CREATE VECTOR INDEX … IF NOT EXISTS … OPTIONS {indexConfig: …}). similarity is :cosine (default) or :euclidean.\n\ncreate_vector_index(conn, \"chunk_vec\", \"Chunk\", \"embedding\"; dimensions=384, similarity=:cosine)\n\nValidation (fail loud): ArgumentError if dimensions < 1, similarity ∉ (:cosine, :euclidean), or if name/label/property is empty or contains a backtick, quote, whitespace, or control character. Cypher DDL cannot be parameterized, so these identifiers are interpolated (and backtick-wrapped); the sanitizer makes that interpolation un-injectable.\n\n","title":"create_vector_index(conn, name, label, property; dimensions)"},{"category":"section","location":"llm/#DSL-—-Schema-System","page":"Neo4jQuery.jl — LLM Reference","text":"","title":"DSL — Schema System"},{"category":"section","location":"llm/#@node-Label-begin-...-end","page":"Neo4jQuery.jl — LLM Reference","text":"@node Person begin\n    name::String        # required\n    age::Int            # required\n    email::String = \"\"  # optional, default \"\"\nend\n\nCreates a NodeSchema constant and registers it globally. Label-only: @node Marker.","title":"@node Label begin ... end"},{"category":"section","location":"llm/#@rel-TYPE-begin-...-end","page":"Neo4jQuery.jl — LLM Reference","text":"@rel KNOWS begin\n    since::Int\n    weight::Float64 = 1.0\nend\n\nCreates a RelSchema constant. Label-only: @rel LINKS.","title":"@rel TYPE begin ... end"},{"category":"section","location":"llm/#Lookup-and-validation","page":"Neo4jQuery.jl — LLM Reference","text":"get_node_schema(:Person)   # NodeSchema or nothing\nget_rel_schema(:KNOWS)     # RelSchema or nothing\n\nvalidate_node_properties(schema, Dict{String,Any}(\"name\" => \"Alice\", \"age\" => 30))\n# Throws on missing required props; warns on unknown props. No type-checking.\n\n","title":"Lookup and validation"},{"category":"section","location":"llm/#DSL-—-Standalone-Mutation-Macros","page":"Neo4jQuery.jl — LLM Reference","text":"","title":"DSL — Standalone Mutation Macros"},{"category":"section","location":"llm/#@create-—-create-a-node","page":"Neo4jQuery.jl — LLM Reference","text":"node = @create conn Person(name=\"Alice\", age=30)\n# Returns: Node\n\nValidates against registered schema if present.","title":"@create — create a node"},{"category":"section","location":"llm/#@merge-—-upsert-a-node","page":"Neo4jQuery.jl — LLM Reference","text":"node = @merge conn Person(name=\"Alice\") on_create(age=30) on_match(last_seen=\"2025-02-15\")\nnode = @merge conn Person(name=\"Alice\", age=30)   # simple merge","title":"@merge — upsert a node"},{"category":"section","location":"llm/#@relate-—-create-a-relationship","page":"Neo4jQuery.jl — LLM Reference","text":"alice = @create conn Person(name=\"Alice\", age=30)\nbob = @create conn Person(name=\"Bob\", age=25)\nrel = @relate conn alice => KNOWS(since=2024) => bob\n# Returns: Relationship\n# Matches nodes by elementId()\n\n","title":"@relate — create a relationship"},{"category":"section","location":"llm/#DSL-—-@cypher-Macro-(Unified-Query-Builder)","page":"Neo4jQuery.jl — LLM Reference","text":"Compiles a Julia block into parameterised Cypher at macro expansion time. Only $param values are captured at runtime.","title":"DSL — @cypher Macro (Unified Query Builder)"},{"category":"section","location":"llm/#Invocation-forms","page":"Neo4jQuery.jl — LLM Reference","text":"# Block form\nresult = @cypher conn begin\n    p::Person >> r::KNOWS >> q::Person\n    where(p.age > $min_age)\n    ret(p.name => :name, r.since)\n    order(p.age, :desc)\n    take(10)\nend\n\n# With keyword arguments\nresult = @cypher conn begin\n    p::Person\n    ret(p)\nend include_counters=true access_mode=:write\n\n# Comprehension form\nresult = @cypher conn [p.name for p in Person if p.age > 25]\nresult = @cypher conn [(p.name, p.age) for p in Person]\nresult = @cypher conn [p.name => :n for p in Person]\nresult = @cypher conn [p for p in Person]","title":"Invocation forms"},{"category":"section","location":"llm/#access_mode-auto-inference","page":"Neo4jQuery.jl — LLM Reference","text":"@cypher automatically sets access_mode=:read for pure reads and :write when any mutation clause is present. Explicit kwarg overrides.\n\n","title":"access_mode auto-inference"},{"category":"section","location":"llm/#Pattern-Syntax","page":"Neo4jQuery.jl — LLM Reference","text":"","title":"Pattern Syntax"},{"category":"section","location":"llm/#chain-(recommended)","page":"Neo4jQuery.jl — LLM Reference","text":"p::Person                                   # (p:Person)\n::Person                                    # (:Person)\np::Person >> r::KNOWS >> q::Person          # (p:Person)-[r:KNOWS]->(q:Person)\np::Person >> KNOWS >> q::Person             # (p:Person)-[:KNOWS]->(q:Person)\np::Person << r::KNOWS << q::Person          # (p:Person)<-[r:KNOWS]-(q:Person)\na::A >> R1 >> b::B >> R2 >> c::C            # (a:A)-[:R1]->(b:B)-[:R2]->(c:C)\n\n# Mixed-direction chains (>> forward, << backward)\ndr::Drug >> ::TREATS >> d::Disease << ::ASSOCIATED_WITH << g::Gene\n# → (dr:Drug)-[:TREATS]->(d:Disease)<-[:ASSOCIATED_WITH]-(g:Gene)","title":">> chain (recommended)"},{"category":"section","location":"llm/#Quantified-relationships-(variable-length,-Neo4j-5)","page":"Neo4jQuery.jl — LLM Reference","text":"a::Person >> KNOWS{2,5} >> b::Person        # (a:Person)-[:KNOWS]->{2,5}(b:Person)\na::Person >> KNOWS{3} >> b::Person           # exact: (a:Person)-[:KNOWS]->{3}(b:Person)\na::Person >> KNOWS{1,nothing} >> b::Person   # one-or-more: ->+\na::Person >> KNOWS{0,nothing} >> b::Person   # zero-or-more: ->*\na::Person >> r::KNOWS{1,5} >> b::Person      # named: (a)-[r:KNOWS]->{1,5}(b)\nb::Person << KNOWS{2,4} << a::Person         # left-directed with quantifier","title":"Quantified relationships (variable-length, Neo4j 5+)"},{"category":"section","location":"llm/#Path-variables","page":"Neo4jQuery.jl — LLM Reference","text":"p = a::Person >> KNOWS >> b::Person          # MATCH p = (a:Person)-[:KNOWS]->(b:Person)\np = a::Person >> KNOWS{1,nothing} >> b::Person  # path with quantifier\n\nUse length(p), nodes(p), relationships(p) on path variables.","title":"Path variables"},{"category":"section","location":"llm/#Shortest-path-selectors-(Neo4j-5)","page":"Neo4jQuery.jl — LLM Reference","text":"shortest(1, a::Person >> KNOWS{1,nothing} >> b::Person)        # SHORTEST 1\nall_shortest(a::Person >> KNOWS{1,nothing} >> b::Person)        # ALL SHORTEST\nshortest_groups(2, a::Person >> KNOWS{1,nothing} >> b::Person)  # SHORTEST 2 GROUPS\nany_paths(a::Person >> KNOWS{1,nothing} >> b::Person)           # ANY\nany_paths(3, a::Person >> KNOWS{1,nothing} >> b::Person)        # ANY 3\n\n# With path variable:\np = shortest(1, a::Person >> KNOWS{1,nothing} >> b::Person)\np = all_shortest(a::Person >> KNOWS{1,nothing} >> b::Person)","title":"Shortest path selectors (Neo4j 5+)"},{"category":"section","location":"llm/#Arrow-syntax-(also-supported)","page":"Neo4jQuery.jl — LLM Reference","text":"(p:Person)-[r:KNOWS]->(q:Person)            # right arrow\n(a:Person)<-[r:KNOWS]-(b:Person)            # left arrow\n(a:Person)-[r:KNOWS]-(b:Person)             # undirected\n(a:Person)-[r:KNOWS, 1, 3]->(b:Person)      # variable-length *1..3\n(a) --> (b)                                 # simple right arrow\n(a) <-- (b)                                 # simple left arrow\n\nBare patterns in a block are implicit MATCH clauses.\n\n","title":"Arrow syntax (also supported)"},{"category":"section","location":"llm/#Clause-Functions","page":"Neo4jQuery.jl — LLM Reference","text":"DSL Cypher\nwhere(cond1, cond2) WHERE cond1 AND cond2\nret(expr => :alias, ...) RETURN expr AS alias, ...\nreturning(expr => :alias) RETURN expr AS alias (synonym)\nret(distinct, expr) RETURN DISTINCT expr\nret(*) RETURN *\norder(expr, :desc) ORDER BY expr DESC\ntake(n) / skip(n) LIMIT n / SKIP n\nmatch(p1, p2) MATCH p1, p2\noptional(pattern) OPTIONAL MATCH pattern\noptional(p1, p2) OPTIONAL MATCH p1, p2 (multi-pattern)\ncreate(pattern) CREATE pattern\ncreate(p1, p2) CREATE p1, p2 (multi-pattern)\nmerge(pattern) MERGE pattern\nwith(expr => :alias, ...) WITH expr AS alias, ...\nwith(distinct, expr => :alias, ...) WITH DISTINCT expr AS alias, ...\nunwind($list => :var) UNWIND $list AS var\ndelete(vars...) DELETE vars\ndetach_delete(vars...) DETACH DELETE vars\non_create(p.prop = val) ON CREATE SET p.prop = val\non_match(p.prop = val) ON MATCH SET p.prop = val\nremove(p.prop) REMOVE p.prop\np.prop = $val (assignment) SET p.prop = $val\nunion() UNION\nunion_all() UNION ALL\ncall(begin ... end) CALL { ... } subquery\nload_csv(url => :row) LOAD CSV FROM url AS row\nload_csv_headers(url => :row) LOAD CSV WITH HEADERS FROM url AS row\nforeach(var, :in, expr, begin ... end) FOREACH (var IN expr | ...)\ncreate_index(:Label, :prop) CREATE INDEX FOR (n:Label) ON (n.prop)\ncreate_index(:Label, :prop, :name) named index\ncreate_index(:Label, :prop, :if_not_exists) trailing :if_not_exists → IF NOT EXISTS (also on create_constraint)\ndrop_index(:name) DROP INDEX name IF EXISTS\ncreate_vector_index(:name, :Label, :prop, dims::Int, :sim) CREATE VECTOR INDEX name IF NOT EXISTS FOR (n:Label) ON n.prop OPTIONS {…} (:sim = :cosine/:euclidean)\ncreate_fulltext_index(:name, :Label, :prop...) CREATE FULLTEXT INDEX name IF NOT EXISTS FOR (n:Label) ON EACH [n.prop, …]\ncreate_constraint(:Label, :prop, :unique) uniqueness constraint\ncreate_constraint(:Label, :prop, :not_null, :name) NOT NULL constraint (named) (:notnull also accepted)\ndrop_constraint(:name) DROP CONSTRAINT name IF EXISTS\n\n","title":"Clause Functions"},{"category":"section","location":"llm/#WHERE-Operator-Mapping","page":"Neo4jQuery.jl — LLM Reference","text":"Julia Cypher\n== =\n!= / ≠ <>\n&& AND\n|| OR\n! NOT\n>=, <=, >, < same\n+, -, *, /, %, ^ same (arithmetic)\n-expr (unary) -expr\nstartswith(p.name, \"A\") p.name STARTS WITH 'A'\nendswith(p.name, \"e\") p.name ENDS WITH 'e'\ncontains(p.name, \"li\") p.name CONTAINS 'li'\nin(p.name, $list) p.name IN $list\nisnothing(p.email) p.email IS NULL\nmatches(p.name, \"^A.*\") p.name =~ '^A.*'\nexists((p)-[:R]->(q)) EXISTS { MATCH (p)-[:R]->(q) }\nif/elseif/else/end CASE WHEN ... THEN ... ELSE ... END\nnothing null literal\n[a, b, c] [a, b, c] (Cypher list literal)\n\nMulti-condition where() auto-ANDs: where(a, b, c) → WHERE a AND b AND c.\n\nGeneric function pass-through: Any function call not listed above is passed through verbatim to Cypher. This means you can use any Cypher built-in function directly — coalesce(a, b), toUpper(x), toInteger(x), trim(s), size(list), id(n), elementId(n), labels(n), type(r), keys(n), properties(n), toString(x), point(...), distance(...), date(...), abs(x), round(x), substring(s, start, len), etc.\n\n","title":"WHERE Operator Mapping"},{"category":"section","location":"llm/#Parameter-Capture","page":"Neo4jQuery.jl — LLM Reference","text":"$var captures Julia variables as safe Cypher parameters. Works in where(), property assignments, unwind(), skip(), take(), and any expression.\n\nmin_age = 25\nresult = @cypher conn begin\n    p::Person\n    where(p.age > $min_age)\n    ret(p.name)\nend\n\n","title":"Parameter Capture"},{"category":"section","location":"llm/#Auto-SET","page":"Neo4jQuery.jl — LLM Reference","text":"Property assignments become SET clauses; multiple merge into one:\n\n@cypher conn begin\n    p::Person\n    where(p.name == $name)\n    p.age = $new_age\n    p.email = $new_email\n    ret(p)\nend\n# → MATCH (p:Person) WHERE p.name = $name SET p.age = $new_age, p.email = $new_email RETURN p\n\n","title":"Auto-SET"},{"category":"section","location":"llm/#Complete-Examples","page":"Neo4jQuery.jl — LLM Reference","text":"","title":"Complete Examples"},{"category":"section","location":"llm/#Aggregation-with-WITH","page":"Neo4jQuery.jl — LLM Reference","text":"result = @cypher conn begin\n    p::Person >> r::KNOWS >> q::Person\n    with(p, count(r) => :degree)\n    where(degree > $min_connections)\n    order(degree, :desc)\n    ret(p.name => :person, degree)\nend","title":"Aggregation with WITH"},{"category":"section","location":"llm/#OPTIONAL-MATCH","page":"Neo4jQuery.jl — LLM Reference","text":"result = @cypher conn begin\n    p::Person\n    optional(p >> w::WORKS_AT >> c::Company)\n    ret(p.name => :person, c.name => :company)\nend","title":"OPTIONAL MATCH"},{"category":"section","location":"llm/#Batch-with-UNWIND","page":"Neo4jQuery.jl — LLM Reference","text":"people = [Dict(\"name\" => \"Dave\", \"age\" => 28), Dict(\"name\" => \"Eve\", \"age\" => 22)]\nresult = @cypher conn begin\n    unwind($people => :person)\n    create((p:Person))\n    p.name = person.name\n    p.age = person.age\n    ret(p)\nend","title":"Batch with UNWIND"},{"category":"section","location":"llm/#UNION","page":"Neo4jQuery.jl — LLM Reference","text":"result = @cypher conn begin\n    p::Person\n    where(p.age > 30)\n    ret(p.name => :name)\n    union()\n    p::Person\n    where(startswith(p.name, \"A\"))\n    ret(p.name => :name)\nend","title":"UNION"},{"category":"section","location":"llm/#CALL-subquery","page":"Neo4jQuery.jl — LLM Reference","text":"result = @cypher conn begin\n    p::Person\n    call(begin\n        with(p)\n        p >> r::KNOWS >> friend::Person\n        ret(count(friend) => :friend_count)\n    end)\n    ret(p.name => :name, friend_count)\nend","title":"CALL subquery"},{"category":"section","location":"llm/#FOREACH","page":"Neo4jQuery.jl — LLM Reference","text":"names = [\"Alice\", \"Bob\"]\n@cypher conn begin\n    p::Person\n    where(in(p.name, $names))\n    with(collect(p) => :people)\n    foreach(people => :n, begin\n        n.verified = true\n    end)\nend","title":"FOREACH"},{"category":"section","location":"llm/#MERGE-with-conditional-SET","page":"Neo4jQuery.jl — LLM Reference","text":"result = @cypher conn begin\n    merge((p:Person))\n    on_create(p.created_at = $now)\n    on_match(p.last_seen = $now)\n    ret(p)\nend","title":"MERGE with conditional SET"},{"category":"section","location":"llm/#Delete","page":"Neo4jQuery.jl — LLM Reference","text":"@cypher conn begin\n    (p:Person)\n    where(p.name == $target)\n    detach_delete(p)\nend","title":"Delete"},{"category":"section","location":"llm/#Pagination","page":"Neo4jQuery.jl — LLM Reference","text":"result = @cypher conn begin\n    p::Person\n    ret(p.name => :name)\n    order(p.name)\n    skip($offset)\n    take($page_size)\nend","title":"Pagination"},{"category":"section","location":"llm/#Variable-length-paths","page":"Neo4jQuery.jl — LLM Reference","text":"result = @cypher conn begin\n    (a:Person)-[r:KNOWS, 1, 3]->(b:Person)\n    ret(a.name => :start, b.name => :reachable)\nend","title":"Variable-length paths"},{"category":"section","location":"llm/#CASE/WHEN","page":"Neo4jQuery.jl — LLM Reference","text":"result = @cypher conn begin\n    p::Person\n    ret(p.name => :name, if p.age > 65; \"senior\"; elseif p.age > 30; \"adult\"; else; \"young\"; end => :category)\nend","title":"CASE/WHEN"},{"category":"section","location":"llm/#EXISTS-subquery","page":"Neo4jQuery.jl — LLM Reference","text":"result = @cypher conn begin\n    p::Person\n    where(exists((p)-[:KNOWS]->(:Person)))\n    ret(p.name)\nend","title":"EXISTS subquery"},{"category":"section","location":"llm/#Regex-matching","page":"Neo4jQuery.jl — LLM Reference","text":"result = @cypher conn begin\n    p::Person\n    where(matches(p.name, \"^A.*e\\$\"))\n    ret(p.name)\nend\n\n","title":"Regex matching"},{"category":"section","location":"llm/#DSL-Known-Limitations","page":"Neo4jQuery.jl — LLM Reference","text":"These Cypher features are not supported by the DSL:\n\nInline property patterns in MATCH ({name: $v}) — Julia's parser cannot parse {…}; use where() instead\nLegacy shortestPath / allShortestPaths — use the modern shortest(), all_shortest() DSL functions instead\nQuantified path patterns (((a)-[r]->(b)){2,4}) — only quantified relationships (KNOWS{2,5}) are supported\nMatch modes (REPEATABLE ELEMENTS, DIFFERENT RELATIONSHIPS) are not yet supported\nProcedure calls via CALL db.xxx() (distinct from CALL subqueries)\nMap projections and list comprehensions (Cypher sense)\n\n","title":"DSL Known Limitations"},{"category":"section","location":"llm/#Type-Mapping-(Typed-JSON)","page":"Neo4jQuery.jl — LLM Reference","text":"Neo4j Query API uses Typed JSON envelopes: {\"$type\": \"Integer\", \"_value\": \"42\"}.\n\nNeo4j Integer is 64-bit signed: out-of-range Julia integers (BigInt, Int128, large UInt64) are rejected client-side with a loud ArgumentError before any request. Julia Float32 values (e.g. embedding vectors) widen to Float64 on the wire: round-trips are exact at Float32 precision (Float32.(returned) == sent) but not bit-equal under Float64 comparison — do not assert exact Float64 equality on embeddings.\n\nNeo4j Type Julia Type\nNull nothing\nBoolean Bool\nInteger Int64\nFloat Float64\nString String\nBase64 Vector{UInt8}\nList Vector\nMap JSON.Object\nDate Dates.Date\nTime CypherTime\nLocalTime Dates.Time\nOffsetDateTime TimeZones.ZonedDateTime\nZonedDateTime TimeZones.ZonedDateTime\nLocalDateTime DateTime\nDuration CypherDuration\nPoint CypherPoint\nNode Node\nRelationship Relationship\nPath Path\nVector CypherVector\n\nTemporal notes: Neo4j Time (zoned time-of-day) materializes as CypherTime (a Dates.Time + fixed offset), while offset-free LocalTime is a bare Dates.Time. Both OffsetDateTime (numeric offset, e.g. +01:00) and ZonedDateTime (named IANA zone, e.g. Europe/Paris) materialize as TimeZones.ZonedDateTime; on the way out, to_typed_json lowers a fixed-offset zone back to OffsetDateTime and a named zone to ZonedDateTime.\n\nJulia→Neo4j serialization (to_typed_json): Julia values are wrapped in typed envelopes when sent as parameters.\n\n","title":"Type Mapping (Typed JSON)"},{"category":"section","location":"llm/#HTTP-Protocol-Details","page":"Neo4jQuery.jl — LLM Reference","text":"Content-Type: application/vnd.neo4j.query.v1.1 (Typed JSON)\nStreaming Accept: application/vnd.neo4j.query.v1.1+jsonl (JSONL)\nImplicit query endpoint: {base_url}/db/{database}/query/v2\nTransaction endpoint: {base_url}/db/{database}/query/v2/tx\nTransaction operations: POST .../tx (begin), POST .../tx/{id} (query), POST .../tx/{id}/commit, DELETE .../tx/{id} (rollback)\nCluster affinity via neo4j-cluster-affinity header (Aura)\n\n","title":"HTTP Protocol Details"},{"category":"section","location":"llm/#Source-File-Map","page":"Neo4jQuery.jl — LLM Reference","text":"File Purpose\nsrc/Neo4jQuery.jl Module definition, includes, exports\nsrc/auth.jl BasicAuth, BearerAuth, auth_header\nsrc/connection.jl Neo4jConnection, connect, discovery\nsrc/cypher_macro.jl CypherQuery, @cypher_str\nsrc/env.jl dotenv, connect_from_env, URI parsing\nsrc/errors.jl Error types hierarchy, is_transient\nsrc/introspect.jl validate_cypher, graph_schema/schema_prompt, vector_search/create_vector_index\nsrc/query.jl query() for connections and CypherQuery\nsrc/readonly.jl ReadOnlyConnection, read_query/read_stream, write-clause guard\nsrc/request.jl HTTP helpers, error handling\nsrc/result.jl QueryResult, QueryCounters, Notification\nsrc/streaming.jl stream(), StreamingResult, summary\nsrc/transactions.jl Transaction, begin_transaction, commit!, etc.\nsrc/typed_json.jl Typed JSON (de)serialization\nsrc/types.jl Node, Relationship, Path, spatial/temporal\nsrc/dsl/schema.jl @node, @rel, PropertyDef, validation\nsrc/dsl/compile.jl AST→Cypher compilation (macro expansion time)\nsrc/dsl/mutations.jl @create, @merge, @relate\nsrc/dsl/cypher.jl @cypher macro (unified DSL)","title":"Source File Map"},{"category":"section","location":"guide/queries/#queries","page":"Queries","text":"","title":"Queries"},{"category":"section","location":"guide/queries/#Implicit-(auto-commit)-queries","page":"Queries","text":"The simplest way to execute Cypher:\n\nresult = query(conn, \"MATCH (p:Person) RETURN p.name AS name\")\n\nEvery call creates an implicit (auto-commit) transaction that opens and closes with a single request.","title":"Implicit (auto-commit) queries"},{"category":"section","location":"guide/queries/#Parameters-with-@cypher_str-(recommended)","page":"Queries","text":"The cypher\"\" string macro automatically captures local variables as parameterised Cypher — no escaping, no boilerplate:\n\nname = \"Alice\"\nage = 30\nq = cypher\"CREATE (p:Person {name: $name, age: $age}) RETURN p\"\nresult = query(conn, q)\n\nThe macro produces a CypherQuery that carries both the parameterised statement and the captured bindings. This prevents Cypher injection and enables server-side query plan caching.\n\nMulti-parameter example:\n\nmin_age = 25\ncity = \"Berlin\"\nq = cypher\"MATCH (p:Person) WHERE p.age > $min_age AND p.city = $city RETURN p\"\nresult = query(conn, q; access_mode=:read)\n\nParameter capture also works inside procedure calls — pass Julia values (vectors, integers, …) straight through instead of hand-building a parameter dict (shown here without execution, as it requires a vector index):\n\nidx = \"embeddings\"; k = 5; vec = rand(Float64, 384)\nq = cypher\"CALL db.index.vector.queryNodes($idx, $k, $vec) YIELD node, score RETURN elementId(node) AS id, score\"\nresult = query(conn, q)   # idx, k, vec captured as parameters","title":"Parameters with @cypher_str (recommended)"},{"category":"section","location":"guide/queries/#Parameters-with-raw-strings","page":"Queries","text":"If you prefer raw strings, pass a parameters dict.  Use \\$ to denote Neo4j parameter placeholders, or {{param}} Mustache-style placeholders to avoid escaping entirely:\n\n# Mustache-style (no escaping needed)\nresult = query(conn,\n    \"MATCH (p:Person {name: {{name}}}) RETURN p\",\n    parameters=Dict{String,Any}(\"name\" => \"Alice\"))\n\n# Traditional \\$-style\nresult = query(conn,\n    \"MATCH (p:Person) WHERE p.age > \\$min_age AND p.age < \\$max_age RETURN p.name AS name, p.age AS age\",\n    parameters=Dict{String,Any}(\"min_age\" => 20, \"max_age\" => 40);\n    access_mode=:read)","title":"Parameters with raw strings"},{"category":"section","location":"guide/queries/#Options","page":"Queries","text":"Keyword Type Default Description\nparameters Dict{String,Any} Dict() Query parameters\naccess_mode Symbol :write :read or :write — server routing hint\ninclude_counters Bool false Include mutation statistics\nbookmarks Vector{String} String[] Causal consistency bookmarks\nimpersonated_user Union{String, Nothing} nothing Impersonate a different user","title":"Options"},{"category":"section","location":"guide/queries/#Working-with-results","page":"Queries","text":"QueryResult supports indexing, iteration, and standard Julia protocols:\n\nresult = query(conn, \"MATCH (p:Person) RETURN p.name AS name, p.age AS age ORDER BY p.name\")\n\n# Indexing\nfirst_row = result[1]\nprintln(\"First: \", first_row)\n\n# Range indexing\nsubset = result[1:2]\nprintln(\"Subset: \", subset)\n\n# first / last\nprintln(\"First: \", first(result))\nprintln(\"Last: \", last(result))\n\n# Fields\nprintln(\"Fields: \", result.fields)\n\n# Iteration\nfor row in result\n    println(row.name, \" — \", row.age)\nend\n\n# Comprehensions\nnames = [row.name for row in result]\nprintln(\"Names: \", names)\n\n# Standard functions\nprintln(\"Length: \", length(result))\nprintln(\"Size: \", size(result))\nprintln(\"Empty? \", isempty(result))","title":"Working with results"},{"category":"section","location":"guide/queries/#Counters","page":"Queries","text":"When include_counters=true, the result includes a QueryCounters struct:\n\nresult = query(conn, \"CREATE (n:Test) RETURN n\"; include_counters=true)\nc = result.counters\nprintln(\"Nodes created: \", c.nodes_created)\nprintln(\"Properties set: \", c.properties_set)\nprintln(\"Labels added: \", c.labels_added)\n\nAvailable counter fields: nodes_created, nodes_deleted, relationships_created, relationships_deleted, properties_set, labels_added, labels_removed, indexes_added, indexes_removed, constraints_added, constraints_removed, contains_updates, contains_system_updates, system_updates.","title":"Counters"},{"category":"section","location":"guide/queries/#Query-plans","page":"Queries","text":"The result also carries optional query plan data:\n\nresult.query_plan           # Union{JSON.Object, Nothing} — EXPLAIN plan\nresult.profiled_query_plan  # Union{JSON.Object, Nothing} — PROFILE plan","title":"Query plans"},{"category":"section","location":"guide/queries/#Bookmarks","page":"Queries","text":"Every result carries bookmarks for causal consistency across queries:\n\nr1 = query(conn, \"CREATE (n:Test)\")\nr2 = query(conn, \"MATCH (n:Test) RETURN n\"; bookmarks=r1.bookmarks)\nprintln(\"Bookmarks: \", length(r2.bookmarks), \" bookmark(s)\")\nprintln(\"Test nodes found: \", length(r2))","title":"Bookmarks"},{"category":"section","location":"guide/queries/#Notifications","page":"Queries","text":"The server may attach performance warnings or deprecation hints:\n\nresult = query(conn, \"MATCH (a), (b) RETURN a, b\")\nfor n in result.notifications\n    println(n.severity, \": \", n.title)\n    println(\"  \", n.description)\n    n.position !== nothing && println(\"  Position: \", n.position)\nend\nprintln(\"Notifications: \", length(result.notifications))","title":"Notifications"},{"category":"section","location":"guide/queries/#Graph-types","page":"Queries","text":"Nodes, relationships, and paths are returned as rich Julia structs:\n\nresult = query(conn, \"MATCH (p:Person)-[r:KNOWS]->(q) RETURN p, r, q\")\nrow = result[1]\n\nnode = row.p\nprintln(\"Node: \", node)\nprintln(\"Labels: \", node.labels)\nprintln(\"Name: \", node[\"name\"])\nprintln(\"Has name? \", haskey(node, \"name\"))\nprintln(\"City: \", get(node, \"city\", \"unknown\"))\n\nrel = row.r\nprintln(\"Rel type: \", rel.type)\nprintln(\"Since: \", rel[\"since\"])","title":"Graph types"},{"category":"section","location":"guide/queries/#Paths","page":"Queries","text":"result = query(conn, \"\"\"\n    MATCH path = (a:Person)-[:KNOWS*1..3]->(b:Person)\n    WHERE a.name = 'Alice'\n    RETURN path\n\"\"\"; access_mode=:read)\n\np = result[1].path\nprintln(\"Path: \", p)\nprintln(\"Elements: \", p.elements)","title":"Paths"},{"category":"section","location":"guide/queries/#Spatial-values","page":"Queries","text":"result = query(conn, \"RETURN point({latitude: 51.5, longitude: -0.1}) AS pt\")\npt = result[1].pt\nprintln(\"SRID: \", pt.srid)\nprintln(\"Coordinates: \", pt.coordinates)","title":"Spatial values"},{"category":"section","location":"guide/queries/#Duration-values","page":"Queries","text":"result = query(conn, \"RETURN duration('P1Y2M3DT4H') AS d\")\nd = result[1].d\nprintln(\"Duration: \", d.value)","title":"Duration values"},{"category":"section","location":"guide/agentic/#agentic","page":"Agentic Systems","text":"Neo4jQuery is built to be a reliable foundation for LLM and agentic consumers — text-to-Cypher question answering, GraphRAG retrieval, and multi-step agents that read (and sometimes write) a graph. Those consumers fail on the tail: the model emits invalid Cypher, a retrieval call races a write, a transient cluster error interrupts a step. This guide covers the primitives that make those failures loud, typed, and recoverable instead of silent.\n\nThe running assumption throughout: the LLM's output is a conjecture to be refuted before it touches production data. Validate it, ground it, bound it, and classify its failures.\n\nAll snippets assume a connection and a read-only wrapper:\n\nusing Neo4jQuery\nconn = connect_from_env()\nroc = ReadOnlyConnection(conn)","title":"Agentic Systems"},{"category":"section","location":"guide/agentic/#Least-privilege:-the-read-only-stack","page":"Agentic Systems","text":"An agent that only needs to answer questions should be unable to mutate the graph — not by convention, but by construction. Neo4jQuery gives you three independent layers; use all three for a production read agent.\n\nLayer 1 — client-side lexical guard. A ReadOnlyConnection classifies every statement with a write-clause scan before any HTTP request is built. A write throws ReadOnlyViolationError with zero wire traffic:\n\nroc = ReadOnlyConnection(conn)\n\nread_query(roc, \"MATCH (p:Person) RETURN p.name AS name\")   # ok\nread_query(roc, \"CREATE (p:Person)\")   # throws ReadOnlyViolationError — no request sent\n\nThis layer is a fail-fast convenience, not the guarantee. It is lexical, not a Cypher parser, so it has two known, opposite inaccuracies: it over-refuses a write keyword used as a bare alias (RETURN n AS create), and it cannot see a write hidden inside a called procedure (CALL some.write.proc()).\n\nLayer 2 — server-enforced access mode. A ReadOnlyConnection always sends access_mode=:read, so Neo4j itself enforces read-only server-side. This is the authoritative boundary — it catches exactly the procedure-write that the lexical scan in Layer 1 misses. The client classifier exists to fail fast; the server is what actually makes the guarantee.\n\nLayer 3 — least-privilege database user (recommended). Give the agent credentials that carry no write privilege at all. This is defense in depth beyond the library: even code that bypasses ReadOnlyConnection with a raw query(conn, …) cannot write, because the user is not allowed to.\n\n// Run once by an admin; the agent then connects AS this user.\n// (Exact privilege DDL is Enterprise/Aura-specific — the point is: no write grants.)\nCREATE USER agent_ro SET PASSWORD 'secret' CHANGE NOT REQUIRED;\nGRANT ROLE reader TO agent_ro;\n\nread_query and read_stream are the guarded read entry points; validate_cypher, graph_schema, schema_prompt, and vector_search all accept a ReadOnlyConnection and stay within the guard.","title":"Least privilege: the read-only stack"},{"category":"section","location":"guide/agentic/#Pre-flight-validation","page":"Agentic Systems","text":"Before executing model-generated Cypher, plan it on the server without running it. validate_cypher runs EXPLAIN <statement> under access_mode=:read: the server parses and plans the statement but never executes it.\n\nv = validate_cypher(roc, \"MATCH (n) RETURN n LIMIT 1\")\nv.valid              # true\nv.plan               # JSON.Object — the EXPLAIN plan (never executed)\n\nIt returns a typed NamedTuple (valid, error, plan):\n\nvalid=true, error=nothing, plan=<queryPlan> when the server planned it.\nvalid=false, error=<Neo4jQueryError>, plan=nothing on a genuine Cypher error.\n\nOnly a real Cypher error yields valid=false. Any other failure — transport, proxy, timeout, auth — is rethrown, never folded into a false \"invalid\" verdict. Distinguishing \"the Cypher is wrong\" from \"the network is down\" is the whole point: you retry one and repair the other.\n\nTwo safety properties matter for untrusted input:\n\nA leading PROFILE (which would execute the statement) is stripped; a leading EXPLAIN is not doubled; several leading modifiers collapse to exactly one EXPLAIN. PROFILE can never reach the wire.\nOn a ReadOnlyConnection, validate_cypher intentionally bypasses the Layer-1 lexical guard — because EXPLAIN never executes, validating even a write statement is provably side-effect-free. That lets you tell the model \"this would have been a write\" before it ever runs.\n\nnote: What pre-flight validation can and cannot buy you\nFor a read-only repair loop, validating before executing does not save turns or tokens: an invalid statement fails at plan time either way, and a valid one executes either way — the error an agent sees is the same. Pre-flight pays off when the statement could write (validate without running it, then refuse) or when execution itself is expensive. It cannot catch silent mistakes: a wrong property name returns nulls, not an error (properties are schema-optional), so no validation sees it — ground the model with graph_schema/schema_prompt instead of relying on validation to catch vocabulary drift.","title":"Pre-flight validation"},{"category":"section","location":"guide/agentic/#Error-messages-as-LLM-feedback","page":"Agentic Systems","text":"When valid=false, error.message carries the server's (line, column) position. That position is the highest-signal thing you can hand back to a model for self-correction — far better than \"your query was invalid\". Render it as a caret, the way a compiler does:\n\nbad = validate_cypher(roc, \"MATCH (n RETURN n\")   # missing ')'\n# bad.error.message includes: \"...(line 1, column 10 (offset: 9))\"\n\nThe feedback you return to the model looks like this:\n\nMATCH (n RETURN n\n         ^\nNeo.ClientError.Statement.SyntaxError: Invalid input 'RETURN':\nexpected ',', 'WHERE', ')' ... (line 1, column 10 (offset: 9))\n\nEvery Neo4jQueryError carries the same code/message pair, so this pattern works whether the error came from validate_cypher (pre-flight) or from an actual read_query/query execution. A self-correction loop closes over it:\n\nfunction answer(roc, prompt; max_repairs=2)\n    stmt, params = llm_generate(prompt)                 # your text-to-Cypher step\n    for _ in 0:max_repairs\n        v = validate_cypher(roc, stmt; parameters=params)\n        v.valid && return read_query(roc, stmt; parameters=params)\n        # Feed the server's line/column back — the model repairs against ground truth.\n        stmt, params = llm_repair(prompt, stmt, v.error.message)\n    end\n    error(\"could not produce valid Cypher after $max_repairs repairs\")\nend\n\nNote the pre-registered refutation baked into the loop: after a bounded number of repairs it fails loud, rather than silently returning an empty result.","title":"Error messages as LLM feedback"},{"category":"section","location":"guide/agentic/#Grounding-the-prompt","page":"Agentic Systems","text":"A text-to-Cypher model can only use labels, relationship types, and indexes that actually exist. Do not hand-roll a schema description in your prompt — it drifts from the live graph the moment someone adds a label. Read it from the server instead, so there is exactly one source of truth.\n\nschema_prompt renders the live schema as a compact, deterministic block ready to drop into a system prompt:\n\nsystem_prompt = \"\"\"\nYou translate natural-language questions into Neo4j Cypher.\nUse ONLY the labels, relationships, and indexes below.\n\n$(schema_prompt(roc))\n\"\"\"\n\nThe rendering is deterministic (safe to cache) and looks like:\n\nNode labels:\n(:Person {name: String, age?: Integer})\n(:Company {name: String})\nRelationships:\n(:Person)-[:KNOWS]->(:Person)\n(:Person)-[:WORKS_AT]->(:Company)\nVector indexes:\nVECTOR index `chunk_vec` on :Chunk(embedding), 384-dim cosine\n\nOptional properties are suffixed ?; multiple observed types are joined with |. Labels beyond max_labels (default 50) are reported with an explicit … and N more labels marker — never silently dropped.\n\nFor programmatic use, graph_schema returns a typed GraphSchema (labels, reltypes, indexes) instead of a string. It issues four read queries (db.schema.nodeTypeProperties(), db.schema.relTypeProperties(), a sampled connectivity MATCH … LIMIT 1000, and SHOW INDEXES), all under access_mode=:read — provably side-effect-free, so it is safe on a ReadOnlyConnection to a production database.","title":"Grounding the prompt"},{"category":"section","location":"guide/agentic/#Retry-taxonomy","page":"Agentic Systems","text":"There are two retry layers. Conflating them is the classic agent bug — either you double-retry a deterministic failure forever, or you fail to retry a recoverable one.\n\n1. Transport read-retry (automatic — you do not write this). A read that fails on a stale pooled connection is retried once inside query/read_query/stream/read_stream, because a read is provably side-effect-free. A timeout is never retried (it is treated as unrecoverable). This happens below your code.\n\n2. Work-level retry (yours). When the server signals a transient condition — a deadlock, a 429/503 — replay the whole idempotent unit of work. is_transient is the predicate that decides:\n\nis_transient(Neo4jQueryError(\"Neo.TransientError.Transaction.Terminated\", \"deadlock\"))  # true\nis_transient(Neo4jQueryError(\"Neo.ClientError.Statement.SyntaxError\", \"bad syntax\"))     # false\n\nCypher errors ride HTTP 202 with an errors[] body, so status codes alone cannot classify them — is_transient reads Neo4j's Neo.TransientError.* taxonomy (plus HTTP 429/503). Wrap the idempotent work:\n\nfunction with_retry(work; max_attempts=4)\n    for attempt in 1:max_attempts\n        try\n            return work()\n        catch e\n            if e isa Neo4jError && is_transient(e) && attempt < max_attempts\n                sleep(0.25 * 2.0^(attempt - 1))   # exponential backoff\n                continue\n            end\n            rethrow()   # non-transient, or out of attempts — fail loud\n        end\n    end\nend\n\n# `work` is replayed verbatim, so it must be idempotent.\nrows = with_retry() do\n    read_query(roc, cypher\"MATCH (p:Person {name: $name}) RETURN p\")\nend\n\nis_transient is deliberately false for failures a blind retry cannot fix:\n\nSyntax/constraint errors — retrying the same Cypher loops forever. Fix it (that is what validate_cypher is for).\nAuthenticationError — the same credentials will fail every time.\nTransactionExpiredError — the server has discarded the transaction (it expired, timed out, or was rolled back). The handle is dead; re-sending against it is pointless. Re-begin_transaction and replay the work against a fresh handle — this is a re-begin, not a blind retry:\n\nfunction run_tx(conn, work; max_attempts=3)\n    for attempt in 1:max_attempts\n        tx = begin_transaction(conn)\n        try\n            result = work(tx)      # your idempotent unit of work\n            commit!(tx)\n            return result\n        catch e\n            e isa TransactionExpiredError && attempt < max_attempts && continue  # fresh begin next loop\n            rethrow()\n        end\n    end\nend","title":"Retry taxonomy"},{"category":"section","location":"guide/agentic/#Timeouts:-bound-both-sides","page":"Agentic Systems","text":"An agent must never hang on a pathological query. Two independent timeouts apply (see Connections for the full semantics):\n\nClient-side timeout / readtimeout — aborts the client's wait. A timed-out request raises a typed Neo4jHTTPError instead of blocking the caller forever. It does not tell the server to stop.\nServer-side max_execution_time (seconds) — tells Neo4j itself to abort the query and return a Cypher error (requires Neo4j 2026.04+).\n\nThe pairing rule: set the client timeout larger than the server max_execution_time. Then the server's own abort wins and surfaces as a clean, typed Cypher error, instead of the client giving up first and leaving the query running server-side:\n\nresult = read_query(roc, expensive_cypher;\n    max_execution_time = 10,   # server aborts after 10s (Neo4j 2026.04+)\n    timeout = 15)              # client waits 15s (> server budget) so the server wins\n\nBoth max_execution_time and the companion tx_metadata ride through read_query/read_stream as well as query/stream.","title":"Timeouts: bound both sides"},{"category":"section","location":"guide/agentic/#Null-safe-parameters","page":"Agentic Systems","text":"Models extract parameters that are sometimes absent — an optional filter the question never mentioned. A missing value must not crash the wire or silently drop the key. Julia's nothing serializes to an explicit typed Null envelope ({\"$type\": \"Null\", \"_value\": null}) and round-trips as Cypher null:\n\n# The model found no city constraint for this question:\nmaybe_city = nothing\n\nq = cypher\"\"\"\n    MATCH (p:Person)\n    WHERE $maybe_city IS NULL OR p.city = $maybe_city\n    RETURN p.name AS name\n\"\"\"\nread_query(roc, q)   # `maybe_city` ships as a typed Null, not an omitted key\n\n@cypher_str captures nothing like any other local, so an optional parameter needs no special-casing in the caller — the \"absent\" case is just a value, encoded in the type system rather than in control flow.","title":"Null-safe parameters"},{"category":"section","location":"guide/agentic/#GraphRAG-retrieval","page":"Agentic Systems","text":"vector_search is the retrieval half of a GraphRAG loop: k-nearest- neighbour search over a Neo4j vector index.\n\nroc = ReadOnlyConnection(conn)\nhits = vector_search(roc, \"chunk_vec\", query_embedding; k=5)   # query_embedding::Vector{<:Real}\nfor h in hits\n    println(h.score, \"  \", h.properties[\"text\"])\nend\n\nRows come back ordered by descending similarity score. Two safety properties:\n\nThe index name, k, and the embedding are sent as parameters ($idx, $k, $vec) — never string-interpolated. A hostile or write-looking index name can neither inject Cypher nor bypass the read-only guard, because it is not part of the statement text.\nInputs are validated client-side, before any request: ArgumentError if k < 1, the index name is empty, or the embedding is empty, non-finite (NaN/Inf), or all-zero. A zero-norm vector has no direction, so cosine KNN against it is undefined — the server rejects it, and so does the client, loudly, up front. (This one caught a real bug: a zeros(384) placebo query vector is invalid input, not a neutral probe.)\n\nOn a ReadOnlyConnection the call funnels through read_query under server-enforced accessMode=Read — safe against a production read replica.\n\nCreate the index once with create_vector_index (a runtime helper; identifiers are sanitized because DDL cannot be parameterized):\n\ncreate_vector_index(conn, \"chunk_vec\", \"Chunk\", \"embedding\";\n                    dimensions=384, similarity=:cosine)","title":"GraphRAG retrieval"},{"category":"section","location":"guide/agentic/#Read-your-writes:-bookmarks-for-agent-chains","page":"Agentic Systems","text":"A multi-step agent often writes and then must read its own write back — create a task node, then query its status on the next step. In a cluster (Aura), the follow-up read may land on a replica that has not yet seen the write. Thread the write's bookmarks into the read so it waits until the replica has caught up to at least that point:\n\n# Step 1: the agent writes.\nw = query(conn, cypher\"CREATE (t:Task {id: $id, status: 'open'}) RETURN t\")\n\n# Step 2: a later step reads it back — bookmarks guarantee causal consistency.\nr = query(conn, cypher\"MATCH (t:Task {id: $id}) RETURN t.status AS status\";\n    bookmarks = w.bookmarks, access_mode = :read)\n\nEvery QueryResult (and streaming summary) exposes .bookmarks; commit returns them too. Chaining them across steps is what makes \"the agent sees what it just did\" a guarantee rather than a race.","title":"Read-your-writes: bookmarks for agent chains"},{"category":"section","location":"guide/agentic/#Putting-it-together","page":"Agentic Systems","text":"A single read-only text-to-Cypher agent step, composing the pieces above: ground the prompt from live schema, generate, validate before executing, repair against the server's line/column, and run the read under retry — with every failure mode typed and loud.\n\nfunction agent_step(roc, question; max_repairs=2)\n    schema = schema_prompt(roc)                              # ground (one source of truth)\n    stmt, params = llm_generate(question, schema)            # generate\n\n    for _ in 0:max_repairs\n        v = validate_cypher(roc, stmt; parameters=params)    # pre-flight: EXPLAIN, no execution\n        if v.valid\n            return with_retry() do                           # execute under transient-retry\n                read_query(roc, stmt; parameters=params)     # server-enforced read-only\n            end\n        end\n        stmt, params = llm_repair(question, stmt, v.error.message)   # self-correct on line/column\n    end\n    error(\"no valid Cypher after $max_repairs repairs\")      # pre-registered failure\nend","title":"Putting it together"},{"category":"section","location":"guide/transactions/#transactions","page":"Transactions","text":"Neo4jQuery supports both implicit (auto-commit) and explicit transactions.","title":"Transactions"},{"category":"section","location":"guide/transactions/#Explicit-transactions","page":"Transactions","text":"Open a transaction, run multiple queries, then commit or rollback:\n\ntx = begin_transaction(conn)\n\nname = \"Savings\"\nquery(tx, cypher\"CREATE (a:Account {name: $name})\")\nname = \"Checking\"\nquery(tx, cypher\"CREATE (a:Account {name: $name})\")\n\nbookmarks = commit!(tx)\nprintln(\"Committed with \", length(bookmarks), \" bookmark(s)\")","title":"Explicit transactions"},{"category":"section","location":"guide/transactions/#Rollback","page":"Transactions","text":"tx = begin_transaction(conn)\nquery(tx, \"CREATE (n:Temp)\")\nrollback!(tx)\nprintln(\"Rolled back — Temp node never persists\")","title":"Rollback"},{"category":"section","location":"guide/transactions/#Initial-statement","page":"Transactions","text":"begin_transaction accepts an optional initial statement — either a String or a CypherQuery:\n\ntx = begin_transaction(conn;\n    statement=cypher\"CREATE (n:Init) RETURN n\")\ncommit!(tx)\nprintln(\"Transaction with initial statement committed\")","title":"Initial statement"},{"category":"section","location":"guide/transactions/#Final-statement-on-commit","page":"Transactions","text":"commit! can run a final statement atomically with the commit:\n\ntx = begin_transaction(conn)\nbookmarks = commit!(tx;\n    statement=cypher\"CREATE (n:Final) RETURN n\")\nprintln(\"Committed with final statement, \", length(bookmarks), \" bookmark(s)\")","title":"Final statement on commit"},{"category":"section","location":"guide/transactions/#Using-@cypher_str-in-transactions","page":"Transactions","text":"The cypher\"\" macro is the recommended way to pass parameterised queries everywhere:\n\ntx = begin_transaction(conn)\n\nname = \"Alice\"\nage = 30\nresult = query(tx, cypher\"CREATE (p:Person {name: $name, age: $age}) RETURN p\")\nprintln(\"Created: \", result[1].p)\n\nname = \"Bob\"\nage = 25\nquery(tx, cypher\"CREATE (p:Person {name: $name, age: $age}) RETURN p\")\n\n# Create a relationship between them\nquery(tx, \"\"\"\n    MATCH (a:Person {name: 'Alice'}), (b:Person {name: 'Bob'})\n    CREATE (a)-[:KNOWS {since: 2024}]->(b)\n\"\"\")\n\nbookmarks = commit!(tx)\nprintln(\"Transaction committed\")","title":"Using @cypher_str in transactions"},{"category":"section","location":"guide/transactions/#Do-block-(recommended)","page":"Transactions","text":"The safest pattern — auto-commits on success, auto-rolls-back on exception:\n\ntransaction(conn) do tx\n    query(tx, \"CREATE (a:Person {name: 'Diana'})\")\n    query(tx, \"CREATE (b:Person {name: 'Edgar'})\")\n    query(tx, \"\"\"\n        MATCH (a:Person {name: 'Diana'}), (b:Person {name: 'Edgar'})\n        CREATE (a)-[:KNOWS]->(b)\n    \"\"\")\nend\nprintln(\"Do-block transaction committed\")\n\nIf any exception occurs inside the block, the transaction is automatically rolled back before the exception propagates:\n\ntry\n    transaction(conn) do tx\n        query(tx, \"CREATE (n:Temp)\")\n        error(\"something went wrong\")\n    end\ncatch e\n    println(\"Caught: \", e.msg)\n    println(\"Transaction was rolled back — no :Temp node was created\")\nend","title":"Do-block (recommended)"},{"category":"section","location":"guide/transactions/#Complete-example-using-do-block","page":"Transactions","text":"# Build a small graph atomically\ntransaction(conn) do tx\n    # Create people\n    for (n, a) in [(\"Fay\", 30), (\"George\", 25), (\"Helen\", 35)]\n        name = n\n        age = a\n        query(tx, cypher\"CREATE (p:Person {name: $name, age: $age})\")\n    end\n\n    # Create relationships\n    query(tx, \"\"\"\n        MATCH (a:Person {name: 'Fay'}), (b:Person {name: 'George'})\n        CREATE (a)-[:KNOWS {since: 2020}]->(b)\n    \"\"\")\n    query(tx, \"\"\"\n        MATCH (a:Person {name: 'Fay'}), (c:Person {name: 'Helen'})\n        CREATE (a)-[:KNOWS {since: 2022}]->(c)\n    \"\"\")\nend\nprintln(\"All operations committed together\")","title":"Complete example using do-block"},{"category":"section","location":"guide/transactions/#Transaction-state","page":"Transactions","text":"The Transaction struct tracks its lifecycle:\n\ntx = begin_transaction(conn)\nprintln(\"Committed: \", tx.committed, \", Rolled back: \", tx.rolled_back)\n\ncommit!(tx)\nprintln(\"Committed: \", tx.committed)\n\nAdditional fields: tx.expires (server-reported expiry timestamp) and tx.cluster_affinity (Aura cluster routing, or nothing).\n\nAttempting to use a committed or rolled-back transaction raises an error:\n\ntx = begin_transaction(conn)\ncommit!(tx)\n\n# This will throw an error:\ntry\n    query(tx, \"RETURN 1\")\ncatch e\n    println(\"Error: \", e)\nend","title":"Transaction state"},{"category":"section","location":"#Neo4jQuery.jl","page":"Home","text":"A modern Julia client for Neo4j using the Query API v2.\n\n(Image: Build Status) (Image: Documentation (stable)) (Image: Documentation (dev)) (Image: codecov) (Image: Aqua QA) (Image: JET)","title":"Neo4jQuery.jl"},{"category":"section","location":"#Features","page":"Home","text":"Query API v2 — uses Neo4j's modern HTTP endpoint with Typed JSON for lossless data exchange.\nParameterised Cypher — the @cypher_str macro captures local variables as safe query parameters, preventing injection and enabling server-side caching.\nExplicit & implicit transactions — both auto-commit queries and full begin/commit/rollback lifecycle with a convenient do-block API.\nStreaming results — row-by-row iteration over JSONL responses for memory-efficient processing of large result sets.\nRich type mapping — automatic round-trip conversion between Julia types (Int64, Float64, Date, DateTime, ZonedDateTime, …) and Neo4j's type system, including spatial (CypherPoint), temporal (CypherDuration), and vector (CypherVector) values.\nGraph DSL — the unified @cypher macro plus @create, @merge, and @relate let you write Julia-native graph operations that compile to parameterised Cypher at macro-expansion time. @cypher offers >> chain operators, bare type annotations, function-call clauses, auto-SET, and comprehension forms.\nFull Cypher coverage — directed, left-arrow, and undirected patterns; variable-length relationships; CASE/WHEN expressions; EXISTS subqueries; regex matching (matches()); UNION/UNION ALL; CALL subqueries; LOAD CSV; FOREACH; and index/constraint management.\nSchema declarations — @node and @rel register typed schemas with validation for safer graph mutations.\nFlexible authentication — BasicAuth (HTTP Basic) and BearerAuth (token) with an extension point for custom strategies.\nEnvironment configuration — connect_from_env loads credentials from .env files or environment variables with automatic URI scheme parsing.","title":"Features"},{"category":"section","location":"#Quick-Start","page":"Home","text":"using Neo4jQuery\n\n# Connect\nconn = connect(\"localhost\", \"neo4j\"; port=7474, auth=BasicAuth(\"neo4j\", \"password\"))\n\n# Query\nresult = query(conn, \"MATCH (p:Person) RETURN p.name AS name, p.age AS age\")\nfor row in result\n    println(\"\\$(row.name) is \\$(row.age) years old\")\nend\n\n# Parameterised query\nname = \"Alice\"\nq = cypher\"MATCH (p:Person {name: $name}) RETURN p\"\nresult = query(conn, q)\n\nSee the Getting Started guide for installation instructions and a more complete walkthrough.","title":"Quick Start"},{"category":"section","location":"#Package-Overview","page":"Home","text":"Pages = [\n    \"getting_started.md\",\n    \"guide/connections.md\",\n    \"guide/queries.md\",\n    \"guide/transactions.md\",\n    \"guide/streaming.md\",\n    \"guide/dsl.md\",\n    \"guide/agentic.md\",\n    \"guide/biomedical_case_study.md\",\n    \"api.md\",\n]\nDepth = 1","title":"Package Overview"},{"category":"section","location":"#LLM-Reference","page":"Home","text":"For LLM and agentic integrators there is a single-page, machine-oriented API reference at llm.md (kept out of the navigation on purpose — it is meant to be handed to an agent whole).","title":"LLM Reference"},{"category":"section","location":"guide/streaming/#streaming","page":"Streaming","text":"For large result sets, streaming avoids loading all rows into memory at once. Results arrive as JSONL (one JSON object per line) and are parsed lazily.\n\nStreaming is genuinely incremental: stream issues the HTTP request on a background task that drains the response body into a buffer as bytes arrive, and each iterate reads the next row as soon as it lands — a consumer can process the first row long before the server has sent the last. (The request itself is bounded by the connection's read timeout, which covers the whole transfer, not idle time; see Connections.)\n\nOne honest caveat: the internal buffer is unbounded, so if you consume rows slower than the server sends them, the undrained tail of the response still accumulates in memory. Incremental streaming buys first-row latency, not bounded-memory backpressure.","title":"Streaming"},{"category":"section","location":"guide/streaming/#Basic-usage","page":"Streaming","text":"sr = stream(conn, \"MATCH (p:Person) RETURN p.name AS name, p.age AS age\")\n\nfor row in sr\n    println(row.name, \" — \", row.age)\nend\n\nEach iterate call reads and parses the next row from the HTTP response body.","title":"Basic usage"},{"category":"section","location":"guide/streaming/#Streaming-in-transactions","page":"Streaming","text":"# Implicit transaction\nsr = stream(conn, \"MATCH (p) RETURN p\"; access_mode=:read)\ncollect(sr)  # consume the stream\nprintln(\"Streamed \", length(collect(stream(conn, \"MATCH (p) RETURN p\"; access_mode=:read))), \" rows\")\n\n# Explicit transaction\ntx = begin_transaction(conn)\nsr = stream(tx, \"MATCH (p) RETURN p\")\nrows = collect(sr)\ncommit!(tx)\nprintln(\"Streamed \", length(rows), \" rows in transaction\")","title":"Streaming in transactions"},{"category":"section","location":"guide/streaming/#Options","page":"Streaming","text":"stream accepts the same keyword arguments as query:\n\nKeyword Description\nparameters Query parameters\naccess_mode :read or :write\ninclude_counters Include mutation counters in summary\nbookmarks Causal consistency bookmarks\nimpersonated_user User impersonation","title":"Options"},{"category":"section","location":"guide/streaming/#Summary","page":"Streaming","text":"After fully consuming the stream, call summary to get metadata:\n\nsr = stream(conn, \"MATCH (p:Person) RETURN p\")\nrows = collect(sr)   # consume all rows\n\ns = summary(sr)\nprintln(\"Bookmarks: \", length(s.bookmarks))\n\nnote: Note\nsummary must be explicitly imported with import Neo4jQuery: summary because Base.summary takes precedence over the re-exported name. Alternatively, use the qualified form Neo4jQuery.summary(sr).\n\nwarning: Warning\nsummary is only available after the stream has been fully consumed. Calling it mid-stream will block until all remaining rows are read.","title":"Summary"},{"category":"section","location":"guide/streaming/#Collecting-rows","page":"Streaming","text":"You can materialize the entire stream with collect:\n\nsr = stream(conn, \"MATCH (p:Person) RETURN p.name AS name, p.age AS age\")\nrows = collect(sr)\n\n# rows is a Vector; use normal Julia operations\nnames = [r.name for r in rows]\nages  = [r.age  for r in rows]\nprintln(\"Names: \", names)\nprintln(\"Ages: \", ages)","title":"Collecting rows"},{"category":"section","location":"guide/streaming/#Stopping-early","page":"Streaming","text":"If you only need part of a stream, call close to abandon the rest and release the underlying HTTP connection:\n\nsr = stream(conn, \"MATCH (p:Person) RETURN p.name AS name\")\nfor row in sr\n    println(row.name)\n    if row.name == \"Alice\"\n        close(sr)   # stop early — releases the connection\n        break\n    end\nend\n\nAfter close(sr), further iteration yields nothing and a second close is a no-op.\n\nwarning: Warning\nDo not simply drop a partially-consumed StreamingResult on the floor. Until it is either fully consumed or closed, the background task keeps draining the response and the HTTP connection stays checked out until the server finishes sending. Prefer close(sr) (or consume to the end) so the connection is released promptly.","title":"Stopping early"},{"category":"section","location":"guide/streaming/#Streaming-with-parameters","page":"Streaming","text":"# Recommended: use cypher\"\" for parameterised streaming\nmin_age = 25\nsr = stream(conn, cypher\"MATCH (p:Person) WHERE p.age > $min_age RETURN p.name AS name\")\n\nfor row in sr\n    println(row.name)\nend\n\n# Also works: raw string with parameters dict\nsr = stream(conn, \"MATCH (p:Person) WHERE p.age > \\$min_age RETURN p.name AS name\",\n    parameters=Dict{String,Any}(\"min_age\" => 25))\n\nfor row in sr\n    println(row.name)\nend","title":"Streaming with parameters"},{"category":"section","location":"guide/streaming/#Streaming-inside-a-transaction","page":"Streaming","text":"Streaming works within explicit transactions for multi-step workflows:\n\ntransaction(conn) do tx\n    # Step 1: create a node\n    query(tx, \"CREATE (p:Person {name: 'Diana', age: 28})\")\n\n    # Step 2: stream results from the same transaction\n    sr = stream(tx, \"MATCH (p:Person) RETURN p.name AS name\")\n    for row in sr\n        println(\"Found: \", row.name)\n    end\nend","title":"Streaming inside a transaction"},{"category":"section","location":"guide/streaming/#CypherQuery-support","page":"Streaming","text":"name = \"Alice\"\nq = cypher\"MATCH (p:Person {name: $name}) RETURN p\"\nsr = stream(conn, q)\n\nfor row in sr\n    println(row.p)\nend","title":"CypherQuery support"},{"category":"section","location":"guide/streaming/#StreamingResult-details","page":"Streaming","text":"A StreamingResult tracks its consumption state:\n\nField Type Description\nfields Vector Column names\nconsumed Bool true after all rows have been read\n_summary internal Populated after consumption; access via summary\n\nInternally a StreamingResult also owns the background task running the HTTP request and the buffer it drains into; close(sr) tears both down. The task surfaces any transport error (a dropped connection, a read timeout) when the stream ends, so a truncated result raises rather than silently ending short.\n\nThe iterator protocol (Base.iterate) is implemented, so streaming results work with for loops, collect, comprehensions, and any iterator combinator. Base.IteratorSize returns SizeUnknown() and Base.eltype returns NamedTuple.","title":"StreamingResult details"}]
}
